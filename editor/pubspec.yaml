name: editor
publish_to: none
resolution: workspace

environment:
  sdk: ^3.13.0

dependencies:
  flutter:
    sdk: flutter

  state:
  mouse_cursor:

  color:
  geometry:
  kernel:
  scene:
  renderer:
  ui:
  schema:
  tool:
  
  equatable: ^2.1.0
  web_socket_channel: ^3.0.3
