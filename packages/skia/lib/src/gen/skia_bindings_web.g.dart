// ignore_for_file: unused_import, unused_local_variable, unnecessary_cast
// AUTO GENERATED FILE, DO NOT EDIT.
//
// Generated by `package:jsgen`.
// ignore_for_file: type=lint
import 'dart:typed_data';
import 'dart:js_interop';
import 'dart:js_interop_unsafe';

import 'package:ffigen_js/ffigen_js.dart';


extension type GeneratedBindings(NativeLibrary _) implements JSObject {
  static GeneratedBindings get instance => NativeLibrary.instance as GeneratedBindings;

  static void initBindings(String moduleName) {
    var lib = globalContext.getProperty(moduleName.toJS);
    if (lib == null) {
      throw Exception("Failed to find JS module ${moduleName}");
    }
    NativeLibrary.instance = lib as NativeLibrary;
  }

  external font_provider_t _motif_font_provider_create();
  external void _motif_font_provider_destroy(
    font_provider_t provider,
  );
  external int _motif_font_provider_add(
    font_provider_t provider,
    Pointer<Uint8> data,
    size_t length,
    int index,
    Pointer<Char> family,
  );
  external int _motif_font_provider_family_count(
    font_provider_t provider,
  );
  external size_t _motif_font_provider_family_name(
    font_provider_t provider,
    int index,
    Pointer<Char> buffer,
  );
  external paragraph_style_t _motif_paragraph_style_create();
  external void _motif_paragraph_style_destroy(
    paragraph_style_t style,
  );
  external text_style_t _motif_text_style_create();
  external void _motif_text_style_destroy(
    text_style_t style,
  );
  external double _motif_text_style_get_font_size(
    text_style_t style,
  );
  external void _motif_text_style_set_font_size(
    text_style_t style,
    double size,
  );
  external size_t _motif_text_style_get_font_families_count(
    text_style_t style,
  );
  external size_t _motif_text_style_get_font_family(
    text_style_t style,
    int index,
    Pointer<Char> buffer,
  );
  external void _motif_text_style_set_font_families(
    text_style_t style,
    Pointer<PointerClass<Char>> families,
    int count,
  );
  external void _motif_paragraph_destroy(
    paragraph_t p,
  );
  external void _motif_paragraph_layout(
    paragraph_t p,
    double width,
  );
  external double _motif_paragraph_get_height(
    paragraph_t p,
  );
  external double _motif_paragraph_get_longest_line(
    paragraph_t p,
  );
  external double _motif_paragraph_get_max_intrinsic_width(
    paragraph_t p,
  );
  external double _motif_paragraph_get_min_intrinsic_width(
    paragraph_t p,
  );
  external size_t _motif_paragraph_get_line_metrics_count(
    paragraph_t p,
  );
  external Pointer<line_metrics> _motif_paragraph_get_line_metrics(
    paragraph_t p,
  );
  external size_t _motif_paragraph_get_glyph_metrics_count(
    paragraph_t p,
  );
  external Pointer<glyph_metrics> _motif_paragraph_get_glyph_metrics(
    paragraph_t p,
  );
  external Pointer<glyph_path> _motif_paragraph_get_glyph_path(
    paragraph_t p,
    size_t index,
  );
  external paragraph_builder_t _motif_paragraph_builder_create(
    paragraph_style_t style,
    font_provider_t provider,
  );
  external void _motif_paragraph_builder_destroy(
    paragraph_builder_t builder,
  );
  external void _motif_paragraph_builder_add_text(
    paragraph_builder_t builder,
    Pointer<Char> text,
  );
  external void _motif_paragraph_builder_push_style(
    paragraph_builder_t builder,
    text_style_t style,
  );
  external void _motif_paragraph_builder_pop_style(
    paragraph_builder_t builder,
  );
  external paragraph_t _motif_paragraph_builder_build(
    paragraph_builder_t builder,
  );
}

Dartfont_provider_t motif_font_provider_create() {
  final result = GeneratedBindings.instance._motif_font_provider_create();
  return Dartfont_provider_t(result);
}

void motif_font_provider_destroy(
  Dartfont_provider_t provider,
) {
  final result = GeneratedBindings.instance._motif_font_provider_destroy(provider as Pointer<font_provider>);
  return result;
}

int motif_font_provider_add(
  Dartfont_provider_t provider,
  Pointer<Uint8> data,
  Dartsize_t length,
  int index,
  Pointer<Char> family,
) {
  final result = GeneratedBindings.instance._motif_font_provider_add(
    provider as Pointer<font_provider>,
    data,
    length,
    index,
    family,
  );
  return result;
}

int motif_font_provider_family_count(
  Dartfont_provider_t provider,
) {
  final result = GeneratedBindings.instance._motif_font_provider_family_count(provider as Pointer<font_provider>);
  return result;
}

Dartsize_t motif_font_provider_family_name(
  Dartfont_provider_t provider,
  int index,
  Pointer<Char> buffer,
) {
  final result = GeneratedBindings.instance._motif_font_provider_family_name(
    provider as Pointer<font_provider>,
    index,
    buffer,
  );
  return result;
}

Dartparagraph_style_t motif_paragraph_style_create() {
  final result = GeneratedBindings.instance._motif_paragraph_style_create();
  return Dartparagraph_style_t(result);
}

void motif_paragraph_style_destroy(
  Dartparagraph_style_t style,
) {
  final result = GeneratedBindings.instance._motif_paragraph_style_destroy(style as Pointer<paragraph_style>);
  return result;
}

Darttext_style_t motif_text_style_create() {
  final result = GeneratedBindings.instance._motif_text_style_create();
  return Darttext_style_t(result);
}

void motif_text_style_destroy(
  Darttext_style_t style,
) {
  final result = GeneratedBindings.instance._motif_text_style_destroy(style as Pointer<text_style>);
  return result;
}

double motif_text_style_get_font_size(
  Darttext_style_t style,
) {
  final result = GeneratedBindings.instance._motif_text_style_get_font_size(style as Pointer<text_style>);
  return result;
}

void motif_text_style_set_font_size(
  Darttext_style_t style,
  double size,
) {
  final result = GeneratedBindings.instance._motif_text_style_set_font_size(style as Pointer<text_style>, size);
  return result;
}

Dartsize_t motif_text_style_get_font_families_count(
  Darttext_style_t style,
) {
  final result = GeneratedBindings.instance._motif_text_style_get_font_families_count(style as Pointer<text_style>);
  return result;
}

Dartsize_t motif_text_style_get_font_family(
  Darttext_style_t style,
  int index,
  Pointer<Char> buffer,
) {
  final result = GeneratedBindings.instance._motif_text_style_get_font_family(
    style as Pointer<text_style>,
    index,
    buffer,
  );
  return result;
}

void motif_text_style_set_font_families(
  Darttext_style_t style,
  Pointer<PointerClass<Char>> families,
  int count,
) {
  final result = GeneratedBindings.instance._motif_text_style_set_font_families(
    style as Pointer<text_style>,
    families,
    count,
  );
  return result;
}

void motif_paragraph_destroy(
  Dartparagraph_t p,
) {
  final result = GeneratedBindings.instance._motif_paragraph_destroy(p as Pointer<paragraph>);
  return result;
}

void motif_paragraph_layout(
  Dartparagraph_t p,
  double width,
) {
  final result = GeneratedBindings.instance._motif_paragraph_layout(p as Pointer<paragraph>, width);
  return result;
}

double motif_paragraph_get_height(
  Dartparagraph_t p,
) {
  final result = GeneratedBindings.instance._motif_paragraph_get_height(p as Pointer<paragraph>);
  return result;
}

double motif_paragraph_get_longest_line(
  Dartparagraph_t p,
) {
  final result = GeneratedBindings.instance._motif_paragraph_get_longest_line(p as Pointer<paragraph>);
  return result;
}

double motif_paragraph_get_max_intrinsic_width(
  Dartparagraph_t p,
) {
  final result = GeneratedBindings.instance._motif_paragraph_get_max_intrinsic_width(p as Pointer<paragraph>);
  return result;
}

double motif_paragraph_get_min_intrinsic_width(
  Dartparagraph_t p,
) {
  final result = GeneratedBindings.instance._motif_paragraph_get_min_intrinsic_width(p as Pointer<paragraph>);
  return result;
}

Dartsize_t motif_paragraph_get_line_metrics_count(
  Dartparagraph_t p,
) {
  final result = GeneratedBindings.instance._motif_paragraph_get_line_metrics_count(p as Pointer<paragraph>);
  return result;
}

Pointer<line_metrics> motif_paragraph_get_line_metrics(
  Dartparagraph_t p,
) {
  final result = GeneratedBindings.instance._motif_paragraph_get_line_metrics(p as Pointer<paragraph>);
  return Pointer<line_metrics>(result);
}

Dartsize_t motif_paragraph_get_glyph_metrics_count(
  Dartparagraph_t p,
) {
  final result = GeneratedBindings.instance._motif_paragraph_get_glyph_metrics_count(p as Pointer<paragraph>);
  return result;
}

Pointer<glyph_metrics> motif_paragraph_get_glyph_metrics(
  Dartparagraph_t p,
) {
  final result = GeneratedBindings.instance._motif_paragraph_get_glyph_metrics(p as Pointer<paragraph>);
  return Pointer<glyph_metrics>(result);
}

Pointer<glyph_path> motif_paragraph_get_glyph_path(
  Dartparagraph_t p,
  Dartsize_t index,
) {
  final result = GeneratedBindings.instance._motif_paragraph_get_glyph_path(p as Pointer<paragraph>, index);
  return Pointer<glyph_path>(result);
}

Dartparagraph_builder_t motif_paragraph_builder_create(
  Dartparagraph_style_t style,
  Dartfont_provider_t provider,
) {
  final result = GeneratedBindings.instance._motif_paragraph_builder_create(
    style as Pointer<paragraph_style>,
    provider as Pointer<font_provider>,
  );
  return Dartparagraph_builder_t(result);
}

void motif_paragraph_builder_destroy(
  Dartparagraph_builder_t builder,
) {
  final result = GeneratedBindings.instance._motif_paragraph_builder_destroy(builder as Pointer<paragraph_builder>);
  return result;
}

void motif_paragraph_builder_add_text(
  Dartparagraph_builder_t builder,
  Pointer<Char> text,
) {
  final result = GeneratedBindings.instance._motif_paragraph_builder_add_text(
    builder as Pointer<paragraph_builder>,
    text,
  );
  return result;
}

void motif_paragraph_builder_push_style(
  Dartparagraph_builder_t builder,
  Darttext_style_t style,
) {
  final result = GeneratedBindings.instance._motif_paragraph_builder_push_style(
    builder as Pointer<paragraph_builder>,
    style as Pointer<text_style>,
  );
  return result;
}

void motif_paragraph_builder_pop_style(
  Dartparagraph_builder_t builder,
) {
  final result = GeneratedBindings.instance._motif_paragraph_builder_pop_style(builder as Pointer<paragraph_builder>);
  return result;
}

Dartparagraph_t motif_paragraph_builder_build(
  Dartparagraph_builder_t builder,
) {
  final result = GeneratedBindings.instance._motif_paragraph_builder_build(builder as Pointer<paragraph_builder>);
  return Dartparagraph_t(result);
}

enum em_promise_result_t {
  EM_PROMISE_FULFILL(0),
  EM_PROMISE_MATCH(1),
  EM_PROMISE_MATCH_RELEASE(2),
  EM_PROMISE_REJECT(3);

  final int value;
  const em_promise_result_t(this.value);

  static em_promise_result_t fromValue(int value) => switch (value) {
    0 => EM_PROMISE_FULFILL,
    1 => EM_PROMISE_MATCH,
    2 => EM_PROMISE_MATCH_RELEASE,
    3 => EM_PROMISE_REJECT,
    _ => throw ArgumentError("Unknown value for em_promise_result_t: $value"),
  };
}

typedef font_provider_t = Pointer<font_provider>;
typedef Dartfont_provider_t = Pointer<font_provider>;

extension font_providerExt on Pointer<font_provider> {
  font_provider toDart() {
    return font_provider(this);
  }
  font_provider get ref => toDart();
  font_provider operator [](int index) => Pointer<font_provider>(this.address + sizeOf * index).toDart();
  operator []=(int index, font_provider value) => NativeLibrary.instance.setValue(this[index].address, value.address.toJS, '*');
  int get sizeOf => 0;
}

final class font_provider extends Struct {
  Pointer<font_provider> get address => super.address.cast();
  font_provider(super.address);

  static Pointer<font_provider> stackAlloc() {
    return Pointer<font_provider>(NativeLibrary.instance.stackAlloc<font_provider>(0));
  }
}

typedef size_t = int;
typedef Dartsize_t = int;
typedef paragraph_style_t = Pointer<paragraph_style>;
typedef Dartparagraph_style_t = Pointer<paragraph_style>;

extension paragraph_styleExt on Pointer<paragraph_style> {
  paragraph_style toDart() {
    return paragraph_style(this);
  }
  paragraph_style get ref => toDart();
  paragraph_style operator [](int index) => Pointer<paragraph_style>(this.address + sizeOf * index).toDart();
  operator []=(int index, paragraph_style value) => NativeLibrary.instance.setValue(this[index].address, value.address.toJS, '*');
  int get sizeOf => 0;
}

final class paragraph_style extends Struct {
  Pointer<paragraph_style> get address => super.address.cast();
  paragraph_style(super.address);

  static Pointer<paragraph_style> stackAlloc() {
    return Pointer<paragraph_style>(NativeLibrary.instance.stackAlloc<paragraph_style>(0));
  }
}

typedef text_style_t = Pointer<text_style>;
typedef Darttext_style_t = Pointer<text_style>;

extension text_styleExt on Pointer<text_style> {
  text_style toDart() {
    return text_style(this);
  }
  text_style get ref => toDart();
  text_style operator [](int index) => Pointer<text_style>(this.address + sizeOf * index).toDart();
  operator []=(int index, text_style value) => NativeLibrary.instance.setValue(this[index].address, value.address.toJS, '*');
  int get sizeOf => 0;
}

final class text_style extends Struct {
  Pointer<text_style> get address => super.address.cast();
  text_style(super.address);

  static Pointer<text_style> stackAlloc() {
    return Pointer<text_style>(NativeLibrary.instance.stackAlloc<text_style>(0));
  }
}

typedef paragraph_t = Pointer<paragraph>;
typedef Dartparagraph_t = Pointer<paragraph>;

extension paragraphExt on Pointer<paragraph> {
  paragraph toDart() {
    return paragraph(this);
  }
  paragraph get ref => toDart();
  paragraph operator [](int index) => Pointer<paragraph>(this.address + sizeOf * index).toDart();
  operator []=(int index, paragraph value) => NativeLibrary.instance.setValue(this[index].address, value.address.toJS, '*');
  int get sizeOf => 0;
}

final class paragraph extends Struct {
  Pointer<paragraph> get address => super.address.cast();
  paragraph(super.address);

  static Pointer<paragraph> stackAlloc() {
    return Pointer<paragraph>(NativeLibrary.instance.stackAlloc<paragraph>(0));
  }
}

extension line_metricsExt on Pointer<line_metrics> {
  line_metrics toDart() {
    return line_metrics(this);
  }
  line_metrics get ref => toDart();
  line_metrics operator [](int index) => Pointer<line_metrics>(this.address + sizeOf * index).toDart();
  operator []=(int index, line_metrics value) => NativeLibrary.instance.setValue(this[index].address, value.address.toJS, '*');
  int get sizeOf => 32;
}

final class line_metrics extends Struct {
  Pointer<line_metrics> get address => super.address.cast();
  double get left {
    final addr = Pointer<line_metrics>(this.address.addr + 0);
    final value = NativeLibrary.instance.getValue(addr, 'float').toDartDouble;
    return value;
  }

  set left(double val) {
    NativeLibrary.instance.setValue(Pointer<line_metrics>(this.address.addr + 0), val.toJS, 'float');
  }

  double get baseline {
    final addr = Pointer<line_metrics>(this.address.addr + 4);
    final value = NativeLibrary.instance.getValue(addr, 'float').toDartDouble;
    return value;
  }

  set baseline(double val) {
    NativeLibrary.instance.setValue(Pointer<line_metrics>(this.address.addr + 4), val.toJS, 'float');
  }

  double get ascent {
    final addr = Pointer<line_metrics>(this.address.addr + 8);
    final value = NativeLibrary.instance.getValue(addr, 'float').toDartDouble;
    return value;
  }

  set ascent(double val) {
    NativeLibrary.instance.setValue(Pointer<line_metrics>(this.address.addr + 8), val.toJS, 'float');
  }

  double get descent {
    final addr = Pointer<line_metrics>(this.address.addr + 12);
    final value = NativeLibrary.instance.getValue(addr, 'float').toDartDouble;
    return value;
  }

  set descent(double val) {
    NativeLibrary.instance.setValue(Pointer<line_metrics>(this.address.addr + 12), val.toJS, 'float');
  }

  double get width {
    final addr = Pointer<line_metrics>(this.address.addr + 16);
    final value = NativeLibrary.instance.getValue(addr, 'float').toDartDouble;
    return value;
  }

  set width(double val) {
    NativeLibrary.instance.setValue(Pointer<line_metrics>(this.address.addr + 16), val.toJS, 'float');
  }

  double get height {
    final addr = Pointer<line_metrics>(this.address.addr + 20);
    final value = NativeLibrary.instance.getValue(addr, 'float').toDartDouble;
    return value;
  }

  set height(double val) {
    NativeLibrary.instance.setValue(Pointer<line_metrics>(this.address.addr + 20), val.toJS, 'float');
  }

  int get glyph_start {
    final addr = Pointer<line_metrics>(this.address.addr + 24);
    final value = NativeLibrary.instance.getValue(addr, 'i32').toDartInt;
    return value;
  }

  set glyph_start(int val) {
    NativeLibrary.instance.setValue(Pointer<line_metrics>(this.address.addr + 24), val.toJS, 'i32');
  }

  int get glyph_end {
    final addr = Pointer<line_metrics>(this.address.addr + 28);
    final value = NativeLibrary.instance.getValue(addr, 'i32').toDartInt;
    return value;
  }

  set glyph_end(int val) {
    NativeLibrary.instance.setValue(Pointer<line_metrics>(this.address.addr + 28), val.toJS, 'i32');
  }

  line_metrics(super.address);

  static Pointer<line_metrics> stackAlloc() {
    return Pointer<line_metrics>(NativeLibrary.instance.stackAlloc<line_metrics>(32));
  }
}

extension glyph_metricsExt on Pointer<glyph_metrics> {
  glyph_metrics toDart() {
    return glyph_metrics(this);
  }
  glyph_metrics get ref => toDart();
  glyph_metrics operator [](int index) => Pointer<glyph_metrics>(this.address + sizeOf * index).toDart();
  operator []=(int index, glyph_metrics value) => NativeLibrary.instance.setValue(this[index].address, value.address.toJS, '*');
  int get sizeOf => 28;
}

final class glyph_metrics extends Struct {
  Pointer<glyph_metrics> get address => super.address.cast();
  double get x {
    final addr = Pointer<glyph_metrics>(this.address.addr + 0);
    final value = NativeLibrary.instance.getValue(addr, 'float').toDartDouble;
    return value;
  }

  set x(double val) {
    NativeLibrary.instance.setValue(Pointer<glyph_metrics>(this.address.addr + 0), val.toJS, 'float');
  }

  double get y {
    final addr = Pointer<glyph_metrics>(this.address.addr + 4);
    final value = NativeLibrary.instance.getValue(addr, 'float').toDartDouble;
    return value;
  }

  set y(double val) {
    NativeLibrary.instance.setValue(Pointer<glyph_metrics>(this.address.addr + 4), val.toJS, 'float');
  }

  double get left {
    final addr = Pointer<glyph_metrics>(this.address.addr + 8);
    final value = NativeLibrary.instance.getValue(addr, 'float').toDartDouble;
    return value;
  }

  set left(double val) {
    NativeLibrary.instance.setValue(Pointer<glyph_metrics>(this.address.addr + 8), val.toJS, 'float');
  }

  double get top {
    final addr = Pointer<glyph_metrics>(this.address.addr + 12);
    final value = NativeLibrary.instance.getValue(addr, 'float').toDartDouble;
    return value;
  }

  set top(double val) {
    NativeLibrary.instance.setValue(Pointer<glyph_metrics>(this.address.addr + 12), val.toJS, 'float');
  }

  double get right {
    final addr = Pointer<glyph_metrics>(this.address.addr + 16);
    final value = NativeLibrary.instance.getValue(addr, 'float').toDartDouble;
    return value;
  }

  set right(double val) {
    NativeLibrary.instance.setValue(Pointer<glyph_metrics>(this.address.addr + 16), val.toJS, 'float');
  }

  double get bottom {
    final addr = Pointer<glyph_metrics>(this.address.addr + 20);
    final value = NativeLibrary.instance.getValue(addr, 'float').toDartDouble;
    return value;
  }

  set bottom(double val) {
    NativeLibrary.instance.setValue(Pointer<glyph_metrics>(this.address.addr + 20), val.toJS, 'float');
  }

  int get id {
    final addr = Pointer<glyph_metrics>(this.address.addr + 24);
    final value = NativeLibrary.instance.getValue(addr, 'i16').toDartInt;
    return value;
  }

  set id(int val) {
    NativeLibrary.instance.setValue(Pointer<glyph_metrics>(this.address.addr + 24), val.toJS, 'i16');
  }

  int get font {
    final addr = Pointer<glyph_metrics>(this.address.addr + 26);
    final value = NativeLibrary.instance.getValue(addr, 'i16').toDartInt;
    return value;
  }

  set font(int val) {
    NativeLibrary.instance.setValue(Pointer<glyph_metrics>(this.address.addr + 26), val.toJS, 'i16');
  }

  glyph_metrics(super.address);

  static Pointer<glyph_metrics> stackAlloc() {
    return Pointer<glyph_metrics>(NativeLibrary.instance.stackAlloc<glyph_metrics>(28));
  }
}

extension glyph_pathExt on Pointer<glyph_path> {
  glyph_path toDart() {
    return glyph_path(this);
  }
  glyph_path get ref => toDart();
  glyph_path operator [](int index) => Pointer<glyph_path>(this.address + sizeOf * index).toDart();
  operator []=(int index, glyph_path value) => NativeLibrary.instance.setValue(this[index].address, value.address.toJS, '*');
  int get sizeOf => 16;
}

final class glyph_path extends Struct {
  Pointer<glyph_path> get address => super.address.cast();
  int get verb_count {
    final addr = Pointer<glyph_path>(this.address.addr + 0);
    final value = NativeLibrary.instance.getValue(addr, 'i32').toDartInt;
    return value;
  }

  set verb_count(int val) {
    NativeLibrary.instance.setValue(Pointer<glyph_path>(this.address.addr + 0), val.toJS, 'i32');
  }

  int get point_count {
    final addr = Pointer<glyph_path>(this.address.addr + 4);
    final value = NativeLibrary.instance.getValue(addr, 'i32').toDartInt;
    return value;
  }

  set point_count(int val) {
    NativeLibrary.instance.setValue(Pointer<glyph_path>(this.address.addr + 4), val.toJS, 'i32');
  }

  Pointer<Uint8> get verbs {
    final addr = Pointer<glyph_path>(this.address.addr + 8);
    final value = NativeLibrary.instance.getValue(addr, '*');
    return Pointer<Uint8>(value.toDartInt);
  }

  set verbs(Pointer<Uint8> val) {
    NativeLibrary.instance.setValue(Pointer<glyph_path>(this.address.addr + 8), val.toJS, '*');
  }

  Pointer<Float32> get points {
    final addr = Pointer<glyph_path>(this.address.addr + 12);
    final value = NativeLibrary.instance.getValue(addr, '*');
    return Pointer<Float32>(value.toDartInt);
  }

  set points(Pointer<Float32> val) {
    NativeLibrary.instance.setValue(Pointer<glyph_path>(this.address.addr + 12), val.toJS, '*');
  }

  glyph_path(super.address);

  static Pointer<glyph_path> stackAlloc() {
    return Pointer<glyph_path>(NativeLibrary.instance.stackAlloc<glyph_path>(16));
  }
}

typedef paragraph_builder_t = Pointer<paragraph_builder>;
typedef Dartparagraph_builder_t = Pointer<paragraph_builder>;

extension paragraph_builderExt on Pointer<paragraph_builder> {
  paragraph_builder toDart() {
    return paragraph_builder(this);
  }
  paragraph_builder get ref => toDart();
  paragraph_builder operator [](int index) => Pointer<paragraph_builder>(this.address + sizeOf * index).toDart();
  operator []=(int index, paragraph_builder value) => NativeLibrary.instance.setValue(this[index].address, value.address.toJS, '*');
  int get sizeOf => 0;
}

final class paragraph_builder extends Struct {
  Pointer<paragraph_builder> get address => super.address.cast();
  paragraph_builder(super.address);

  static Pointer<paragraph_builder> stackAlloc() {
    return Pointer<paragraph_builder>(NativeLibrary.instance.stackAlloc<paragraph_builder>(0));
  }
}

extension StructAllocator on Struct {
  static T create<T>() {
    switch (T) {
      case font_provider:
        final ptr = font_provider.stackAlloc();
        return ptr.toDart() as T;
      case paragraph_style:
        final ptr = paragraph_style.stackAlloc();
        return ptr.toDart() as T;
      case text_style:
        final ptr = text_style.stackAlloc();
        return ptr.toDart() as T;
      case paragraph:
        final ptr = paragraph.stackAlloc();
        return ptr.toDart() as T;
      case line_metrics:
        final ptr = line_metrics.stackAlloc();
        return ptr.toDart() as T;
      case glyph_metrics:
        final ptr = glyph_metrics.stackAlloc();
        return ptr.toDart() as T;
      case glyph_path:
        final ptr = glyph_path.stackAlloc();
        return ptr.toDart() as T;
      case paragraph_builder:
        final ptr = paragraph_builder.stackAlloc();
        return ptr.toDart() as T;
    }
    throw Exception("Unsupported type $T");
  }
}

final addresses = _SymbolAddresses();

class _SymbolAddresses {
  _SymbolAddresses();

  late final void Function(Pointer) motif_font_provider_destroy = (ptr) => GeneratedBindings.instance._motif_font_provider_destroy(ptr.cast());
  late final void Function(Pointer) motif_paragraph_style_destroy = (ptr) => GeneratedBindings.instance._motif_paragraph_style_destroy(ptr.cast());
  late final void Function(Pointer) motif_text_style_destroy = (ptr) => GeneratedBindings.instance._motif_text_style_destroy(ptr.cast());
  late final void Function(Pointer) motif_paragraph_destroy = (ptr) => GeneratedBindings.instance._motif_paragraph_destroy(ptr.cast());
  late final void Function(Pointer) motif_paragraph_builder_destroy = (ptr) => GeneratedBindings.instance._motif_paragraph_builder_destroy(ptr.cast());
}