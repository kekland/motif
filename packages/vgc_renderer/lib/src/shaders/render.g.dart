// GENERATED CODE - DO NOT MODIFY BY HAND
//
// Generated by wgslgen

import 'dart:typed_data';
import 'package:wgpu/wgpu.dart' as wgpu;
import 'package:wgpu/wgsl.dart' as wgsl;
import 'package:wgpu/wgsl_meta.dart' as meta;

@meta.Overrides()
extension type const Overrides._(() _) {
  const Overrides(): this._(());

  List<wgpu.ConstantEntry> get entries => const [];
}

TypedDataList<int> _readbackSync(wgpu.Device device, wgpu.Buffer buffer, int sizeInBytes) {
  final staging = device.createBuffer(.new(size: sizeInBytes, usage: .of([.copyDst, .mapRead])));

  final encoder = device.createCommandEncoder();
  encoder.copyBufferToBuffer(buffer, 0, staging, 0, sizeInBytes);
  final commands = encoder.finish();
  device.queue.submit([commands]);

  staging.mapSync(offset: 0, size: sizeInBytes, mode: .read);
  final result = staging.getMappedRange(0, sizeInBytes, readOnly: true);
  staging.unmap();
  staging.destroy();
  return result;
}

typedef F32Array6 = (double, double, double, double, double, double);
typedef F32Array6Internal = (wgsl.F32, wgsl.F32, wgsl.F32, wgsl.F32, wgsl.F32, wgsl.F32);

typedef Cubic2Array3 = ((wgsl.Vec2f p0, wgsl.Vec2f p1, wgsl.Vec2f p2, wgsl.Vec2f p3), (wgsl.Vec2f p0, wgsl.Vec2f p1, wgsl.Vec2f p2, wgsl.Vec2f p3), (wgsl.Vec2f p0, wgsl.Vec2f p1, wgsl.Vec2f p2, wgsl.Vec2f p3));
typedef Cubic2Array3Internal = ((wgsl.Vec2f p0, wgsl.Vec2f p1, wgsl.Vec2f p2, wgsl.Vec2f p3), (wgsl.Vec2f p0, wgsl.Vec2f p1, wgsl.Vec2f p2, wgsl.Vec2f p3), (wgsl.Vec2f p0, wgsl.Vec2f p1, wgsl.Vec2f p2, wgsl.Vec2f p3));

typedef F32Array3 = (double, double, double);
typedef F32Array3Internal = (wgsl.F32, wgsl.F32, wgsl.F32);

typedef RenderGeometryArray = List<(wgsl.Vec2f v0, wgsl.Vec2f v1, wgsl.Vec2f v2, wgsl.Vec2f v3, wgsl.Vec2f arcLength, wgsl.Vec4f color, double weight, int zIndex, int brushIdx, int kind)>;
typedef RenderGeometryArrayInternal = List<(wgsl.Vec2f v0, wgsl.Vec2f v1, wgsl.Vec2f v2, wgsl.Vec2f v3, wgsl.Vec2f arcLength, wgsl.Vec4f color, wgsl.F32 weight, wgsl.U32 zIndex, wgsl.U32 brushIdx, wgsl.U32 kind)>;

typedef BrushDataArray = List<(double length, double spacing, int textureIdx)>;
typedef BrushDataArrayInternal = List<(wgsl.F32 length, wgsl.F32 spacing, wgsl.U32 textureIdx)>;

typedef Cubic2 = (wgsl.Vec2f p0, wgsl.Vec2f p1, wgsl.Vec2f p2, wgsl.Vec2f p3);
typedef Cubic2Internal = (wgsl.Vec2f p0, wgsl.Vec2f p1, wgsl.Vec2f p2, wgsl.Vec2f p3);

extension Cubic2Ext on Cubic2 {
  wgsl.Vec2f get p0 => this.$1;
  wgsl.Vec2f get p1 => this.$2;
  wgsl.Vec2f get p2 => this.$3;
  wgsl.Vec2f get p3 => this.$4;

  @pragma('vm:prefer-inline')
  Cubic2Internal get asInternal => (
    p0,
    p1,
    p2,
    p3,
  );
}

extension Cubic2InternalExt on Cubic2Internal {
  wgsl.Vec2f get p0 => this.$1;
  wgsl.Vec2f get p1 => this.$2;
  wgsl.Vec2f get p2 => this.$3;
  wgsl.Vec2f get p3 => this.$4;

  @pragma('vm:prefer-inline')
  Cubic2 get asDart => (
    this.$1,
    this.$2,
    this.$3,
    this.$4,
  );

  @pragma('vm:prefer-inline')
  void write(ByteData data, int offset) {
    this.$1.write(data, offset + 0);
    this.$2.write(data, offset + 8);
    this.$3.write(data, offset + 16);
    this.$4.write(data, offset + 24);
  }
}

extension Cubic2Struct on Cubic2 {
  @pragma('vm:prefer-inline')
  static Cubic2Internal read(ByteData data, int offset) => (
    wgsl.Vec2f.read(data, offset + 0),
    wgsl.Vec2f.read(data, offset + 8),
    wgsl.Vec2f.read(data, offset + 16),
    wgsl.Vec2f.read(data, offset + 24),
  );

}

typedef Cubic2InflectionsResult = (int count, wgsl.Vec2f t);
typedef Cubic2InflectionsResultInternal = (wgsl.U32 count, wgsl.Vec2f t);

extension Cubic2InflectionsResultExt on Cubic2InflectionsResult {
  int get count => this.$1;
  wgsl.Vec2f get t => this.$2;

  @pragma('vm:prefer-inline')
  Cubic2InflectionsResultInternal get asInternal => (
    .new(count),
    t,
  );
}

extension Cubic2InflectionsResultInternalExt on Cubic2InflectionsResultInternal {
  wgsl.U32 get count => this.$1;
  wgsl.Vec2f get t => this.$2;

  @pragma('vm:prefer-inline')
  Cubic2InflectionsResult get asDart => (
    this.$1,
    this.$2,
  );

  @pragma('vm:prefer-inline')
  void write(ByteData data, int offset) {
    wgsl.U32(this.$1).write(data, offset + 0);
    this.$2.write(data, offset + 8);
  }
}

extension Cubic2InflectionsResultStruct on Cubic2InflectionsResult {
  @pragma('vm:prefer-inline')
  static Cubic2InflectionsResultInternal read(ByteData data, int offset) => (
    wgsl.U32.read(data, offset + 0),
    wgsl.Vec2f.read(data, offset + 8),
  );

}

typedef Extrema1D = (int count, wgsl.Vec2f t);
typedef Extrema1DInternal = (wgsl.U32 count, wgsl.Vec2f t);

extension Extrema1DExt on Extrema1D {
  int get count => this.$1;
  wgsl.Vec2f get t => this.$2;

  @pragma('vm:prefer-inline')
  Extrema1DInternal get asInternal => (
    .new(count),
    t,
  );
}

extension Extrema1DInternalExt on Extrema1DInternal {
  wgsl.U32 get count => this.$1;
  wgsl.Vec2f get t => this.$2;

  @pragma('vm:prefer-inline')
  Extrema1D get asDart => (
    this.$1,
    this.$2,
  );

  @pragma('vm:prefer-inline')
  void write(ByteData data, int offset) {
    wgsl.U32(this.$1).write(data, offset + 0);
    this.$2.write(data, offset + 8);
  }
}

extension Extrema1DStruct on Extrema1D {
  @pragma('vm:prefer-inline')
  static Extrema1DInternal read(ByteData data, int offset) => (
    wgsl.U32.read(data, offset + 0),
    wgsl.Vec2f.read(data, offset + 8),
  );

}

typedef Extrema2D = (int count, wgsl.Vec4f t);
typedef Extrema2DInternal = (wgsl.U32 count, wgsl.Vec4f t);

extension Extrema2DExt on Extrema2D {
  int get count => this.$1;
  wgsl.Vec4f get t => this.$2;

  @pragma('vm:prefer-inline')
  Extrema2DInternal get asInternal => (
    .new(count),
    t,
  );
}

extension Extrema2DInternalExt on Extrema2DInternal {
  wgsl.U32 get count => this.$1;
  wgsl.Vec4f get t => this.$2;

  @pragma('vm:prefer-inline')
  Extrema2D get asDart => (
    this.$1,
    this.$2,
  );

  @pragma('vm:prefer-inline')
  void write(ByteData data, int offset) {
    wgsl.U32(this.$1).write(data, offset + 0);
    this.$2.write(data, offset + 16);
  }
}

extension Extrema2DStruct on Extrema2D {
  @pragma('vm:prefer-inline')
  static Extrema2DInternal read(ByteData data, int offset) => (
    wgsl.U32.read(data, offset + 0),
    wgsl.Vec4f.read(data, offset + 16),
  );

}

typedef Cubic2KeyPointsResult = (F32Array6 t, int count);
typedef Cubic2KeyPointsResultInternal = (F32Array6Internal t, wgsl.U32 count);

extension Cubic2KeyPointsResultExt on Cubic2KeyPointsResult {
  (double, double, double, double, double, double) get t => this.$1;
  int get count => this.$2;

  @pragma('vm:prefer-inline')
  Cubic2KeyPointsResultInternal get asInternal => (
    (.new(t.$1), .new(t.$2), .new(t.$3), .new(t.$4), .new(t.$5), .new(t.$6)),
    .new(count),
  );
}

extension Cubic2KeyPointsResultInternalExt on Cubic2KeyPointsResultInternal {
  (wgsl.F32, wgsl.F32, wgsl.F32, wgsl.F32, wgsl.F32, wgsl.F32) get t => this.$1;
  wgsl.U32 get count => this.$2;

  @pragma('vm:prefer-inline')
  Cubic2KeyPointsResult get asDart => (
    (this.$1.$1, this.$1.$2, this.$1.$3, this.$1.$4, this.$1.$5, this.$1.$6),
    this.$2,
  );

  @pragma('vm:prefer-inline')
  void write(ByteData data, int offset) {
    wgsl.F32(this.$1.$1).write(data, offset + 0 + 0 * 4);
    wgsl.F32(this.$1.$2).write(data, offset + 0 + 1 * 4);
    wgsl.F32(this.$1.$3).write(data, offset + 0 + 2 * 4);
    wgsl.F32(this.$1.$4).write(data, offset + 0 + 3 * 4);
    wgsl.F32(this.$1.$5).write(data, offset + 0 + 4 * 4);
    wgsl.F32(this.$1.$6).write(data, offset + 0 + 5 * 4);
    wgsl.U32(this.$2).write(data, offset + 24);
  }
}

extension Cubic2KeyPointsResultStruct on Cubic2KeyPointsResult {
  @pragma('vm:prefer-inline')
  static Cubic2KeyPointsResultInternal read(ByteData data, int offset) => (
    (
      wgsl.F32.read(data, offset + 0 + 0 * 4),
      wgsl.F32.read(data, offset + 0 + 1 * 4),
      wgsl.F32.read(data, offset + 0 + 2 * 4),
      wgsl.F32.read(data, offset + 0 + 3 * 4),
      wgsl.F32.read(data, offset + 0 + 4 * 4),
      wgsl.F32.read(data, offset + 0 + 5 * 4),
    ),
    wgsl.U32.read(data, offset + 24),
  );

}

typedef Cubic2ChopAtInflectionsResult = (Cubic2Array3 cubics, F32Array3 t0, F32Array3 t1, int count);
typedef Cubic2ChopAtInflectionsResultInternal = (Cubic2Array3Internal cubics, F32Array3Internal t0, F32Array3Internal t1, wgsl.U32 count);

extension Cubic2ChopAtInflectionsResultExt on Cubic2ChopAtInflectionsResult {
  ((wgsl.Vec2f p0, wgsl.Vec2f p1, wgsl.Vec2f p2, wgsl.Vec2f p3), (wgsl.Vec2f p0, wgsl.Vec2f p1, wgsl.Vec2f p2, wgsl.Vec2f p3), (wgsl.Vec2f p0, wgsl.Vec2f p1, wgsl.Vec2f p2, wgsl.Vec2f p3)) get cubics => this.$1;
  (double, double, double) get t0 => this.$2;
  (double, double, double) get t1 => this.$3;
  int get count => this.$4;

  @pragma('vm:prefer-inline')
  Cubic2ChopAtInflectionsResultInternal get asInternal => (
    cubics,
    (.new(t0.$1), .new(t0.$2), .new(t0.$3)),
    (.new(t1.$1), .new(t1.$2), .new(t1.$3)),
    .new(count),
  );
}

extension Cubic2ChopAtInflectionsResultInternalExt on Cubic2ChopAtInflectionsResultInternal {
  ((wgsl.Vec2f p0, wgsl.Vec2f p1, wgsl.Vec2f p2, wgsl.Vec2f p3), (wgsl.Vec2f p0, wgsl.Vec2f p1, wgsl.Vec2f p2, wgsl.Vec2f p3), (wgsl.Vec2f p0, wgsl.Vec2f p1, wgsl.Vec2f p2, wgsl.Vec2f p3)) get cubics => this.$1;
  (wgsl.F32, wgsl.F32, wgsl.F32) get t0 => this.$2;
  (wgsl.F32, wgsl.F32, wgsl.F32) get t1 => this.$3;
  wgsl.U32 get count => this.$4;

  @pragma('vm:prefer-inline')
  Cubic2ChopAtInflectionsResult get asDart => (
    this.$1,
    (this.$2.$1, this.$2.$2, this.$2.$3),
    (this.$3.$1, this.$3.$2, this.$3.$3),
    this.$4,
  );

  @pragma('vm:prefer-inline')
  void write(ByteData data, int offset) {
    this.$1.$1.asInternal.write(data, offset + 0 + 0 * 32);
    this.$1.$2.asInternal.write(data, offset + 0 + 1 * 32);
    this.$1.$3.asInternal.write(data, offset + 0 + 2 * 32);
    wgsl.F32(this.$2.$1).write(data, offset + 96 + 0 * 4);
    wgsl.F32(this.$2.$2).write(data, offset + 96 + 1 * 4);
    wgsl.F32(this.$2.$3).write(data, offset + 96 + 2 * 4);
    wgsl.F32(this.$3.$1).write(data, offset + 108 + 0 * 4);
    wgsl.F32(this.$3.$2).write(data, offset + 108 + 1 * 4);
    wgsl.F32(this.$3.$3).write(data, offset + 108 + 2 * 4);
    wgsl.U32(this.$4).write(data, offset + 120);
  }
}

extension Cubic2ChopAtInflectionsResultStruct on Cubic2ChopAtInflectionsResult {
  @pragma('vm:prefer-inline')
  static Cubic2ChopAtInflectionsResultInternal read(ByteData data, int offset) => (
    (
      Cubic2Struct.read(data, offset + 0 + 0 * 32),
      Cubic2Struct.read(data, offset + 0 + 1 * 32),
      Cubic2Struct.read(data, offset + 0 + 2 * 32),
    ),
    (
      wgsl.F32.read(data, offset + 96 + 0 * 4),
      wgsl.F32.read(data, offset + 96 + 1 * 4),
      wgsl.F32.read(data, offset + 96 + 2 * 4),
    ),
    (
      wgsl.F32.read(data, offset + 108 + 0 * 4),
      wgsl.F32.read(data, offset + 108 + 1 * 4),
      wgsl.F32.read(data, offset + 108 + 2 * 4),
    ),
    wgsl.U32.read(data, offset + 120),
  );

}

typedef Uniforms = (int cubicCount, int weightAnchorCount, double tolerance, wgsl.Vec2f screenSize, wgsl.Mat4x4f transform);
typedef UniformsInternal = (wgsl.U32 cubicCount, wgsl.U32 weightAnchorCount, wgsl.F32 tolerance, wgsl.Vec2f screenSize, wgsl.Mat4x4f transform);

extension UniformsExt on Uniforms {
  int get cubicCount => this.$1;
  int get weightAnchorCount => this.$2;
  double get tolerance => this.$3;
  wgsl.Vec2f get screenSize => this.$4;
  wgsl.Mat4x4f get transform => this.$5;

  @pragma('vm:prefer-inline')
  UniformsInternal get asInternal => (
    .new(cubicCount),
    .new(weightAnchorCount),
    .new(tolerance),
    screenSize,
    transform,
  );
}

extension UniformsInternalExt on UniformsInternal {
  wgsl.U32 get cubicCount => this.$1;
  wgsl.U32 get weightAnchorCount => this.$2;
  wgsl.F32 get tolerance => this.$3;
  wgsl.Vec2f get screenSize => this.$4;
  wgsl.Mat4x4f get transform => this.$5;

  @pragma('vm:prefer-inline')
  Uniforms get asDart => (
    this.$1,
    this.$2,
    this.$3,
    this.$4,
    this.$5,
  );

  @pragma('vm:prefer-inline')
  void write(ByteData data, int offset) {
    wgsl.U32(this.$1).write(data, offset + 0);
    wgsl.U32(this.$2).write(data, offset + 4);
    wgsl.F32(this.$3).write(data, offset + 8);
    this.$4.write(data, offset + 16);
    this.$5.write(data, offset + 32);
  }
}

extension UniformsStruct on Uniforms {
  @pragma('vm:prefer-inline')
  static UniformsInternal read(ByteData data, int offset) => (
    wgsl.U32.read(data, offset + 0),
    wgsl.U32.read(data, offset + 4),
    wgsl.F32.read(data, offset + 8),
    wgsl.Vec2f.read(data, offset + 16),
    wgsl.Mat4x4f.read(data, offset + 32),
  );

}

typedef VertexData = (wgsl.Vec2f pos, wgsl.Vec2u incidentEdgesSpan);
typedef VertexDataInternal = (wgsl.Vec2f pos, wgsl.Vec2u incidentEdgesSpan);

extension VertexDataExt on VertexData {
  wgsl.Vec2f get pos => this.$1;
  wgsl.Vec2u get incidentEdgesSpan => this.$2;

  @pragma('vm:prefer-inline')
  VertexDataInternal get asInternal => (
    pos,
    incidentEdgesSpan,
  );
}

extension VertexDataInternalExt on VertexDataInternal {
  wgsl.Vec2f get pos => this.$1;
  wgsl.Vec2u get incidentEdgesSpan => this.$2;

  @pragma('vm:prefer-inline')
  VertexData get asDart => (
    this.$1,
    this.$2,
  );

  @pragma('vm:prefer-inline')
  void write(ByteData data, int offset) {
    this.$1.write(data, offset + 0);
    this.$2.write(data, offset + 8);
  }
}

extension VertexDataStruct on VertexData {
  @pragma('vm:prefer-inline')
  static VertexDataInternal read(ByteData data, int offset) => (
    wgsl.Vec2f.read(data, offset + 0),
    wgsl.Vec2u.read(data, offset + 8),
  );

}

typedef EdgeData = (int startVertexIdx, int endVertexIdx, wgsl.Vec2u cubicsSpan, wgsl.Vec2u weightSpan, double width, double arcLength, wgsl.Vec4f color, int brushIdx);
typedef EdgeDataInternal = (wgsl.U32 startVertexIdx, wgsl.U32 endVertexIdx, wgsl.Vec2u cubicsSpan, wgsl.Vec2u weightSpan, wgsl.F32 width, wgsl.F32 arcLength, wgsl.Vec4f color, wgsl.U32 brushIdx);

extension EdgeDataExt on EdgeData {
  int get startVertexIdx => this.$1;
  int get endVertexIdx => this.$2;
  wgsl.Vec2u get cubicsSpan => this.$3;
  wgsl.Vec2u get weightSpan => this.$4;
  double get width => this.$5;
  double get arcLength => this.$6;
  wgsl.Vec4f get color => this.$7;
  int get brushIdx => this.$8;

  @pragma('vm:prefer-inline')
  EdgeDataInternal get asInternal => (
    .new(startVertexIdx),
    .new(endVertexIdx),
    cubicsSpan,
    weightSpan,
    .new(width),
    .new(arcLength),
    color,
    .new(brushIdx),
  );
}

extension EdgeDataInternalExt on EdgeDataInternal {
  wgsl.U32 get startVertexIdx => this.$1;
  wgsl.U32 get endVertexIdx => this.$2;
  wgsl.Vec2u get cubicsSpan => this.$3;
  wgsl.Vec2u get weightSpan => this.$4;
  wgsl.F32 get width => this.$5;
  wgsl.F32 get arcLength => this.$6;
  wgsl.Vec4f get color => this.$7;
  wgsl.U32 get brushIdx => this.$8;

  @pragma('vm:prefer-inline')
  EdgeData get asDart => (
    this.$1,
    this.$2,
    this.$3,
    this.$4,
    this.$5,
    this.$6,
    this.$7,
    this.$8,
  );

  @pragma('vm:prefer-inline')
  void write(ByteData data, int offset) {
    wgsl.U32(this.$1).write(data, offset + 0);
    wgsl.U32(this.$2).write(data, offset + 4);
    this.$3.write(data, offset + 8);
    this.$4.write(data, offset + 16);
    wgsl.F32(this.$5).write(data, offset + 24);
    wgsl.F32(this.$6).write(data, offset + 28);
    this.$7.write(data, offset + 32);
    wgsl.U32(this.$8).write(data, offset + 48);
  }
}

extension EdgeDataStruct on EdgeData {
  @pragma('vm:prefer-inline')
  static EdgeDataInternal read(ByteData data, int offset) => (
    wgsl.U32.read(data, offset + 0),
    wgsl.U32.read(data, offset + 4),
    wgsl.Vec2u.read(data, offset + 8),
    wgsl.Vec2u.read(data, offset + 16),
    wgsl.F32.read(data, offset + 24),
    wgsl.F32.read(data, offset + 28),
    wgsl.Vec4f.read(data, offset + 32),
    wgsl.U32.read(data, offset + 48),
  );

}

typedef CubicData = (Cubic2 cubic, int edgeIdx, int edgeSegmentIdx, int edgeSegmentCount, double edgeStartArcLength, double opacity);
typedef CubicDataInternal = (Cubic2Internal cubic, wgsl.U32 edgeIdx, wgsl.U32 edgeSegmentIdx, wgsl.U32 edgeSegmentCount, wgsl.F32 edgeStartArcLength, wgsl.F32 opacity);

extension CubicDataExt on CubicData {
  (wgsl.Vec2f p0, wgsl.Vec2f p1, wgsl.Vec2f p2, wgsl.Vec2f p3) get cubic => this.$1;
  int get edgeIdx => this.$2;
  int get edgeSegmentIdx => this.$3;
  int get edgeSegmentCount => this.$4;
  double get edgeStartArcLength => this.$5;
  double get opacity => this.$6;

  @pragma('vm:prefer-inline')
  CubicDataInternal get asInternal => (
    cubic,
    .new(edgeIdx),
    .new(edgeSegmentIdx),
    .new(edgeSegmentCount),
    .new(edgeStartArcLength),
    .new(opacity),
  );
}

extension CubicDataInternalExt on CubicDataInternal {
  (wgsl.Vec2f p0, wgsl.Vec2f p1, wgsl.Vec2f p2, wgsl.Vec2f p3) get cubic => this.$1;
  wgsl.U32 get edgeIdx => this.$2;
  wgsl.U32 get edgeSegmentIdx => this.$3;
  wgsl.U32 get edgeSegmentCount => this.$4;
  wgsl.F32 get edgeStartArcLength => this.$5;
  wgsl.F32 get opacity => this.$6;

  @pragma('vm:prefer-inline')
  CubicData get asDart => (
    this.$1,
    this.$2,
    this.$3,
    this.$4,
    this.$5,
    this.$6,
  );

  @pragma('vm:prefer-inline')
  void write(ByteData data, int offset) {
    this.$1.asInternal.write(data, offset + 0);
    wgsl.U32(this.$2).write(data, offset + 32);
    wgsl.U32(this.$3).write(data, offset + 36);
    wgsl.U32(this.$4).write(data, offset + 40);
    wgsl.F32(this.$5).write(data, offset + 44);
    wgsl.F32(this.$6).write(data, offset + 48);
  }
}

extension CubicDataStruct on CubicData {
  @pragma('vm:prefer-inline')
  static CubicDataInternal read(ByteData data, int offset) => (
    Cubic2Struct.read(data, offset + 0),
    wgsl.U32.read(data, offset + 32),
    wgsl.U32.read(data, offset + 36),
    wgsl.U32.read(data, offset + 40),
    wgsl.F32.read(data, offset + 44),
    wgsl.F32.read(data, offset + 48),
  );

}

typedef BrushData = (double length, double spacing, int textureIdx);
typedef BrushDataInternal = (wgsl.F32 length, wgsl.F32 spacing, wgsl.U32 textureIdx);

extension BrushDataExt on BrushData {
  double get length => this.$1;
  double get spacing => this.$2;
  int get textureIdx => this.$3;

  @pragma('vm:prefer-inline')
  BrushDataInternal get asInternal => (
    .new(length),
    .new(spacing),
    .new(textureIdx),
  );
}

extension BrushDataInternalExt on BrushDataInternal {
  wgsl.F32 get length => this.$1;
  wgsl.F32 get spacing => this.$2;
  wgsl.U32 get textureIdx => this.$3;

  @pragma('vm:prefer-inline')
  BrushData get asDart => (
    this.$1,
    this.$2,
    this.$3,
  );

  @pragma('vm:prefer-inline')
  void write(ByteData data, int offset) {
    wgsl.F32(this.$1).write(data, offset + 0);
    wgsl.F32(this.$2).write(data, offset + 4);
    wgsl.U32(this.$3).write(data, offset + 8);
  }
}

extension BrushDataStruct on BrushData {
  @pragma('vm:prefer-inline')
  static BrushDataInternal read(ByteData data, int offset) => (
    wgsl.F32.read(data, offset + 0),
    wgsl.F32.read(data, offset + 4),
    wgsl.U32.read(data, offset + 8),
  );

}

typedef RenderGeometry = (wgsl.Vec2f v0, wgsl.Vec2f v1, wgsl.Vec2f v2, wgsl.Vec2f v3, wgsl.Vec2f arcLength, wgsl.Vec4f color, double weight, int zIndex, int brushIdx, int kind);
typedef RenderGeometryInternal = (wgsl.Vec2f v0, wgsl.Vec2f v1, wgsl.Vec2f v2, wgsl.Vec2f v3, wgsl.Vec2f arcLength, wgsl.Vec4f color, wgsl.F32 weight, wgsl.U32 zIndex, wgsl.U32 brushIdx, wgsl.U32 kind);

extension RenderGeometryExt on RenderGeometry {
  wgsl.Vec2f get v0 => this.$1;
  wgsl.Vec2f get v1 => this.$2;
  wgsl.Vec2f get v2 => this.$3;
  wgsl.Vec2f get v3 => this.$4;
  wgsl.Vec2f get arcLength => this.$5;
  wgsl.Vec4f get color => this.$6;
  double get weight => this.$7;
  int get zIndex => this.$8;
  int get brushIdx => this.$9;
  int get kind => this.$10;

  @pragma('vm:prefer-inline')
  RenderGeometryInternal get asInternal => (
    v0,
    v1,
    v2,
    v3,
    arcLength,
    color,
    .new(weight),
    .new(zIndex),
    .new(brushIdx),
    .new(kind),
  );
}

extension RenderGeometryInternalExt on RenderGeometryInternal {
  wgsl.Vec2f get v0 => this.$1;
  wgsl.Vec2f get v1 => this.$2;
  wgsl.Vec2f get v2 => this.$3;
  wgsl.Vec2f get v3 => this.$4;
  wgsl.Vec2f get arcLength => this.$5;
  wgsl.Vec4f get color => this.$6;
  wgsl.F32 get weight => this.$7;
  wgsl.U32 get zIndex => this.$8;
  wgsl.U32 get brushIdx => this.$9;
  wgsl.U32 get kind => this.$10;

  @pragma('vm:prefer-inline')
  RenderGeometry get asDart => (
    this.$1,
    this.$2,
    this.$3,
    this.$4,
    this.$5,
    this.$6,
    this.$7,
    this.$8,
    this.$9,
    this.$10,
  );

  @pragma('vm:prefer-inline')
  void write(ByteData data, int offset) {
    this.$1.write(data, offset + 0);
    this.$2.write(data, offset + 8);
    this.$3.write(data, offset + 16);
    this.$4.write(data, offset + 24);
    this.$5.write(data, offset + 32);
    this.$6.write(data, offset + 48);
    wgsl.F32(this.$7).write(data, offset + 64);
    wgsl.U32(this.$8).write(data, offset + 68);
    wgsl.U32(this.$9).write(data, offset + 72);
    wgsl.U32(this.$10).write(data, offset + 76);
  }
}

extension RenderGeometryStruct on RenderGeometry {
  @pragma('vm:prefer-inline')
  static RenderGeometryInternal read(ByteData data, int offset) => (
    wgsl.Vec2f.read(data, offset + 0),
    wgsl.Vec2f.read(data, offset + 8),
    wgsl.Vec2f.read(data, offset + 16),
    wgsl.Vec2f.read(data, offset + 24),
    wgsl.Vec2f.read(data, offset + 32),
    wgsl.Vec4f.read(data, offset + 48),
    wgsl.F32.read(data, offset + 64),
    wgsl.U32.read(data, offset + 68),
    wgsl.U32.read(data, offset + 72),
    wgsl.U32.read(data, offset + 76),
  );

}

@meta.UniformBufferView(['u'])
extension type UniformsUniformBufferView(ByteData data) {
  UniformsUniformBufferView.create(): this(.new(sizeInBytes));

  void writeToQueue(wgpu.Queue queue, wgpu.Buffer buffer, {int offset = 0}) => queue.writeBuffer(buffer, offset, data);
  void clear() => data.buffer.asUint8List(data.offsetInBytes, data.lengthInBytes).fillRange(0, data.lengthInBytes, 0);

  static const int sizeInBytes = 96;

  static wgpu.BufferDescriptor descriptor({
    String? label,
    wgpu.BufferUsage? usage,
    bool mappedAtCreation = false,
  }) => .new(
    label: label ?? '(render.wgsl) Uniforms buffer',
    size: sizeInBytes,
    usage: usage ?? .of([.uniform, .copyDst]),
    mappedAtCreation: mappedAtCreation,
  );

  static wgpu.Buffer createBuffer(
    wgpu.Device device, {
    String? label,
    wgpu.BufferUsage? usage,
    bool mappedAtCreation = false,
  }) {
    return device.createBuffer(descriptor(
      label: label,
      usage: usage,
      mappedAtCreation: mappedAtCreation,
    ));
  }

  @pragma('vm:prefer-inline')
  void write(Uniforms value) {
    value.asInternal.write(data, 0);
  }

  @pragma('vm:prefer-inline')
  Uniforms read() {
    return UniformsStruct.read(data, 0);
  }

  @pragma('vm:prefer-inline')
  int get cubicCount {
    return wgsl.U32.read(data, 0);
  }

  @pragma('vm:prefer-inline')
  set cubicCount(int value) {
    wgsl.U32(value).write(data, 0);
  }

  @pragma('vm:prefer-inline')
  int get weightAnchorCount {
    return wgsl.U32.read(data, 4);
  }

  @pragma('vm:prefer-inline')
  set weightAnchorCount(int value) {
    wgsl.U32(value).write(data, 4);
  }

  @pragma('vm:prefer-inline')
  double get tolerance {
    return wgsl.F32.read(data, 8);
  }

  @pragma('vm:prefer-inline')
  set tolerance(double value) {
    wgsl.F32(value).write(data, 8);
  }

  @pragma('vm:prefer-inline')
  wgsl.Vec2f get screenSize {
    return wgsl.Vec2f.read(data, 16);
  }

  @pragma('vm:prefer-inline')
  set screenSize(wgsl.Vec2f value) {
    value.write(data, 16);
  }

  @pragma('vm:prefer-inline')
  wgsl.Mat4x4f get transform {
    return wgsl.Mat4x4f.read(data, 32);
  }

  @pragma('vm:prefer-inline')
  set transform(wgsl.Mat4x4f value) {
    value.write(data, 32);
  }

  @pragma('vm:prefer-inline')
  void set({int cubicCount = 0, int weightAnchorCount = 0, double tolerance = 0, wgsl.Vec2f screenSize = .zero, wgsl.Mat4x4f transform = .identity}) {
    this.cubicCount = cubicCount;
    this.weightAnchorCount = weightAnchorCount;
    this.tolerance = tolerance;
    this.screenSize = screenSize;
    this.transform = transform;
  }

}

@meta.UniformBuffer(['u'])
class UniformsUniformBuffer extends wgsl.UniformBuffer<UniformsInternal> {
  UniformsUniformBuffer(wgpu.Device device, {String? label, wgpu.BufferUsage? usage}):
    _view = UniformsUniformBufferView.create(),
    super(UniformsUniformBufferView.createBuffer(device, label: label, usage: usage));

  static const int sizeInBytes = 96;

  final UniformsUniformBufferView _view;
  UniformsUniformBufferView get view => _view;

  @override
  void writeToQueue(wgpu.Queue queue) => _view.writeToQueue(queue, buffer);

  Uniforms readSync(wgpu.Device device) {
    final mapped = _readbackSync(device, buffer, sizeInBytes);
    _view.data.buffer.asUint8List(_view.data.offsetInBytes, _view.data.lengthInBytes).setAll(0, mapped);
    return _view.read();
  }

  @pragma('vm:prefer-inline')
  void write(Uniforms value) => _view.write(value);

  @pragma('vm:prefer-inline')
  void clear([wgpu.Queue? queue]) {
    _view.clear();
    if (queue != null) writeToQueue(queue);
  }

  @pragma('vm:prefer-inline')
  set cubicCount(int value) => _view.cubicCount = value;

  @pragma('vm:prefer-inline')
  wgpu.BufferView get cubicCountBufferView => wgpu.BufferView(buffer, offset: 0, size: 4);

  @pragma('vm:prefer-inline')
  set weightAnchorCount(int value) => _view.weightAnchorCount = value;

  @pragma('vm:prefer-inline')
  wgpu.BufferView get weightAnchorCountBufferView => wgpu.BufferView(buffer, offset: 4, size: 4);

  @pragma('vm:prefer-inline')
  set tolerance(double value) => _view.tolerance = value;

  @pragma('vm:prefer-inline')
  wgpu.BufferView get toleranceBufferView => wgpu.BufferView(buffer, offset: 8, size: 4);

  @pragma('vm:prefer-inline')
  set screenSize(wgsl.Vec2f value) => _view.screenSize = value;

  @pragma('vm:prefer-inline')
  wgpu.BufferView get screenSizeBufferView => wgpu.BufferView(buffer, offset: 16, size: 8);

  @pragma('vm:prefer-inline')
  set transform(wgsl.Mat4x4f value) => _view.transform = value;

  @pragma('vm:prefer-inline')
  wgpu.BufferView get transformBufferView => wgpu.BufferView(buffer, offset: 32, size: 64);

  @pragma('vm:prefer-inline')
  void set({int cubicCount = 0, int weightAnchorCount = 0, double tolerance = 0, wgsl.Vec2f screenSize = .zero, wgsl.Mat4x4f transform = .identity}) => _view.set(cubicCount: cubicCount, weightAnchorCount: weightAnchorCount, tolerance: tolerance, screenSize: screenSize, transform: transform);

}

@meta.StorageBufferView(['in_render_geometry'])
extension type RenderGeometryArrayStorageBufferView(ByteData data) {
  RenderGeometryArrayStorageBufferView.create(int length): this(.new(length * strideInBytes));

  void writeToQueue(wgpu.Queue queue, wgpu.Buffer buffer, {int? count}) {
    final length = count != null ? count * strideInBytes : data.lengthInBytes;
    queue.writeBuffer(buffer, 0, ByteData.sublistView(data, 0, length));
  }
  void clear() => data.buffer.asUint8List(data.offsetInBytes, data.lengthInBytes).fillRange(0, data.lengthInBytes, 0);

  static const int strideInBytes = 80;
  int get sizeInBytes => data.lengthInBytes;
  int get length => sizeInBytes ~/ strideInBytes;

  static wgpu.BufferDescriptor descriptor({
    required int length,
    String? label,
    wgpu.BufferUsage? usage,
    bool mappedAtCreation = false,
  }) => .new(
    label: label ?? '(render.wgsl) array<RenderGeometry> buffer',
    size: length * strideInBytes,
    usage: usage ?? .of([.storage, .copyDst]),
    mappedAtCreation: mappedAtCreation,
  );

  static wgpu.Buffer createBuffer(
    wgpu.Device device, {
    required int length,
    String? label,
    wgpu.BufferUsage? usage,
    bool mappedAtCreation = false,
  }) {
    return device.createBuffer(descriptor(
      length: length,
      label: label,
      usage: usage,
      mappedAtCreation: mappedAtCreation,
    ));
  }

  @pragma('vm:prefer-inline')
  void write(RenderGeometryArray value) {
    for (var i = 0; i < value.length; i++) {
      value[i].asInternal.write(data, 0 + i * 80);
    }
  }

  @pragma('vm:prefer-inline')
  RenderGeometryArray read() {
    return List.generate(length, (i) {
      return RenderGeometryStruct.read(data, 0 + i * 80);
    });
  }

  @pragma('vm:prefer-inline')
  void operator []=(int index, RenderGeometry value) {
    value.asInternal.write(data, index * strideInBytes);
  }

  @pragma('vm:prefer-inline')
  RenderGeometry operator [](int index) {
    return RenderGeometryStruct.read(data, index * strideInBytes);
  }

  @pragma('vm:prefer-inline')
  void set(int index, {wgsl.Vec2f v0 = .zero, wgsl.Vec2f v1 = .zero, wgsl.Vec2f v2 = .zero, wgsl.Vec2f v3 = .zero, wgsl.Vec2f arcLength = .zero, wgsl.Vec4f color = .zero, double weight = 0, int zIndex = 0, int brushIdx = 0, int kind = 0}) {
    v0.write(data, index * strideInBytes + 0);
    v1.write(data, index * strideInBytes + 8);
    v2.write(data, index * strideInBytes + 16);
    v3.write(data, index * strideInBytes + 24);
    arcLength.write(data, index * strideInBytes + 32);
    color.write(data, index * strideInBytes + 48);
    wgsl.F32(weight).write(data, index * strideInBytes + 64);
    wgsl.U32(zIndex).write(data, index * strideInBytes + 68);
    wgsl.U32(brushIdx).write(data, index * strideInBytes + 72);
    wgsl.U32(kind).write(data, index * strideInBytes + 76);
  }
}

@meta.StorageBuffer(['in_render_geometry'])
class RenderGeometryArrayStorageBuffer extends wgsl.ArrayStorageBuffer<RenderGeometryInternal> {
  RenderGeometryArrayStorageBuffer(wgpu.Device device, {required int length, String? label, wgpu.BufferUsage? usage}):
    _view = RenderGeometryArrayStorageBufferView.create(length),
    super(length, RenderGeometryArrayStorageBufferView.createBuffer(device, length: length, label: label, usage: usage));


  static const int strideInBytes = 80;
  int get sizeInBytes => _view.sizeInBytes;

  final RenderGeometryArrayStorageBufferView _view;
  RenderGeometryArrayStorageBufferView get view => _view;

  @override
  void writeToQueue(wgpu.Queue queue, {int? count}) => _view.writeToQueue(queue, buffer, count: count);

  RenderGeometryArray readSync(wgpu.Device device) {
    final mapped = _readbackSync(device, buffer, sizeInBytes);
    _view.data.buffer.asUint8List(_view.data.offsetInBytes, _view.data.lengthInBytes).setAll(0, mapped);
    return _view.read();
  }

  @pragma('vm:prefer-inline')
  void write(RenderGeometryArray value) => _view.write(value);

  @pragma('vm:prefer-inline')
  void clear([wgpu.Queue? queue]) {
    _view.clear();
    if (queue != null) writeToQueue(queue);
  }

  @pragma('vm:prefer-inline')
  void operator []=(int index, RenderGeometry value) => _view[index] = value;

  @pragma('vm:prefer-inline')
  void set(int index, {wgsl.Vec2f v0 = .zero, wgsl.Vec2f v1 = .zero, wgsl.Vec2f v2 = .zero, wgsl.Vec2f v3 = .zero, wgsl.Vec2f arcLength = .zero, wgsl.Vec4f color = .zero, double weight = 0, int zIndex = 0, int brushIdx = 0, int kind = 0}) => _view.set(index, v0: v0, v1: v1, v2: v2, v3: v3, arcLength: arcLength, color: color, weight: weight, zIndex: zIndex, brushIdx: brushIdx, kind: kind);
}

@meta.StorageBufferView(['in_brush_data'])
extension type BrushDataArrayStorageBufferView(ByteData data) {
  BrushDataArrayStorageBufferView.create(int length): this(.new(length * strideInBytes));

  void writeToQueue(wgpu.Queue queue, wgpu.Buffer buffer, {int? count}) {
    final length = count != null ? count * strideInBytes : data.lengthInBytes;
    queue.writeBuffer(buffer, 0, ByteData.sublistView(data, 0, length));
  }
  void clear() => data.buffer.asUint8List(data.offsetInBytes, data.lengthInBytes).fillRange(0, data.lengthInBytes, 0);

  static const int strideInBytes = 12;
  int get sizeInBytes => data.lengthInBytes;
  int get length => sizeInBytes ~/ strideInBytes;

  static wgpu.BufferDescriptor descriptor({
    required int length,
    String? label,
    wgpu.BufferUsage? usage,
    bool mappedAtCreation = false,
  }) => .new(
    label: label ?? '(render.wgsl) array<BrushData> buffer',
    size: length * strideInBytes,
    usage: usage ?? .of([.storage, .copyDst]),
    mappedAtCreation: mappedAtCreation,
  );

  static wgpu.Buffer createBuffer(
    wgpu.Device device, {
    required int length,
    String? label,
    wgpu.BufferUsage? usage,
    bool mappedAtCreation = false,
  }) {
    return device.createBuffer(descriptor(
      length: length,
      label: label,
      usage: usage,
      mappedAtCreation: mappedAtCreation,
    ));
  }

  @pragma('vm:prefer-inline')
  void write(BrushDataArray value) {
    for (var i = 0; i < value.length; i++) {
      value[i].asInternal.write(data, 0 + i * 12);
    }
  }

  @pragma('vm:prefer-inline')
  BrushDataArray read() {
    return List.generate(length, (i) {
      return BrushDataStruct.read(data, 0 + i * 12);
    });
  }

  @pragma('vm:prefer-inline')
  void operator []=(int index, BrushData value) {
    value.asInternal.write(data, index * strideInBytes);
  }

  @pragma('vm:prefer-inline')
  BrushData operator [](int index) {
    return BrushDataStruct.read(data, index * strideInBytes);
  }

  @pragma('vm:prefer-inline')
  void set(int index, {double length = 0, double spacing = 0, int textureIdx = 0}) {
    wgsl.F32(length).write(data, index * strideInBytes + 0);
    wgsl.F32(spacing).write(data, index * strideInBytes + 4);
    wgsl.U32(textureIdx).write(data, index * strideInBytes + 8);
  }
}

@meta.StorageBuffer(['in_brush_data'])
class BrushDataArrayStorageBuffer extends wgsl.ArrayStorageBuffer<BrushDataInternal> {
  BrushDataArrayStorageBuffer(wgpu.Device device, {required int length, String? label, wgpu.BufferUsage? usage}):
    _view = BrushDataArrayStorageBufferView.create(length),
    super(length, BrushDataArrayStorageBufferView.createBuffer(device, length: length, label: label, usage: usage));


  static const int strideInBytes = 12;
  int get sizeInBytes => _view.sizeInBytes;

  final BrushDataArrayStorageBufferView _view;
  BrushDataArrayStorageBufferView get view => _view;

  @override
  void writeToQueue(wgpu.Queue queue, {int? count}) => _view.writeToQueue(queue, buffer, count: count);

  BrushDataArray readSync(wgpu.Device device) {
    final mapped = _readbackSync(device, buffer, sizeInBytes);
    _view.data.buffer.asUint8List(_view.data.offsetInBytes, _view.data.lengthInBytes).setAll(0, mapped);
    return _view.read();
  }

  @pragma('vm:prefer-inline')
  void write(BrushDataArray value) => _view.write(value);

  @pragma('vm:prefer-inline')
  void clear([wgpu.Queue? queue]) {
    _view.clear();
    if (queue != null) writeToQueue(queue);
  }

  @pragma('vm:prefer-inline')
  void operator []=(int index, BrushData value) => _view[index] = value;

  @pragma('vm:prefer-inline')
  void set(int index, {double length = 0, double spacing = 0, int textureIdx = 0}) => _view.set(index, length: length, spacing: spacing, textureIdx: textureIdx);
}

@meta.BindGroupLayoutDescriptor(0)
wgpu.BindGroupLayoutDescriptor get bindGroup0LayoutDescriptor => .new(
  label: '(render.wgsl) bind group 0 layout',
  entries: [
    .new(
      binding: 0,
      visibility: .vertex,
      buffer: .new(
        type: .uniform,
        minBindingSize: 96,
        hasDynamicOffset: false,
      ),
    ),
    .new(
      binding: 1,
      visibility: .of([.vertex, .fragment]),
      buffer: .new(
        type: .readOnlyStorage,
        minBindingSize: 80,
        hasDynamicOffset: false,
      ),
    ),
    .new(
      binding: 2,
      visibility: .none,
      buffer: .new(
        type: .readOnlyStorage,
        minBindingSize: 12,
        hasDynamicOffset: false,
      ),
    ),
    .new(
      binding: 3,
      visibility: .none,
      texture: .new(
        sampleType: .float,
        multisampled: false,
        viewDimension: .twoDArray,
      ),
    ),
    .new(
      binding: 4,
      visibility: .none,
      sampler: .new(type: .filtering),
    ),
  ],
);

@meta.BindGroupLayout(0)
wgpu.BindGroupLayout createBindGroup0Layout(wgpu.Device device) {
  return device.createBindGroupLayout(bindGroup0LayoutDescriptor);
}

@meta.BindGroup(0)
wgpu.BindGroup createBindGroup0Raw(
  wgpu.Device device, {
  wgpu.BindGroupLayout? layout,
  required wgpu.BufferView u,
  required wgpu.BufferView inRenderGeometry,
  required wgpu.BufferView inBrushData,
  required wgpu.TextureView brushTextures,
  required wgpu.Sampler brushSampler,
  String? label,
}) => device.createBindGroup(.new(
  label: label ?? '(render.wgsl) bind group 0',
  layout: layout ?? createBindGroup0Layout(device),
  entries: [
    .new(binding: 0, buffer: u.buffer, offset: u.offset, size: u.size),
    .new(binding: 1, buffer: inRenderGeometry.buffer, offset: inRenderGeometry.offset, size: inRenderGeometry.size),
    .new(binding: 2, buffer: inBrushData.buffer, offset: inBrushData.offset, size: inBrushData.size),
    .new(binding: 3, textureView: brushTextures),
    .new(binding: 4, sampler: brushSampler),
  ],
));

@meta.BindGroup(0)
wgpu.BindGroup createBindGroup0(
  wgpu.Device device, {
  wgpu.BindGroupLayout? layout,
  required wgsl.UniformBuffer<UniformsInternal> u,
  required wgsl.ArrayStorageBuffer<RenderGeometryInternal> inRenderGeometry,
  required wgsl.ArrayStorageBuffer<BrushDataInternal> inBrushData,
  required wgpu.TextureView brushTextures,
  required wgpu.Sampler brushSampler,
  String? label,
}) => createBindGroup0Raw(
  device,
  layout: layout,
  label: label,
  u: u.bufferView,
  inRenderGeometry: inRenderGeometry.bufferView,
  inBrushData: inBrushData.bufferView,
  brushTextures: brushTextures,
  brushSampler: brushSampler,
);

@meta.VertexState('vs_main')
wgpu.VertexState createVsMainVertexState(
  wgpu.ShaderModule module, {
  Overrides? overrides,
  List<wgpu.VertexBufferLayout>? vertexBuffers,
}) => wgpu.VertexState(
  module: module,
  entryPoint: 'vs_main',
  constants: overrides?.entries ?? const [],
  buffers: vertexBuffers ?? const [],
);

@meta.FragmentState('fs_main')
wgpu.FragmentState createFsMainFragmentState(
  wgpu.ShaderModule module,
  List<wgpu.ColorTargetState> targets, {
  Overrides? overrides,
}) => wgpu.FragmentState(
  module: module,
  entryPoint: 'fs_main',
  constants: overrides?.entries ?? const [],
  targets: targets,
);

@meta.PipelineLayout('VsMain - FsMain')
wgpu.PipelineLayout createVsMainFsMainPipelineLayout(
 wgpu.Device device, {
  wgpu.BindGroupLayout? group0,
}) => device.createPipelineLayout(.new(
  label: '(render.wgsl) VsMain - FsMain pipeline layout',
  bindGroupLayouts: [
    group0 ?? device.createBindGroupLayout(bindGroup0LayoutDescriptor),
  ],
));

@meta.RenderPipeline('vs_main', 'fs_main')
wgpu.RenderPipeline createRenderPipeline(
  wgpu.Device device,
  List<wgpu.ColorTargetState> targets, {
  wgpu.ShaderModule? module,
  wgpu.PipelineLayout? layout,
  Overrides? overrides,
  wgpu.PrimitiveState primitive = const wgpu.PrimitiveState(),
  wgpu.MultisampleState multisample = const wgpu.MultisampleState(),
  wgpu.DepthStencilState? depthStencil,
  List<wgpu.VertexBufferLayout>? vertexBuffers,
}) {
  final _module = module ?? createShaderModule(device);
  return device.createRenderPipeline(.new(
   label: '(render.wgsl) vs_main-fs_main render pipeline',
   layout: layout ?? createVsMainFsMainPipelineLayout(device),
   vertex: createVsMainVertexState(_module, overrides: overrides, vertexBuffers: vertexBuffers),
   fragment: createFsMainFragmentState(_module, targets, overrides: overrides),
   primitive: primitive,
   multisample: multisample,
   depthStencil: depthStencil,
 ));
}

@meta.ShaderSource('render.wgsl')
const String _shaderSource = r'''
// import "shared/cubic2.wgsl"
// A cubic bezier curve.
struct Cubic2 {
  p0: vec2f,
  p1: vec2f,
  p2: vec2f,
  p3: vec2f,
}

fn cubic2_transform(c: Cubic2, m: mat4x4f) -> Cubic2 {
  return Cubic2(
    (m * vec4f(c.p0, 0.0, 1.0)).xy,
    (m * vec4f(c.p1, 0.0, 1.0)).xy,
    (m * vec4f(c.p2, 0.0, 1.0)).xy,
    (m * vec4f(c.p3, 0.0, 1.0)).xy
  );
}

// #region Evaluation

// Evaluates the cubic bezier at parameter [t].
fn cubic2_pos(c: Cubic2, t: f32) -> vec2f {
  let mt = 1.0 - t;
  let mt2 = mt * mt;
  let t2 = t * t;

  return c.p0 * (mt2 * mt) + c.p1 * (3.0 * mt2 * t) + c.p2 * (3.0 * mt * t2) + c.p3 * (t2 * t);
}

// Evaluates the tangent of the cubic bezier at parameter [t].
fn cubic2_tan(c: Cubic2, t: f32) -> vec2f {
  let mt = 1.0 - t;
  let mt2 = mt * mt;
  let t2 = t * t;

  return (c.p1 - c.p0) * (3.0 * mt2) + (c.p2 - c.p1) * (6.0 * mt * t) + (c.p3 - c.p2) * (3.0 * t2);
}

// Evaluates the acceleration of the cubic bezier at parameter [t].
fn cubic2_acc(c: Cubic2, t: f32) -> vec2f {
  let mt = 1.0 - t;
  return (c.p2 - 2.0 * c.p1 + c.p0) * (6.0 * mt) + (c.p3 - 2.0 * c.p2 + c.p1) * (6.0 * t);
}

// Evaluates the position and the tangent of the cubic bezier at parameter [t] as (pos.x, pos.y, tan.x, tan.y).
fn cubic2_pos_tan(c: Cubic2, t: f32) -> vec4f {
  let mt = 1.0 - t;
  let mt2 = mt * mt;
  let t2 = t * t;

  let pos = c.p0 * (mt2 * mt) + c.p1 * (3.0 * mt2 * t) + c.p2 * (3.0 * mt * t2) + c.p3 * (t2 * t);
  let tan = (c.p1 - c.p0) * (3.0 * mt2) + (c.p2 - c.p1) * (6.0 * mt * t) + (c.p3 - c.p2) * (3.0 * t2);

  return vec4f(pos, tan);
}

// Evalutes the signed curvature of the cubic at parameter [t].
fn cubic2_signed_curvature(c: Cubic2, t: f32) -> f32 {
  let tan = cubic2_tan(c, t);
  let acc = cubic2_acc(c, t);
  let cross = tan.x * acc.y - tan.y * acc.x;
  let speed = length(tan);
  let speed3 = speed * speed * speed;
  if (speed3 < 1.0e-9) { return 0.0; }
  return cross / speed3;
}

// Evaluates the center of curvature of the cubic at parameter [t].
fn cubic2_curvature_center(c: Cubic2, t: f32) -> vec2f {
  let pos_tan = cubic2_pos_tan(c, t);
  let pos = pos_tan.xy;
  let tan = normalize(pos_tan.zw);
  let normal = vec2f(-tan.y, tan.x);

  let k = cubic2_signed_curvature(c, t);
  if (abs(k) < 1.0e-5) { return pos + normal * (1.0e6 * sign(k)); }

  let radius = 1.0 / k;
  return pos + normal * radius;
}

// #endregion

// #region Key metrics

struct Cubic2InflectionsResult {
  count: u32,
  t: vec2f,
}

// Returns the number of inflection points and their parameter values.
fn cubic2_inflections(c: Cubic2) -> Cubic2InflectionsResult {
  const T_EPS = 1.0e-4;

  let A = c.p1 - c.p0;
  let B = c.p2 - 2.0 * c.p1 + c.p0;
  let C = c.p3 - 3.0 * c.p2 + 3.0 * c.p1 - c.p0;

  let a = B.x * C.y - B.y * C.x;
  let b = A.x * C.y - A.y * C.x;
  let c_ = A.x * B.y - A.y * B.x;

  var root_count = 0u;
  var roots = vec2f(-1.0);

  if (abs(a) < 1.0e-5) {
    if (abs(b) > 1.0e-5) {
      let t = -c_ / b;
      if (t > T_EPS && t < 1.0 - T_EPS) {
        roots[0] = t;
        root_count = 1u;
      }
    }
  }
  else {
    let disc = b * b - 4.0 * a * c_;
    if (disc >= 0.0) {
      let sqrt_disc = sqrt(disc);
      let t1 = (-b + sqrt_disc) / (2.0 * a);
      let t2 = (-b - sqrt_disc) / (2.0 * a);
      
      if (t1 > T_EPS && t1 < 1.0 - T_EPS) {
        roots[root_count] = t1;
        root_count++;
      }
      if (disc > 1.0e-6 && t2 > T_EPS && t2 < 1.0 - T_EPS) {
        roots[root_count] = t2;
        root_count++;
      }
    }
  }

  if (root_count == 2u && roots[0] > roots[1]) {
    let temp = roots[0];
    roots[0] = roots[1];
    roots[1] = temp;
  }

  return Cubic2InflectionsResult(root_count, roots);
}


struct Extrema1D { count: u32, t: vec2f }
struct Extrema2D { count: u32, t: vec4f }

fn _find_unit_quad_roots(a: f32, b: f32, c: f32) -> Extrema1D {
  var out = Extrema1D(0u, vec2f(0.0));

  if (a == 0.0) {
    if (b != 0.0) {
      let t = -c / b;
      if (t > 0.0 && t < 1.0) {
        out.t[0] = t;
        out.count = 1u;
      }
    }

    return out;
  }

  let dr_sq = b * b - 4.0 * a * c;
  if (dr_sq < 0.0) { return out; }

  let r = sqrt(dr_sq);
  let q = select(-(b + r) * 0.5, -(b - r) * 0.5, b < 0.0);

  if (a != 0.0) {
    let t1 = q / a;
    if (t1 > 0.0 && t1 < 1.0) {
      out.t[out.count] = t1;
      out.count += 1u;
    }
  }

  if (q != 0.0) {
    let t2 = c / q;
    if (t2 > 0.0 && t2 < 1.0) {
      out.t[out.count] = t2;
      out.count += 1u;
    }
  }

  if (out.count == 2u) {
    if (out.t.x > out.t.y) { out.t = out.t.yx; }
    else if (out.t.x == out.t.y) { out.count = 1u; }
  }

  return out;
}


fn _cubic_extrema_1d(a: f32, b: f32, c: f32, d: f32) -> Extrema1D {
  return _find_unit_quad_roots(
    d - a + 3.0 * (b - c),
    2.0 * (a - 2.0 * b + c),
    b - a,
  );
}


// Returns the number of extrema in x and y and their parameter values.
fn cubic2_extrema(c: Cubic2) -> Extrema2D {
  var out = Extrema2D(0u, vec4f(0.0));

  let x_extrema = _cubic_extrema_1d(c.p0.x, c.p1.x, c.p2.x, c.p3.x);
  for (var i = 0u; i < x_extrema.count; i++) {
    out.t[out.count] = x_extrema.t[i];
    out.count++;
  }

  let y_extrema = _cubic_extrema_1d(c.p0.y, c.p1.y, c.p2.y, c.p3.y);
  for (var i = 0u; i < y_extrema.count; i++) {
    out.t[out.count] = y_extrema.t[i];
    out.count++;
  }

  if (out.count > 1u) {
    for (var i = 0u; i < out.count - 1u; i++) {
      for (var j = 0u; j < out.count - i - 1u; j++) {
        if (out.t[j] > out.t[j + 1u]) {
          let temp = out.t[j];
          out.t[j] = out.t[j + 1u];
          out.t[j + 1u] = temp;
        }
      }
    }
  }

  return out;
}

struct Cubic2KeyPointsResult {
  t: array<f32, 6>,
  count: u32,
}

// Returns the sorted parameter values of the inflection points and extrema of the cubic.
fn cubic2_key_points(c: Cubic2) -> Cubic2KeyPointsResult {
  var out: Cubic2KeyPointsResult;
  out.count = 0u;

  let v0 = c.p1 - c.p0;
  let v1 = c.p2 - c.p1;
  let v2 = c.p3 - c.p2;

  let A = v2 - 2.0 * v1 + v0;
  let B = 2.0 * (v1 - v0);
  let C = v0;

  let x_extrema = _find_unit_quad_roots(A.x, B.x, C.x);
  for (var i = 0u; i < x_extrema.count; i++) {
    out.t[out.count] = x_extrema.t[i];
    out.count++;
  }

  let y_extrema = _find_unit_quad_roots(A.y, B.y, C.y);
  for (var i = 0u; i < y_extrema.count; i++) {
    out.t[out.count] = y_extrema.t[i];
    out.count++;
  }

  let inf_a = B.x * A.y - B.y * A.x;
  let inf_b = 2.0 * (C.x * A.y - C.y * A.x);
  let inf_c = C.x * B.y - C.y * B.x;

  let inflections = _find_unit_quad_roots(inf_a, inf_b, inf_c);
  for (var i = 0u; i < inflections.count; i++) {
    out.t[out.count] = inflections.t[i];
    out.count++;
  }

  if (out.count > 1u) {
    for (var i = 0u; i < out.count - 1u; i++) {
      for (var j = 0u; j < out.count - i - 1u; j++) {
        if (out.t[j] > out.t[j + 1u]) {
          let temp = out.t[j];
          out.t[j] = out.t[j + 1u];
          out.t[j + 1u] = temp;
        }
      }
    }
  }

  return out;
}

// #endregion

// #region Geometry

// Returns the rough bounding box of the cubic bezier.
fn cubic2_bbox(c: Cubic2) -> vec4f {
  let min_pt = min(c.p0, min(c.p1, min(c.p2, c.p3)));
  let max_pt = max(c.p0, max(c.p1, max(c.p2, c.p3)));
  return vec4f(min_pt, max_pt);
}

// Returns the tight bounding box of the cubic bezier.
fn cubic2_bbox_tight(c: Cubic2) -> vec4f {
  var min_pt = min(c.p0, c.p3);
  var max_pt = max(c.p0, c.p3);

  let extrema = cubic2_extrema(c);
  for (var i = 0u; i < extrema.count; i++) {
    let t = extrema.t[i];
    let pt = cubic2_pos(c, t);
    min_pt = min(min_pt, pt);
    max_pt = max(max_pt, pt);
  }

  return vec4f(min_pt, max_pt);
}

fn _cubic2_arc_len_vel(v0: vec2f, v1: vec2f, v2: vec2f, t: f32) -> f32 {
  let mt = 1.0 - t;
  let vel = v0 * (mt * mt) + v1 * (2.0 * mt * t) + v2 * (t * t);
  return length(vel);
}

fn _cubic2_arc_len_quadrature(c: Cubic2) -> f32 {
  let v0 = 3.0 * (c.p1 - c.p0);
  let v1 = 3.0 * (c.p2 - c.p1);
  let v2 = 3.0 * (c.p3 - c.p2);

  var arc_len = 0.0;

  arc_len += 0.28444444 * _cubic2_arc_len_vel(v0, v1, v2, 0.5);
  
  arc_len += 0.23931434 * _cubic2_arc_len_vel(v0, v1, v2, 0.23076535);
  arc_len += 0.23931434 * _cubic2_arc_len_vel(v0, v1, v2, 0.76923465);
  
  arc_len += 0.11846344 * _cubic2_arc_len_vel(v0, v1, v2, 0.04691008);
  arc_len += 0.11846344 * _cubic2_arc_len_vel(v0, v1, v2, 0.95308992);

  return arc_len;
}

// Returns the approximate arc length of the cubic bezier.
fn cubic2_arc_length_approximate(c: Cubic2) -> f32 {
  return _cubic2_arc_len_quadrature(c);
}

// Returns a more refined arc length estimation of the cubic bezier.
fn cubic2_arc_length(c: Cubic2) -> f32 {
  let key_points = cubic2_key_points(c);

  var total = 0.0;
  var t0 = 0.0;
  for (var i = 0u; i < key_points.count; i++) {
    let t1 = key_points.t[i];
    if (t1 - t0 > 1.0e-4) {
      let piece = cubic2_piece_at(c, t0, t1);
      total += cubic2_arc_length_approximate(piece);
      t0 = t1;
    }
  }

  if (1.0 - t0 > 1.0e-4) {
    let piece = cubic2_piece_at(c, t0, 1.0);
    total += cubic2_arc_length_approximate(piece);
  }

  return total;
}

// Returns the parameter t at arc length.
fn cubic2_distance_to_t(c: Cubic2, dist: f32) -> f32 {
  var t_min = 0.0; var t_max = 1.0; var local_t = 0.5;

  for (var iter = 0u; iter < 12u; iter++) {
    local_t = (t_min + t_max) * 0.5;
    let sub_curve = cubic2_piece_at(c, 0.0, local_t);
    let dist_at_t = cubic2_arc_length(sub_curve);

    if (dist_at_t < dist) { t_min = local_t; }
    else { t_max = local_t; }
  }

  return local_t;
}

// Returns the arc distance at parameter t.
fn cubic2_distance_at_t(c: Cubic2, t: f32) -> f32 {
  let sub = cubic2_piece_at(c, 0.0, t);
  return cubic2_arc_length(sub);
}

// Returns the number of line segments needed to approximate the cubic bezier within the given tolerance.
fn cubic2_wang(c: Cubic2, tolerance: f32) -> f32 {
  let v1 = c.p2 - 2.0 * c.p1 + c.p0;
  let v2 = c.p3 - 2.0 * c.p2 + c.p1;
  let m = max(dot(v1, v1), dot(v2, v2));
  if (m < 1.0e-6) { return 1.0; }
  return sqrt(sqrt(m) * 3.0 / (4.0 * tolerance));
}

// #endregion

// #region Subdivision

// Splits the cubic bezier at [t].
fn cubic2_chop_at(c: Cubic2, t: f32) -> array<Cubic2, 2> {
  let p01 = mix(c.p0, c.p1, t);
  let p12 = mix(c.p1, c.p2, t);
  let p23 = mix(c.p2, c.p3, t);

  let p012 = mix(p01, p12, t);
  let p123 = mix(p12, p23, t);

  let p0123 = mix(p012, p123, t);

  return array<Cubic2, 2>(
    Cubic2(c.p0, p01, p012, p0123),
    Cubic2(p0123, p123, p23, c.p3)
  );
}

// Splits the cubic bezier at [t=0.5].
fn cubic2_chop_at_half(c: Cubic2) -> array<Cubic2, 2> {
  return cubic2_chop_at(c, 0.5);
}

// Returns the cubic bezier piece between [t0] and [t1].
fn cubic2_piece_at(c: Cubic2, t0: f32, t1: f32) -> Cubic2 {
  let pos_tan_t0 = cubic2_pos_tan(c, t0);
  let pos_tan_t1 = cubic2_pos_tan(c, t1);
  let dt = (t1 - t0) / 3.0;

  return Cubic2(
    pos_tan_t0.xy,
    pos_tan_t0.xy + pos_tan_t0.zw * dt,
    pos_tan_t1.xy - pos_tan_t1.zw * dt,
    pos_tan_t1.xy
  );
}


struct Cubic2ChopAtInflectionsResult {
  cubics: array<Cubic2, 3>,
  t0: array<f32, 3>,
  t1: array<f32, 3>,
  count: u32,
}


// Splits the cubic bezier at its inflection points.
fn cubic2_chop_at_inflections(c: Cubic2) -> Cubic2ChopAtInflectionsResult {
  var out: Cubic2ChopAtInflectionsResult;
  let inflections = cubic2_inflections(c);

  if (inflections.count == 0u) {
    out.cubics[0] = c;
    out.t0[0] = 0.0;
    out.t1[0] = 1.0;
    out.count = 1u;
  }
  else if (inflections.count == 1u) {
    let t1 = inflections.t[0];
    let pieces = cubic2_chop_at(c, t1);
  
    out.cubics[0] = pieces[0];
    out.t0[0] = 0.0;
    out.t1[0] = t1;

    out.cubics[1] = pieces[1];
    out.t0[1] = t1;
    out.t1[1] = 1.0;

    out.count = 2u;
  }
  else if (inflections.count == 2u) {
    let t1 = inflections.t[0];
    let t2 = inflections.t[1];
    
    let pieces1 = cubic2_chop_at(c, t1);
    out.cubics[0] = pieces1[0];
    out.t0[0] = 0.0;
    out.t1[0] = t1;

    let pieces2 = cubic2_chop_at(pieces1[1], (t2 - t1) / (1.0 - t1));
    out.cubics[1] = pieces2[0];
    out.t0[1] = t1;
    out.t1[1] = t2;

    out.cubics[2] = pieces2[1];
    out.t0[2] = t2;
    out.t1[2] = 1.0;

    out.count = 3u;
  }

  return out;
}

// #endregion
// end import "shared/cubic2.wgsl"

// import "shared/utils.wgsl"
// Pads the bounding box by [pad] pixels in all directions.
fn bbox_pad(bbox: vec4f, pad: f32) -> vec4f {
  return vec4f(bbox.xy - vec2f(pad), bbox.zw + vec2f(pad));
}

// Returns true if the two bounding boxes intersect.
fn bbox_intersects(b1: vec4f, b2: vec4f) -> bool {
  return b1.x <= b2.z && b1.z >= b2.x && b1.y <= b2.w && b1.w >= b2.y;
}

// Returns true if the point [p] is inside the bounding box [bbox].
fn bbox_contains(bbox: vec4f, p: vec2f) -> bool {
  return p.x >= bbox.x && p.x <= bbox.z && p.y >= bbox.y && p.y <= bbox.w;
}

const PI = 3.14159265358979323;
const TWO_PI = 6.28318530717958646;

fn vec2_transform(v: vec2f, m: mat4x4f) -> vec2f {
  let res = m * vec4f(v, 0.0, 1.0);
  return res.xy;
}
// end import "shared/utils.wgsl"

// import "structs.wgsl"
struct Uniforms {
  cubic_count: u32,
  weight_anchor_count: u32,
  tolerance: f32,
  screen_size: vec2f,
  transform: mat4x4f,
}

struct VertexData {
  pos: vec2f,
  incident_edges_span: vec2u,
}

struct EdgeData {
  start_vertex_idx: u32,
  end_vertex_idx: u32,
  cubics_span: vec2u,
  weight_span: vec2u,
  width: f32,
  arc_length: f32,
  color: vec4f,
  brush_idx: u32,
}

struct CubicData {
  cubic: Cubic2,
  edge_idx: u32,
  edge_segment_idx: u32,
  edge_segment_count: u32,
  edge_start_arc_length: f32,
  opacity: f32,
}

struct BrushData {
  length: f32,
  spacing: f32,
  texture_idx: u32,
}
// end import "structs.wgsl"

// import "geometry.wgsl"
// A quad.
// - v0-v3: four vertices
const RENDER_GEOMETRY_KIND_QUAD = 0u;

// A brush stamp.
const RENDER_GEOMETRY_KIND_STAMP = 2u;

const STAMP_PADDING = 1.5f;

// // An ellipse.
// // - v0: center
// // - v1: radii
// const RENDER_GEOMETRY_KIND_ELLIPSE = 1u;

struct RenderGeometry {
  v0: vec2f,
  v1: vec2f,
  v2: vec2f,
  v3: vec2f,
  arc_length: vec2f,
  color: vec4f,
  weight: f32,
  z_index: u32,
  brush_idx: u32,
  kind: u32,
}

fn render_geometry_create_quad(v0: vec2f, v1: vec2f, v2: vec2f, v3: vec2f, arc_length: vec2f, color: vec4f, z_index: u32, brush_idx: u32) -> RenderGeometry {
  return RenderGeometry(v0, v1, v2, v3, arc_length, color, 0.0, z_index, brush_idx, RENDER_GEOMETRY_KIND_QUAD);
}

fn render_geometry_create_stamp(v0: vec2f, v1: vec2f, v2: vec2f, v3: vec2f, arc_length: vec2f, weight: f32, color: vec4f, z_index: u32, brush_idx: u32) -> RenderGeometry {
  return RenderGeometry(v0, v1, v2, v3, arc_length, color, weight, z_index, brush_idx, RENDER_GEOMETRY_KIND_STAMP);
}

// fn render_geometry_create_ellipse(c: vec2f, r: vec2f, color: vec4f, z_index: u32) -> RenderGeometry {
//   return RenderGeometry(c, r, vec2f(0.0), vec2f(0.0), color, z_index, RENDER_GEOMETRY_KIND_ELLIPSE);
// }

// fn render_geometry_create_circle(c: vec2f, r: f32, color: vec4f, z_index: u32) -> RenderGeometry {
//   return render_geometry_create_ellipse(c, vec2f(r), color, z_index);
// }
// end import "geometry.wgsl"

// import "brush/brush_circle.wgsl"
fn brush_circle(uv: vec2f) -> f32 {
  let dist = length(uv - vec2f(0.5, 0.5));
  return smoothstep(0.5, 0.45, dist);
}

fn brush_chisel(uv: vec2f) -> f32 {
  // 1. Remap UVs from [0.0, 1.0] to [-1.0, 1.0] with center at 0,0
  let p = (uv - vec2f(0.5, 0.5)) * 2.0;

  // 2. Rotate the coordinates by 45 degrees
  let c = 0.707106; // cos(45)
  let s = 0.707106; // sin(45)
  let rotated_p = vec2f(p.x * c - p.y * s, p.x * s + p.y * c);

  // 3. Mathematical Box SDF
  let half_size = vec2f(0.8, 0.15); // Wide and thin
  let corner_radius = 0.05;
  
  let d = abs(rotated_p) - half_size + vec2f(corner_radius);
  let box_sdf = length(max(d, vec2f(0.0))) + min(max(d.x, d.y), 0.0) - corner_radius;

  // 4. Convert to your fragment shader's format (0.5 is edge)
  return 0.5 - box_sdf;
}

fn hash21(p: vec2f) -> f32 {
  return fract(sin(dot(p, vec2f(12.9898, 78.233))) * 43758.5453123);
}

fn value_noise(p: vec2f) -> f32 {
  let i = floor(p);
  let f = fract(p);

  // Smooth Hermite interpolation
  let u = f * f * (3.0 - 2.0 * f);

  let a = hash21(i + vec2f(0.0, 0.0));
  let b = hash21(i + vec2f(1.0, 0.0));
  let c = hash21(i + vec2f(0.0, 1.0));
  let d = hash21(i + vec2f(1.0, 1.0));

  return mix(mix(a, b, u.x), mix(c, d, u.x), u.y);
}

fn brush_charcoal(local_uv: vec2f, stroke_uv: vec2f) -> f32 {
  let circle_sdf = length(local_uv) - 0.8;

  let smudge_scale = 0.9;
  let n = value_noise(stroke_uv * smudge_scale);

  let grain = (n - 0.5) * 0.6;

  let streak_scale = 0.66;
  let streaks = sin(stroke_uv.x * streak_scale) * 0.05;

  let noisy_sdf = circle_sdf + grain + streaks;

  return 0.5 - noisy_sdf;
}

fn brush_stiff_acrylic(uv: vec2f) -> f32 {
  let p = (uv - vec2f(0.5, 0.5)) * 2.0;

  // 1. Base flat brush shape
  let half_size = vec2f(0.25, 0.85);
  let d = abs(p) - half_size;
  let box_dist = length(max(d, vec2f(0.0))) + min(max(d.x, d.y), 0.0);

  // 2. Bristle Grooves
  // We use high-frequency sine waves along the Y-axis.
  // Because they are purely Y-based, they will drag seamlessly along the X-axis!
  let bristles = sin(p.y * 35.0) * 0.04 + sin(p.y * 80.0) * 0.02;

  // 3. Add some macro-clumping so the edges aren't perfectly straight
  let clumping = sin(p.y * 5.0) * 0.05;

  return 0.5 - (box_dist + bristles + clumping);
}

fn brush_crisp_inker(uv: vec2f, weight: f32) -> f32 {
  // 1. Remap UVs to a centered [-1.0, 1.0] coordinate space
  let p = uv * 2.0 - 1.0;

  // 2. Map the pressure (weight) to a physical radius
  let w = clamp(weight, 0.0, 1.0);
  
  // At lowest pressure, radius is 0.02 (hairline).
  // At highest pressure, radius is 0.45 (massive overlap).
  let r = mix(0.02, 0.45, w);

  // 3. Stretch the shape slightly along the X-axis (tangent)
  // This turns our dots into overlapping ovals, ensuring the stroke 
  // doesn't look dotted if your spacing parameter is slightly wide.
  let p_stretched = vec2f(p.x * 0.5, p.y);

  // 4. Construct the Brush via SDF Union ( min() )
  var d = 100.0;

  // Center hair (Thickest)
  d = min(d, length(p_stretched - vec2f(0.0, 0.0)) - r);

  // Inner hairs (Slightly smaller to give the brush a rounded profile)
  d = min(d, length(p_stretched - vec2f(0.0, -0.3)) - (r * 0.85));
  d = min(d, length(p_stretched - vec2f(0.0, 0.3)) - (r * 0.85));

  // Outer hairs (Thinnest)
  d = min(d, length(p_stretched - vec2f(0.0, -0.6)) - (r * 0.55));
  d = min(d, length(p_stretched - vec2f(0.0, 0.6)) - (r * 0.55));

  // 5. Convert to your fragment shader's threshold format (0.5 is the edge)
  return 0.5 - d;
}

fn brush_wet_acrylic(uv: vec2f, weight: f32) -> f32 {
  // Remap to [-1, 1].
  // Because of our tangent rotation: 
  // +X is forward (leading edge), -X is backward (trailing edge).
  let p = uv * 2.0 - 1.0;
  let w = clamp(weight, 0.01, 1.0);

  // 1. The Paint Drop (Leading Edge)
  // A smooth circle shifted slightly forward to act as the heavy bead of wet paint.
  let drop_dist = length(p - vec2f(0.2, 0.0));

  // 2. The Clumped Bristles (Trailing Edge)
  // Wet hairs stick together. We use low-frequency sine waves to create 
  // thick, smooth groups of bristles rather than high-frequency noise.
  let clumps = sin(p.y * 12.0) * 0.1 + sin(p.y * 28.0) * 0.05;
  
  // A V-shape that flares out and fades toward the back of the brush
  let trail_dist = abs(p.y) - (p.x * 0.5) + clumps;

  // 3. Directional Blending
  // If we are at the front of the stamp, it's a smooth drop. 
  // If we are at the back, it breaks into streaky trails.
  let front_mask = smoothstep(-0.3, 0.3, p.x);
  let shape_sdf = mix(trail_dist, drop_dist, front_mask);

  // 4. Pressure Dynamics
  // Pressing hard gives a massive solid radius, pressing light leaves thin streaks
  let radius = mix(0.15, 0.7, w);

  // 5. Convert to your Fragment Shader threshold (0.5 is the exact edge)
  return 0.5 - (shape_sdf - radius);
}

fn brush_impressionist(local_uv: vec2f, stroke_uv: vec2f, pressure: f32) -> f32 {
  // 1. Organic Shape Wobble (Destroys the perfect circle outline)
  // We generate a slow, low-frequency noise purely to alter the brush's footprint.
  let wobble_scale = vec2f(0.015, 0.015);
  let shape_wobble = value_noise(stroke_uv * wobble_scale);
  
  // This smoothly expands and contracts the base radius by +/- 0.25 units
  let radius_offset = (shape_wobble - 0.5) * 0.5; 
  
  let width_squish = mix(1.2, 0.9, pressure);
  let p = vec2f(local_uv.x, local_uv.y * width_squish);
  
  // The base SDF now has an undulating, liquid boundary instead of a perfect curve.
  let base_sdf = length(p) - (1.0 + radius_offset);

  // 2. Domain Warping
  let warp_scale = vec2f(0.02, 0.05);
  let warp = value_noise(stroke_uv * warp_scale);
  let warp_offset = (warp - 0.5) * 15.0; 
  let warped_uv = vec2f(stroke_uv.x, stroke_uv.y + warp_offset);

  // 3. Rounded Ribbon Bristles
  // Lowered the Y-scale slightly to make the globs even wider/chunkier
  let bristle_scale = vec2f(0.002, 1.0);
  let raw_bristle = value_noise(warped_uv * bristle_scale);
  
  // THE SINE WAVE TRICK:
  // Instead of using smoothstep (which creates flat plateaus and sharp drop-offs),
  // we map the noise through a sine wave. This converts the linear noise into 
  // perfectly rounded overlapping tubes (like macaroni or thick paint globs).
  let paint_tubes = sin(raw_bristle * 3.1415); 
  
  // Invert it so the peaks of the tubes carve smooth, U-shaped valleys into the SDF
  let soft_carve = 1.0 - paint_tubes;

  // 4. Pressure Dampening
  let pressure_curve = pow(pressure, 3.0); 
  
  // Lowered max depth to 0.8 so the U-shaped valleys don't slice too deep.
  // We removed the sharp `max()` cutoff, so the transition is perfectly smooth.
  let bristle_depth = (1.0 - pressure_curve) * 0.8; 
  let dynamic_bristles = soft_carve * bristle_depth;

  // 5. Combine
  let noisy_sdf = base_sdf + dynamic_bristles;

  return 0.5 - noisy_sdf;
}

fn brush_standard(local_uv: vec2f, stroke_uv: vec2f, pressure: f32) -> f32 {
  // 1. Base Shape
  let base_sdf = length(local_uv) - 1.75;

  // 2. Domain Warping
  // X is Length, Y is Width. We want slow variation along both.
  let warp_scale = vec2f(0.02, 0.05);
  let warp = value_noise(stroke_uv * warp_scale);
  
  // This pushes the bristles left and right across the WIDTH of the brush
  let warp_offset = (warp - 0.5) * 10.0;
  
  // Apply warp to the Y coordinate (Width)
  let warped_uv = vec2f(stroke_uv.x, stroke_uv.y + warp_offset);

  // 3. Bristle Generation (Swapped Axes)
  // X (Length) gets the extremely low scale to create continuous unbroken lines.
  // Y (Width) gets the high scale to create dense, tight bristles.
  let bristle_scale = vec2f(0.02, 0.4); 
  var raw_bristle = value_noise(warped_uv * bristle_scale);
  raw_bristle = pow(raw_bristle, 1.5);
  
  let ridge = 1.0 - abs((raw_bristle * 2.0) - 1.0);
  let sharp_bristles = ridge * 1.5; 

  // 4. Pressure Mechanic
  let pressure_curve = pow(pressure, 2.0); 
  let bristle_depth = (1.0 - pressure_curve) * 0.6; 
  let dynamic_bristles = sharp_bristles * bristle_depth;

  // 5. Combine
  let noisy_sdf = base_sdf + dynamic_bristles;

  return 0.5 - noisy_sdf;
}

fn brush_cartoony(local_uv: vec2f, stroke_uv: vec2f, pressure: f32) -> f32 {
  // 1. The Wobbly Base
  // Changes slowly along the arc length (stroke_uv.x) to make the brush pulse.
  let outline_wobble = sin(stroke_uv.x * 0.02) * 0.1;
  let base_sdf = length(local_uv) - (0.8 + outline_wobble);

  // 2. The Ribbons
  // Weave them slightly side-to-side as the stroke travels.
  let weave = sin(stroke_uv.x * 0.1) * 0.5;
  
  // Evaluated STRICTLY on the width axis (local_uv.y). 
  // Overlapping stamps share the exact same Y-axis, so the stripes merge perfectly.
  let bands = cos((local_uv.y + weave) * 32.0); 
  
  // Slice the wave into thick ribbons (solid) and U-shaped gaps
  let gaps = 1.0 - smoothstep(0.5, 0.9, bands);

  // 3. Pressure Dampening
  let pressure_curve = pow(pressure, 2.0);
  
  // Max carve is 0.8 so the gaps never slice completely through the core of the paint
  let carve_amount = gaps;

  // 4. Combine (NO EDGE MASK)
  // We simply add the carve directly to the SDF. The gaps will cut clean through 
  // the stamp, perfectly connecting to the stamp next to it.
  let final_sdf = carve_amount;

  return 0.5 - final_sdf;
}
// end import "brush/brush_circle.wgsl"


@group(0) @binding(0) var<uniform> u: Uniforms;
@group(0) @binding(1) var<storage, read> in_render_geometry: array<RenderGeometry>;
@group(0) @binding(2) var<storage, read> in_brush_data: array<BrushData>;
@group(0) @binding(3) var brush_textures: texture_2d_array<f32>;
@group(0) @binding(4) var brush_sampler: sampler;

struct VtxOut {
  @builtin(position) pos: vec4f,
  @location(0) frag_pos: vec2f,
  @location(1) stroke_uv: vec2f,
  @location(2) local_uv: vec2f,
  @location(3) weight: f32,
  @location(4) @interpolate(flat) instance_idx: u32,
}

@vertex
fn vs_main(
  @builtin(vertex_index) vtx_idx: u32,
  @builtin(instance_index) instance_idx: u32,
) -> VtxOut {
  let geom = in_render_geometry[instance_idx];

  var pos_screen = vec2f(0.0);
  var stroke_uv = vec2f(0.0);
  var uv = vec2f(0.0);

  if (geom.kind == RENDER_GEOMETRY_KIND_QUAD) {
    switch(vtx_idx) {
      case 0u, 3u: { pos_screen = geom.v0; uv = vec2f(0.0, 0.0); stroke_uv = vec2f(geom.arc_length.x, 0.0); }
      case 1u:     { pos_screen = geom.v1; uv = vec2f(1.0, 0.0); stroke_uv = vec2f(geom.arc_length.y, 0.0); }
      case 2u, 4u: { pos_screen = geom.v2; uv = vec2f(1.0, 1.0); stroke_uv = vec2f(geom.arc_length.y, 1.0); }
      case 5u:     { pos_screen = geom.v3; uv = vec2f(0.0, 1.0); stroke_uv = vec2f(geom.arc_length.x, 1.0); }
      default: {}
    };
  }
  else if (geom.kind == RENDER_GEOMETRY_KIND_STAMP) {
    let size = geom.arc_length.y - geom.arc_length.x;
    let u = STAMP_PADDING;
    let v = STAMP_PADDING;

    switch(vtx_idx) {
      case 0u, 3u: { pos_screen = geom.v0; uv = vec2f(-u, -v); stroke_uv = vec2f(geom.arc_length.x, 0.0); }
      case 1u:     { pos_screen = geom.v1; uv = vec2f(u, -v); stroke_uv = vec2f(geom.arc_length.y, 0.0); }
      case 2u, 4u: { pos_screen = geom.v2; uv = vec2f(u, v); stroke_uv = vec2f(geom.arc_length.y, size); }
      case 5u:     { pos_screen = geom.v3; uv = vec2f(-u, v); stroke_uv = vec2f(geom.arc_length.x, size); }
      default: {}
    };
  }

  let clip_x = (pos_screen.x / u.screen_size.x) * 2.0 - 1.0;
  let clip_y = 1.0 - (pos_screen.y / u.screen_size.y) * 2.0;

  var out: VtxOut;
  out.pos = vec4f(clip_x, clip_y, 0.0, 1.0);
  out.frag_pos = pos_screen;
  out.stroke_uv = stroke_uv;
  out.local_uv = uv;
  out.instance_idx = instance_idx;
  return out;
}

const FS_DEBUG_WIREFRAME = false;

@fragment
fn fs_main(in: VtxOut) -> @location(0) vec4f {
  let geom = in_render_geometry[in.instance_idx];

  if (FS_DEBUG_WIREFRAME) {
    let p = in.frag_pos;

    let e0 = geom.v1 - geom.v0; let w0 = p - geom.v0;
    let e1 = geom.v2 - geom.v1; let w1 = p - geom.v1;
    let e2 = geom.v3 - geom.v2; let w2 = p - geom.v2;
    let e3 = geom.v0 - geom.v3; let w3 = p - geom.v3;

    let pq0 = w0 - e0 * clamp(dot(w0, e0) / max(dot(e0, e0), 1.0e-8), 0.0, 1.0);
    let pq1 = w1 - e1 * clamp(dot(w1, e1) / max(dot(e1, e1), 1.0e-8), 0.0, 1.0);
    let pq2 = w2 - e2 * clamp(dot(w2, e2) / max(dot(e2, e2), 1.0e-8), 0.0, 1.0);
    let pq3 = w3 - e3 * clamp(dot(w3, e3) / max(dot(e3, e3), 1.0e-8), 0.0, 1.0);

    let d2 = min(min(dot(pq0, pq0), dot(pq1, pq1)), min(dot(pq2, pq2), dot(pq3, pq3)));
    let dist_to_edge = sqrt(d2);

    // --- DEBUG: Wireframe ---
    // Paint a 1-pixel red border around the quad edges
    if (dist_to_edge < 1.0) {
      return vec4f(1.0, 0.0, 0.0, 1.0); // Solid Red
    }

    // Paint a 1-pixel line at centerpoint
    let c0 = (geom.v0 + geom.v3) * 0.5;
    let c1 = (geom.v1 + geom.v2) * 0.5;
    let ec = c1 - c0;
    let wc = p - c0;
    let pqc = wc - ec * clamp(dot(wc, ec) / max(dot(ec, ec), 1.0e-8), 0.0, 1.0);
    let dist_to_center = length(pqc);

    if (dist_to_center < 1.0) {
      return vec4f(0.0, 1.0, 0.0, 1.0);  // solid green centerline
    }

    // Normal stub: perpendicular to the centerline, drawn from its midpoint.
    let center_mid = (c0 + c1) * 0.5;
    let tan = normalize(ec);
    let normal = vec2f(-tan.y, tan.x);
    let stub_len = 60.0;  // pixels
    let stub_end = center_mid + normal * stub_len;

    // Distance from p to the stub segment.
    let es = stub_end - center_mid;
    let ws = p - center_mid;
    let pqs = ws - es * clamp(dot(ws, es) / max(dot(es, es), 1.0e-8), 0.0, 1.0);
    let dist_to_stub = length(pqs);

    if (dist_to_stub < 1.0) {
      return vec4f(0.0, 0.5, 1.0, 1.0);
    }
  }

  if (geom.kind == RENDER_GEOMETRY_KIND_QUAD) {
    return vec4f(geom.color.rgb, geom.color.a);
  }
  else {
    // return vec4f(1.0);
    // let dist = brush_charcoal(in.local_uv, in.stroke_uv);
    // let dist = brush_cartoony(in.local_uv, in.stroke_uv, in.weight);
    // let dist = brush_standard(in.local_uv, in.stroke_uv, in.weight);
    let dist = brush_circle(in.local_uv);
    let pixel_aa = fwidth(dist);

    let alpha = smoothstep(0.5 - pixel_aa, 0.5 + pixel_aa, dist);
    if (alpha < 0.005) { discard; }

    return vec4f(geom.color.rgb, geom.color.a * alpha);
  }
}

''';

@meta.ShaderModule('render.wgsl')
wgpu.ShaderModule createShaderModule(wgpu.Device device) => device.createShaderModule(.new(
  label: '(render.wgsl) module',
  next: const wgpu.ShaderSourceWGSL(code: _shaderSource),
));