// GENERATED CODE - DO NOT MODIFY BY HAND
//
// Generated by tool/webgpu_gen.py
//
// ignore_for_file: unused_element, unused_field, unreachable_switch_case

part of '../src.dart';

class StringView {
  const StringView({
    this.data,
    this.length = 0,
  });

  final String? data;
  final int length;

  Pointer<bindings.WGPUStringView> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPUStringView>();
    ptr.ref.data = (data?.toNativeUtf8(allocator: allocator).cast() ?? nullptr);
    ptr.ref.length = length;

    return ptr;
  }

  factory StringView.fromNative(bindings.WGPUStringView v) {
    return StringView(
      data: (v.data != nullptr ? v.data.cast<ffi.Utf8>().toDartString() : null),
      length: v.length,
    );
  }
}

class BufferMapCallbackInfo {
  const BufferMapCallbackInfo({
    this.next,
    this.mode = CallbackMode.waitAnyOnly,
    required this.callback,
  });

  final ChainedStruct? next;
  final CallbackMode mode;
  final BufferMapCallback? callback;

  Pointer<bindings.WGPUBufferMapCallbackInfo> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPUBufferMapCallbackInfo>();
    ptr.ref.nextInChain = next?.toNative(allocator).cast() ?? nullptr;
    ptr.ref.modeAsInt = mode.value;
    if (callback != null) {
      ptr.ref.callback = _BufferMapCallbackRegistry._nativeFunction;
      ptr.ref.userdata1 = _BufferMapCallbackRegistry.register(callback!);
    }
    else {
      ptr.ref.callback = nullptr;
      ptr.ref.userdata1 = nullptr;
    }

    return ptr;
  }

  factory BufferMapCallbackInfo.fromNative(bindings.WGPUBufferMapCallbackInfo v) {
    return BufferMapCallbackInfo(
      next: ChainedStruct.fromNative(v.nextInChain),
      mode: CallbackMode.fromValue(v.modeAsInt),
      callback: null,
    );
  }
}

class CompilationInfoCallbackInfo {
  const CompilationInfoCallbackInfo({
    this.next,
    this.mode = CallbackMode.waitAnyOnly,
    required this.callback,
  });

  final ChainedStruct? next;
  final CallbackMode mode;
  final CompilationInfoCallback? callback;

  Pointer<bindings.WGPUCompilationInfoCallbackInfo> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPUCompilationInfoCallbackInfo>();
    ptr.ref.nextInChain = next?.toNative(allocator).cast() ?? nullptr;
    ptr.ref.modeAsInt = mode.value;
    if (callback != null) {
      ptr.ref.callback = _CompilationInfoCallbackRegistry._nativeFunction;
      ptr.ref.userdata1 = _CompilationInfoCallbackRegistry.register(callback!);
    }
    else {
      ptr.ref.callback = nullptr;
      ptr.ref.userdata1 = nullptr;
    }

    return ptr;
  }

  factory CompilationInfoCallbackInfo.fromNative(bindings.WGPUCompilationInfoCallbackInfo v) {
    return CompilationInfoCallbackInfo(
      next: ChainedStruct.fromNative(v.nextInChain),
      mode: CallbackMode.fromValue(v.modeAsInt),
      callback: null,
    );
  }
}

class CreateComputePipelineAsyncCallbackInfo {
  const CreateComputePipelineAsyncCallbackInfo({
    this.next,
    this.mode = CallbackMode.waitAnyOnly,
    required this.callback,
  });

  final ChainedStruct? next;
  final CallbackMode mode;
  final CreateComputePipelineAsyncCallback? callback;

  Pointer<bindings.WGPUCreateComputePipelineAsyncCallbackInfo> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPUCreateComputePipelineAsyncCallbackInfo>();
    ptr.ref.nextInChain = next?.toNative(allocator).cast() ?? nullptr;
    ptr.ref.modeAsInt = mode.value;
    if (callback != null) {
      ptr.ref.callback = _CreateComputePipelineAsyncCallbackRegistry._nativeFunction;
      ptr.ref.userdata1 = _CreateComputePipelineAsyncCallbackRegistry.register(callback!);
    }
    else {
      ptr.ref.callback = nullptr;
      ptr.ref.userdata1 = nullptr;
    }

    return ptr;
  }

  factory CreateComputePipelineAsyncCallbackInfo.fromNative(bindings.WGPUCreateComputePipelineAsyncCallbackInfo v) {
    return CreateComputePipelineAsyncCallbackInfo(
      next: ChainedStruct.fromNative(v.nextInChain),
      mode: CallbackMode.fromValue(v.modeAsInt),
      callback: null,
    );
  }
}

class CreateRenderPipelineAsyncCallbackInfo {
  const CreateRenderPipelineAsyncCallbackInfo({
    this.next,
    this.mode = CallbackMode.waitAnyOnly,
    required this.callback,
  });

  final ChainedStruct? next;
  final CallbackMode mode;
  final CreateRenderPipelineAsyncCallback? callback;

  Pointer<bindings.WGPUCreateRenderPipelineAsyncCallbackInfo> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPUCreateRenderPipelineAsyncCallbackInfo>();
    ptr.ref.nextInChain = next?.toNative(allocator).cast() ?? nullptr;
    ptr.ref.modeAsInt = mode.value;
    if (callback != null) {
      ptr.ref.callback = _CreateRenderPipelineAsyncCallbackRegistry._nativeFunction;
      ptr.ref.userdata1 = _CreateRenderPipelineAsyncCallbackRegistry.register(callback!);
    }
    else {
      ptr.ref.callback = nullptr;
      ptr.ref.userdata1 = nullptr;
    }

    return ptr;
  }

  factory CreateRenderPipelineAsyncCallbackInfo.fromNative(bindings.WGPUCreateRenderPipelineAsyncCallbackInfo v) {
    return CreateRenderPipelineAsyncCallbackInfo(
      next: ChainedStruct.fromNative(v.nextInChain),
      mode: CallbackMode.fromValue(v.modeAsInt),
      callback: null,
    );
  }
}

class DeviceLostCallbackInfo {
  const DeviceLostCallbackInfo({
    this.next,
    this.mode = CallbackMode.waitAnyOnly,
    required this.callback,
  });

  final ChainedStruct? next;
  final CallbackMode mode;
  final DeviceLostCallbackListener? callback;

  Pointer<bindings.WGPUDeviceLostCallbackInfo> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPUDeviceLostCallbackInfo>();
    ptr.ref.nextInChain = next?.toNative(allocator).cast() ?? nullptr;
    ptr.ref.modeAsInt = mode.value;
    if (callback != null) {
      ptr.ref.callback = callback!._nativeFunction;
      ptr.ref.userdata1 = callback!._userdata;
    }
    else {
      ptr.ref.callback = nullptr;
      ptr.ref.userdata1 = nullptr;
    }

    return ptr;
  }

  factory DeviceLostCallbackInfo.fromNative(bindings.WGPUDeviceLostCallbackInfo v) {
    return DeviceLostCallbackInfo(
      next: ChainedStruct.fromNative(v.nextInChain),
      mode: CallbackMode.fromValue(v.modeAsInt),
      callback: null,
    );
  }
}

class LoggingCallbackInfo {
  const LoggingCallbackInfo({
    this.next,
    required this.callback,
  });

  final ChainedStruct? next;
  final LoggingCallbackListener? callback;

  Pointer<bindings.WGPULoggingCallbackInfo> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPULoggingCallbackInfo>();
    ptr.ref.nextInChain = next?.toNative(allocator).cast() ?? nullptr;
    if (callback != null) {
      ptr.ref.callback = callback!._nativeFunction;
      ptr.ref.userdata1 = callback!._userdata;
    }
    else {
      ptr.ref.callback = nullptr;
      ptr.ref.userdata1 = nullptr;
    }

    return ptr;
  }

  factory LoggingCallbackInfo.fromNative(bindings.WGPULoggingCallbackInfo v) {
    return LoggingCallbackInfo(
      next: ChainedStruct.fromNative(v.nextInChain),
      callback: null,
    );
  }
}

class PopErrorScopeCallbackInfo {
  const PopErrorScopeCallbackInfo({
    this.next,
    this.mode = CallbackMode.waitAnyOnly,
    required this.callback,
  });

  final ChainedStruct? next;
  final CallbackMode mode;
  final PopErrorScopeCallback? callback;

  Pointer<bindings.WGPUPopErrorScopeCallbackInfo> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPUPopErrorScopeCallbackInfo>();
    ptr.ref.nextInChain = next?.toNative(allocator).cast() ?? nullptr;
    ptr.ref.modeAsInt = mode.value;
    if (callback != null) {
      ptr.ref.callback = _PopErrorScopeCallbackRegistry._nativeFunction;
      ptr.ref.userdata1 = _PopErrorScopeCallbackRegistry.register(callback!);
    }
    else {
      ptr.ref.callback = nullptr;
      ptr.ref.userdata1 = nullptr;
    }

    return ptr;
  }

  factory PopErrorScopeCallbackInfo.fromNative(bindings.WGPUPopErrorScopeCallbackInfo v) {
    return PopErrorScopeCallbackInfo(
      next: ChainedStruct.fromNative(v.nextInChain),
      mode: CallbackMode.fromValue(v.modeAsInt),
      callback: null,
    );
  }
}

class QueueWorkDoneCallbackInfo {
  const QueueWorkDoneCallbackInfo({
    this.next,
    this.mode = CallbackMode.waitAnyOnly,
    required this.callback,
  });

  final ChainedStruct? next;
  final CallbackMode mode;
  final QueueWorkDoneCallback? callback;

  Pointer<bindings.WGPUQueueWorkDoneCallbackInfo> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPUQueueWorkDoneCallbackInfo>();
    ptr.ref.nextInChain = next?.toNative(allocator).cast() ?? nullptr;
    ptr.ref.modeAsInt = mode.value;
    if (callback != null) {
      ptr.ref.callback = _QueueWorkDoneCallbackRegistry._nativeFunction;
      ptr.ref.userdata1 = _QueueWorkDoneCallbackRegistry.register(callback!);
    }
    else {
      ptr.ref.callback = nullptr;
      ptr.ref.userdata1 = nullptr;
    }

    return ptr;
  }

  factory QueueWorkDoneCallbackInfo.fromNative(bindings.WGPUQueueWorkDoneCallbackInfo v) {
    return QueueWorkDoneCallbackInfo(
      next: ChainedStruct.fromNative(v.nextInChain),
      mode: CallbackMode.fromValue(v.modeAsInt),
      callback: null,
    );
  }
}

class RequestAdapterCallbackInfo {
  const RequestAdapterCallbackInfo({
    this.next,
    this.mode = CallbackMode.waitAnyOnly,
    required this.callback,
  });

  final ChainedStruct? next;
  final CallbackMode mode;
  final RequestAdapterCallback? callback;

  Pointer<bindings.WGPURequestAdapterCallbackInfo> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPURequestAdapterCallbackInfo>();
    ptr.ref.nextInChain = next?.toNative(allocator).cast() ?? nullptr;
    ptr.ref.modeAsInt = mode.value;
    if (callback != null) {
      ptr.ref.callback = _RequestAdapterCallbackRegistry._nativeFunction;
      ptr.ref.userdata1 = _RequestAdapterCallbackRegistry.register(callback!);
    }
    else {
      ptr.ref.callback = nullptr;
      ptr.ref.userdata1 = nullptr;
    }

    return ptr;
  }

  factory RequestAdapterCallbackInfo.fromNative(bindings.WGPURequestAdapterCallbackInfo v) {
    return RequestAdapterCallbackInfo(
      next: ChainedStruct.fromNative(v.nextInChain),
      mode: CallbackMode.fromValue(v.modeAsInt),
      callback: null,
    );
  }
}

class RequestDeviceCallbackInfo {
  const RequestDeviceCallbackInfo({
    this.next,
    this.mode = CallbackMode.waitAnyOnly,
    required this.callback,
  });

  final ChainedStruct? next;
  final CallbackMode mode;
  final RequestDeviceCallback? callback;

  Pointer<bindings.WGPURequestDeviceCallbackInfo> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPURequestDeviceCallbackInfo>();
    ptr.ref.nextInChain = next?.toNative(allocator).cast() ?? nullptr;
    ptr.ref.modeAsInt = mode.value;
    if (callback != null) {
      ptr.ref.callback = _RequestDeviceCallbackRegistry._nativeFunction;
      ptr.ref.userdata1 = _RequestDeviceCallbackRegistry.register(callback!);
    }
    else {
      ptr.ref.callback = nullptr;
      ptr.ref.userdata1 = nullptr;
    }

    return ptr;
  }

  factory RequestDeviceCallbackInfo.fromNative(bindings.WGPURequestDeviceCallbackInfo v) {
    return RequestDeviceCallbackInfo(
      next: ChainedStruct.fromNative(v.nextInChain),
      mode: CallbackMode.fromValue(v.modeAsInt),
      callback: null,
    );
  }
}

class UncapturedErrorCallbackInfo {
  const UncapturedErrorCallbackInfo({
    this.next,
    required this.callback,
  });

  final ChainedStruct? next;
  final UncapturedErrorCallbackListener? callback;

  Pointer<bindings.WGPUUncapturedErrorCallbackInfo> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPUUncapturedErrorCallbackInfo>();
    ptr.ref.nextInChain = next?.toNative(allocator).cast() ?? nullptr;
    if (callback != null) {
      ptr.ref.callback = callback!._nativeFunction;
      ptr.ref.userdata1 = callback!._userdata;
    }
    else {
      ptr.ref.callback = nullptr;
      ptr.ref.userdata1 = nullptr;
    }

    return ptr;
  }

  factory UncapturedErrorCallbackInfo.fromNative(bindings.WGPUUncapturedErrorCallbackInfo v) {
    return UncapturedErrorCallbackInfo(
      next: ChainedStruct.fromNative(v.nextInChain),
      callback: null,
    );
  }
}

class AdapterPropertiesD3D extends ChainedStruct {
  const AdapterPropertiesD3D({
    this.shaderModel = 0,
    super.next,
  });

  final int shaderModel;

  @override
  Pointer<bindings.WGPUAdapterPropertiesD3D> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPUAdapterPropertiesD3D>();
    ptr.ref.chain.sTypeAsInt = sType.value;
    ptr.ref.chain.next = next?.toNative(allocator).cast() ?? nullptr;
    ptr.ref.shaderModel = shaderModel;

    return ptr;
  }

  factory AdapterPropertiesD3D.fromNative(bindings.WGPUAdapterPropertiesD3D v) {
    return AdapterPropertiesD3D(
      shaderModel: v.shaderModel,
    );
  }

  @override
  SType get sType => .adapterPropertiesD3D;
}

class AdapterPropertiesDrm extends ChainedStruct {
  const AdapterPropertiesDrm({
    this.hasPrimary = false,
    this.hasRender = false,
    this.primaryMajor = 0,
    this.primaryMinor = 0,
    this.renderMajor = 0,
    this.renderMinor = 0,
    super.next,
  });

  final bool hasPrimary;
  final bool hasRender;
  final int primaryMajor;
  final int primaryMinor;
  final int renderMajor;
  final int renderMinor;

  @override
  Pointer<bindings.WGPUAdapterPropertiesDrm> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPUAdapterPropertiesDrm>();
    ptr.ref.chain.sTypeAsInt = sType.value;
    ptr.ref.chain.next = next?.toNative(allocator).cast() ?? nullptr;
    ptr.ref.hasPrimary = (hasPrimary ? 1 : 0);
    ptr.ref.hasRender = (hasRender ? 1 : 0);
    ptr.ref.primaryMajor = primaryMajor;
    ptr.ref.primaryMinor = primaryMinor;
    ptr.ref.renderMajor = renderMajor;
    ptr.ref.renderMinor = renderMinor;

    return ptr;
  }

  factory AdapterPropertiesDrm.fromNative(bindings.WGPUAdapterPropertiesDrm v) {
    return AdapterPropertiesDrm(
      hasPrimary: (v.hasPrimary != 0),
      hasRender: (v.hasRender != 0),
      primaryMajor: v.primaryMajor,
      primaryMinor: v.primaryMinor,
      renderMajor: v.renderMajor,
      renderMinor: v.renderMinor,
    );
  }

  @override
  SType get sType => .adapterPropertiesDrm;
}

class AdapterPropertiesVk extends ChainedStruct {
  const AdapterPropertiesVk({
    this.driverVersion = 0,
    super.next,
  });

  final int driverVersion;

  @override
  Pointer<bindings.WGPUAdapterPropertiesVk> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPUAdapterPropertiesVk>();
    ptr.ref.chain.sTypeAsInt = sType.value;
    ptr.ref.chain.next = next?.toNative(allocator).cast() ?? nullptr;
    ptr.ref.driverVersion = driverVersion;

    return ptr;
  }

  factory AdapterPropertiesVk.fromNative(bindings.WGPUAdapterPropertiesVk v) {
    return AdapterPropertiesVk(
      driverVersion: v.driverVersion,
    );
  }

  @override
  SType get sType => .adapterPropertiesVk;
}

class AdapterPropertiesWGPU extends ChainedStruct {
  const AdapterPropertiesWGPU({
    this.backendType = BackendType.undefined,
    super.next,
  });

  final BackendType backendType;

  @override
  Pointer<bindings.WGPUAdapterPropertiesWGPU> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPUAdapterPropertiesWGPU>();
    ptr.ref.chain.sTypeAsInt = sType.value;
    ptr.ref.chain.next = next?.toNative(allocator).cast() ?? nullptr;
    ptr.ref.backendTypeAsInt = backendType.value;

    return ptr;
  }

  factory AdapterPropertiesWGPU.fromNative(bindings.WGPUAdapterPropertiesWGPU v) {
    return AdapterPropertiesWGPU(
      backendType: BackendType.fromValue(v.backendTypeAsInt),
    );
  }

  @override
  SType get sType => .adapterPropertiesWGPU;
}

class BindingResource {
  const BindingResource({
    this.next,
    this.buffer,
    this.offset = 0,
    this.size = 0,
    this.sampler,
    this.textureView,
  });

  final ChainedStruct? next;
  final Buffer? buffer;
  final int offset;
  final int size;
  final Sampler? sampler;
  final TextureView? textureView;

  Pointer<bindings.WGPUBindingResource> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPUBindingResource>();
    ptr.ref.nextInChain = next?.toNative(allocator).cast() ?? nullptr;
    ptr.ref.buffer = (buffer?._ptr ?? nullptr);
    ptr.ref.offset = offset;
    ptr.ref.size = size;
    ptr.ref.sampler = (sampler?._ptr ?? nullptr);
    ptr.ref.textureView = (textureView?._ptr ?? nullptr);

    return ptr;
  }

  factory BindingResource.fromNative(bindings.WGPUBindingResource v) {
    return BindingResource(
      next: ChainedStruct.fromNative(v.nextInChain),
      buffer: (v.buffer != nullptr ? Buffer._(v.buffer) : null),
      offset: v.offset,
      size: v.size,
      sampler: (v.sampler != nullptr ? Sampler._(v.sampler) : null),
      textureView: (v.textureView != nullptr ? TextureView._(v.textureView) : null),
    );
  }
}

class BlendComponent {
  const BlendComponent({
    this.operation = BlendOperation.undefined,
    this.srcFactor = BlendFactor.undefined,
    this.dstFactor = BlendFactor.undefined,
  });

  final BlendOperation operation;
  final BlendFactor srcFactor;
  final BlendFactor dstFactor;

  Pointer<bindings.WGPUBlendComponent> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPUBlendComponent>();
    ptr.ref.operationAsInt = operation.value;
    ptr.ref.srcFactorAsInt = srcFactor.value;
    ptr.ref.dstFactorAsInt = dstFactor.value;

    return ptr;
  }

  factory BlendComponent.fromNative(bindings.WGPUBlendComponent v) {
    return BlendComponent(
      operation: BlendOperation.fromValue(v.operationAsInt),
      srcFactor: BlendFactor.fromValue(v.srcFactorAsInt),
      dstFactor: BlendFactor.fromValue(v.dstFactorAsInt),
    );
  }
}

class BufferBindingLayout {
  const BufferBindingLayout({
    this.next,
    this.type = BufferBindingType.bindingNotUsed,
    this.hasDynamicOffset = false,
    this.minBindingSize = 0,
  });

  final ChainedStruct? next;
  final BufferBindingType type;
  final bool hasDynamicOffset;
  final int minBindingSize;

  Pointer<bindings.WGPUBufferBindingLayout> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPUBufferBindingLayout>();
    ptr.ref.nextInChain = next?.toNative(allocator).cast() ?? nullptr;
    ptr.ref.typeAsInt = type.value;
    ptr.ref.hasDynamicOffset = (hasDynamicOffset ? 1 : 0);
    ptr.ref.minBindingSize = minBindingSize;

    return ptr;
  }

  factory BufferBindingLayout.fromNative(bindings.WGPUBufferBindingLayout v) {
    return BufferBindingLayout(
      next: ChainedStruct.fromNative(v.nextInChain),
      type: BufferBindingType.fromValue(v.typeAsInt),
      hasDynamicOffset: (v.hasDynamicOffset != 0),
      minBindingSize: v.minBindingSize,
    );
  }
}

class BufferHostMappedPointer extends ChainedStruct {
  const BufferHostMappedPointer({
    required this.pointer,
    required this.disposeCallback,
    super.next,
  });

  final Pointer<Void> pointer;
  final Callback? disposeCallback;

  @override
  Pointer<bindings.WGPUBufferHostMappedPointer> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPUBufferHostMappedPointer>();
    ptr.ref.chain.sTypeAsInt = sType.value;
    ptr.ref.chain.next = next?.toNative(allocator).cast() ?? nullptr;
    ptr.ref.pointer = pointer;
    if (disposeCallback != null) {
      ptr.ref.disposeCallback = _CallbackRegistry._nativeFunction;
      ptr.ref.userdata = _CallbackRegistry.register(disposeCallback!);
    }
    else {
      ptr.ref.disposeCallback = nullptr;
      ptr.ref.userdata = nullptr;
    }

    return ptr;
  }

  factory BufferHostMappedPointer.fromNative(bindings.WGPUBufferHostMappedPointer v) {
    return BufferHostMappedPointer(
      pointer: v.pointer,
      disposeCallback: null,
    );
  }

  @override
  SType get sType => .bufferHostMappedPointer;
}

class Color {
  const Color({
    this.r = 0,
    this.g = 0,
    this.b = 0,
    this.a = 0,
  });

  final double r;
  final double g;
  final double b;
  final double a;

  Pointer<bindings.WGPUColor> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPUColor>();
    ptr.ref.r = r;
    ptr.ref.g = g;
    ptr.ref.b = b;
    ptr.ref.a = a;

    return ptr;
  }

  factory Color.fromNative(bindings.WGPUColor v) {
    return Color(
      r: v.r,
      g: v.g,
      b: v.b,
      a: v.a,
    );
  }
}

class ColorSpaceDawn {
  const ColorSpaceDawn({
    this.next,
    this.primaries = ColorSpacePrimariesDawn.sRGB,
    this.transfer = ColorSpaceTransferDawn.identity,
    this.yCbCrRange = ColorSpaceYCbCrRangeDawn.identity,
    this.yCbCrMatrix = ColorSpaceYCbCrMatrixDawn.identity,
  });

  final ChainedStruct? next;
  final ColorSpacePrimariesDawn primaries;
  final ColorSpaceTransferDawn transfer;
  final ColorSpaceYCbCrRangeDawn yCbCrRange;
  final ColorSpaceYCbCrMatrixDawn yCbCrMatrix;

  Pointer<bindings.WGPUColorSpaceDawn> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPUColorSpaceDawn>();
    ptr.ref.nextInChain = next?.toNative(allocator).cast() ?? nullptr;
    ptr.ref.primariesAsInt = primaries.value;
    ptr.ref.transferAsInt = transfer.value;
    ptr.ref.yCbCrRangeAsInt = yCbCrRange.value;
    ptr.ref.yCbCrMatrixAsInt = yCbCrMatrix.value;

    return ptr;
  }

  factory ColorSpaceDawn.fromNative(bindings.WGPUColorSpaceDawn v) {
    return ColorSpaceDawn(
      next: ChainedStruct.fromNative(v.nextInChain),
      primaries: ColorSpacePrimariesDawn.fromValue(v.primariesAsInt),
      transfer: ColorSpaceTransferDawn.fromValue(v.transferAsInt),
      yCbCrRange: ColorSpaceYCbCrRangeDawn.fromValue(v.yCbCrRangeAsInt),
      yCbCrMatrix: ColorSpaceYCbCrMatrixDawn.fromValue(v.yCbCrMatrixAsInt),
    );
  }
}

class ColorTargetStateExpandResolveTextureDawn extends ChainedStruct {
  const ColorTargetStateExpandResolveTextureDawn({
    this.enabled = false,
    super.next,
  });

  final bool enabled;

  @override
  Pointer<bindings.WGPUColorTargetStateExpandResolveTextureDawn> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPUColorTargetStateExpandResolveTextureDawn>();
    ptr.ref.chain.sTypeAsInt = sType.value;
    ptr.ref.chain.next = next?.toNative(allocator).cast() ?? nullptr;
    ptr.ref.enabled = (enabled ? 1 : 0);

    return ptr;
  }

  factory ColorTargetStateExpandResolveTextureDawn.fromNative(bindings.WGPUColorTargetStateExpandResolveTextureDawn v) {
    return ColorTargetStateExpandResolveTextureDawn(
      enabled: (v.enabled != 0),
    );
  }

  @override
  SType get sType => .colorTargetStateExpandResolveTextureDawn;
}

class CommandBufferDescriptor {
  const CommandBufferDescriptor({
    this.next,
    this.label,
  });

  final ChainedStruct? next;
  final String? label;

  Pointer<bindings.WGPUCommandBufferDescriptor> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPUCommandBufferDescriptor>();
    ptr.ref.nextInChain = next?.toNative(allocator).cast() ?? nullptr;
    if (label != null) ptr.ref.label = StringView(data: label!, length: label!.length).toNative(allocator).ref;

    return ptr;
  }

  factory CommandBufferDescriptor.fromNative(bindings.WGPUCommandBufferDescriptor v) {
    return CommandBufferDescriptor(
      next: ChainedStruct.fromNative(v.nextInChain),
      label: StringView.fromNative(v.label).toDartString(),
    );
  }
}

class CompatibilityModeLimits extends ChainedStruct {
  const CompatibilityModeLimits({
    this.maxStorageBuffersInVertexStage = 0,
    this.maxStorageTexturesInVertexStage = 0,
    this.maxStorageBuffersInFragmentStage = 0,
    this.maxStorageTexturesInFragmentStage = 0,
    super.next,
  });

  final int maxStorageBuffersInVertexStage;
  final int maxStorageTexturesInVertexStage;
  final int maxStorageBuffersInFragmentStage;
  final int maxStorageTexturesInFragmentStage;

  @override
  Pointer<bindings.WGPUCompatibilityModeLimits> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPUCompatibilityModeLimits>();
    ptr.ref.chain.sTypeAsInt = sType.value;
    ptr.ref.chain.next = next?.toNative(allocator).cast() ?? nullptr;
    ptr.ref.maxStorageBuffersInVertexStage = maxStorageBuffersInVertexStage;
    ptr.ref.maxStorageTexturesInVertexStage = maxStorageTexturesInVertexStage;
    ptr.ref.maxStorageBuffersInFragmentStage = maxStorageBuffersInFragmentStage;
    ptr.ref.maxStorageTexturesInFragmentStage = maxStorageTexturesInFragmentStage;

    return ptr;
  }

  factory CompatibilityModeLimits.fromNative(bindings.WGPUCompatibilityModeLimits v) {
    return CompatibilityModeLimits(
      maxStorageBuffersInVertexStage: v.maxStorageBuffersInVertexStage,
      maxStorageTexturesInVertexStage: v.maxStorageTexturesInVertexStage,
      maxStorageBuffersInFragmentStage: v.maxStorageBuffersInFragmentStage,
      maxStorageTexturesInFragmentStage: v.maxStorageTexturesInFragmentStage,
    );
  }

  @override
  SType get sType => .compatibilityModeLimits;
}

class ConstantEntry {
  const ConstantEntry({
    this.next,
    this.key,
    this.value = 0,
  });

  final ChainedStruct? next;
  final String? key;
  final double value;

  Pointer<bindings.WGPUConstantEntry> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPUConstantEntry>();
    ptr.ref.nextInChain = next?.toNative(allocator).cast() ?? nullptr;
    if (key != null) ptr.ref.key = StringView(data: key!, length: key!.length).toNative(allocator).ref;
    ptr.ref.value = value;

    return ptr;
  }

  factory ConstantEntry.fromNative(bindings.WGPUConstantEntry v) {
    return ConstantEntry(
      next: ChainedStruct.fromNative(v.nextInChain),
      key: StringView.fromNative(v.key).toDartString(),
      value: v.value,
    );
  }
}

class CopyTextureForBrowserOptions {
  const CopyTextureForBrowserOptions({
    this.next,
    this.flipY = false,
    this.needsColorSpaceConversion = false,
    this.srcAlphaMode = AlphaMode.opaque,
    this.srcTransferFunctionParameters,
    this.conversionMatrix,
    this.dstTransferFunctionParameters,
    this.dstAlphaMode = AlphaMode.opaque,
    this.internalUsage = false,
  });

  final ChainedStruct? next;
  final bool flipY;
  final bool needsColorSpaceConversion;
  final AlphaMode srcAlphaMode;
  final Pointer<Float>? srcTransferFunctionParameters;
  final Pointer<Float>? conversionMatrix;
  final Pointer<Float>? dstTransferFunctionParameters;
  final AlphaMode dstAlphaMode;
  final bool internalUsage;

  Pointer<bindings.WGPUCopyTextureForBrowserOptions> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPUCopyTextureForBrowserOptions>();
    ptr.ref.nextInChain = next?.toNative(allocator).cast() ?? nullptr;
    ptr.ref.flipY = (flipY ? 1 : 0);
    ptr.ref.needsColorSpaceConversion = (needsColorSpaceConversion ? 1 : 0);
    ptr.ref.srcAlphaModeAsInt = srcAlphaMode.value;
    ptr.ref.srcTransferFunctionParameters = (srcTransferFunctionParameters ?? nullptr);
    ptr.ref.conversionMatrix = (conversionMatrix ?? nullptr);
    ptr.ref.dstTransferFunctionParameters = (dstTransferFunctionParameters ?? nullptr);
    ptr.ref.dstAlphaModeAsInt = dstAlphaMode.value;
    ptr.ref.internalUsage = (internalUsage ? 1 : 0);

    return ptr;
  }

  factory CopyTextureForBrowserOptions.fromNative(bindings.WGPUCopyTextureForBrowserOptions v) {
    return CopyTextureForBrowserOptions(
      next: ChainedStruct.fromNative(v.nextInChain),
      flipY: (v.flipY != 0),
      needsColorSpaceConversion: (v.needsColorSpaceConversion != 0),
      srcAlphaMode: AlphaMode.fromValue(v.srcAlphaModeAsInt),
      srcTransferFunctionParameters: (v.srcTransferFunctionParameters != nullptr ? v.srcTransferFunctionParameters : null),
      conversionMatrix: (v.conversionMatrix != nullptr ? v.conversionMatrix : null),
      dstTransferFunctionParameters: (v.dstTransferFunctionParameters != nullptr ? v.dstTransferFunctionParameters : null),
      dstAlphaMode: AlphaMode.fromValue(v.dstAlphaModeAsInt),
      internalUsage: (v.internalUsage != 0),
    );
  }
}

class DawnAdapterPropertiesPowerPreference extends ChainedStruct {
  const DawnAdapterPropertiesPowerPreference({
    this.powerPreference = PowerPreference.undefined,
    super.next,
  });

  final PowerPreference powerPreference;

  @override
  Pointer<bindings.WGPUDawnAdapterPropertiesPowerPreference> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPUDawnAdapterPropertiesPowerPreference>();
    ptr.ref.chain.sTypeAsInt = sType.value;
    ptr.ref.chain.next = next?.toNative(allocator).cast() ?? nullptr;
    ptr.ref.powerPreferenceAsInt = powerPreference.value;

    return ptr;
  }

  factory DawnAdapterPropertiesPowerPreference.fromNative(bindings.WGPUDawnAdapterPropertiesPowerPreference v) {
    return DawnAdapterPropertiesPowerPreference(
      powerPreference: PowerPreference.fromValue(v.powerPreferenceAsInt),
    );
  }

  @override
  SType get sType => .dawnAdapterPropertiesPowerPreference;
}

class DawnBufferDescriptorErrorInfoFromWireClient extends ChainedStruct {
  const DawnBufferDescriptorErrorInfoFromWireClient({
    this.outOfMemory = false,
    super.next,
  });

  final bool outOfMemory;

  @override
  Pointer<bindings.WGPUDawnBufferDescriptorErrorInfoFromWireClient> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPUDawnBufferDescriptorErrorInfoFromWireClient>();
    ptr.ref.chain.sTypeAsInt = sType.value;
    ptr.ref.chain.next = next?.toNative(allocator).cast() ?? nullptr;
    ptr.ref.outOfMemory = (outOfMemory ? 1 : 0);

    return ptr;
  }

  factory DawnBufferDescriptorErrorInfoFromWireClient.fromNative(bindings.WGPUDawnBufferDescriptorErrorInfoFromWireClient v) {
    return DawnBufferDescriptorErrorInfoFromWireClient(
      outOfMemory: (v.outOfMemory != 0),
    );
  }

  @override
  SType get sType => .dawnBufferDescriptorErrorInfoFromWireClient;
}

class DawnCompilationMessageUtf16 extends ChainedStruct {
  const DawnCompilationMessageUtf16({
    this.linePos = 0,
    this.offset = 0,
    this.length = 0,
    super.next,
  });

  final int linePos;
  final int offset;
  final int length;

  @override
  Pointer<bindings.WGPUDawnCompilationMessageUtf16> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPUDawnCompilationMessageUtf16>();
    ptr.ref.chain.sTypeAsInt = sType.value;
    ptr.ref.chain.next = next?.toNative(allocator).cast() ?? nullptr;
    ptr.ref.linePos = linePos;
    ptr.ref.offset = offset;
    ptr.ref.length = length;

    return ptr;
  }

  factory DawnCompilationMessageUtf16.fromNative(bindings.WGPUDawnCompilationMessageUtf16 v) {
    return DawnCompilationMessageUtf16(
      linePos: v.linePos,
      offset: v.offset,
      length: v.length,
    );
  }

  @override
  SType get sType => .dawnCompilationMessageUtf16;
}

class DawnConsumeAdapterDescriptor extends ChainedStruct {
  const DawnConsumeAdapterDescriptor({
    this.consumeAdapter = false,
    super.next,
  });

  final bool consumeAdapter;

  @override
  Pointer<bindings.WGPUDawnConsumeAdapterDescriptor> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPUDawnConsumeAdapterDescriptor>();
    ptr.ref.chain.sTypeAsInt = sType.value;
    ptr.ref.chain.next = next?.toNative(allocator).cast() ?? nullptr;
    ptr.ref.consumeAdapter = (consumeAdapter ? 1 : 0);

    return ptr;
  }

  factory DawnConsumeAdapterDescriptor.fromNative(bindings.WGPUDawnConsumeAdapterDescriptor v) {
    return DawnConsumeAdapterDescriptor(
      consumeAdapter: (v.consumeAdapter != 0),
    );
  }

  @override
  SType get sType => .dawnConsumeAdapterDescriptor;
}

class DawnDeviceAllocatorControl extends ChainedStruct {
  const DawnDeviceAllocatorControl({
    this.allocatorHeapBlockSize = 0,
    super.next,
  });

  final int allocatorHeapBlockSize;

  @override
  Pointer<bindings.WGPUDawnDeviceAllocatorControl> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPUDawnDeviceAllocatorControl>();
    ptr.ref.chain.sTypeAsInt = sType.value;
    ptr.ref.chain.next = next?.toNative(allocator).cast() ?? nullptr;
    ptr.ref.allocatorHeapBlockSize = allocatorHeapBlockSize;

    return ptr;
  }

  factory DawnDeviceAllocatorControl.fromNative(bindings.WGPUDawnDeviceAllocatorControl v) {
    return DawnDeviceAllocatorControl(
      allocatorHeapBlockSize: v.allocatorHeapBlockSize,
    );
  }

  @override
  SType get sType => .dawnDeviceAllocatorControl;
}

class DawnDrmFormatProperties {
  const DawnDrmFormatProperties({
    this.modifier = 0,
    this.modifierPlaneCount = 0,
  });

  final int modifier;
  final int modifierPlaneCount;

  Pointer<bindings.WGPUDawnDrmFormatProperties> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPUDawnDrmFormatProperties>();
    ptr.ref.modifier = modifier;
    ptr.ref.modifierPlaneCount = modifierPlaneCount;

    return ptr;
  }

  factory DawnDrmFormatProperties.fromNative(bindings.WGPUDawnDrmFormatProperties v) {
    return DawnDrmFormatProperties(
      modifier: v.modifier,
      modifierPlaneCount: v.modifierPlaneCount,
    );
  }
}

class DawnEncoderInternalUsageDescriptor extends ChainedStruct {
  const DawnEncoderInternalUsageDescriptor({
    this.useInternalUsages = false,
    super.next,
  });

  final bool useInternalUsages;

  @override
  Pointer<bindings.WGPUDawnEncoderInternalUsageDescriptor> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPUDawnEncoderInternalUsageDescriptor>();
    ptr.ref.chain.sTypeAsInt = sType.value;
    ptr.ref.chain.next = next?.toNative(allocator).cast() ?? nullptr;
    ptr.ref.useInternalUsages = (useInternalUsages ? 1 : 0);

    return ptr;
  }

  factory DawnEncoderInternalUsageDescriptor.fromNative(bindings.WGPUDawnEncoderInternalUsageDescriptor v) {
    return DawnEncoderInternalUsageDescriptor(
      useInternalUsages: (v.useInternalUsages != 0),
    );
  }

  @override
  SType get sType => .dawnEncoderInternalUsageDescriptor;
}

class DawnFakeBufferOOMForTesting extends ChainedStruct {
  const DawnFakeBufferOOMForTesting({
    this.fakeOOMAtWireClientMap = false,
    this.fakeOOMAtNativeMap = false,
    this.fakeOOMAtDevice = false,
    super.next,
  });

  final bool fakeOOMAtWireClientMap;
  final bool fakeOOMAtNativeMap;
  final bool fakeOOMAtDevice;

  @override
  Pointer<bindings.WGPUDawnFakeBufferOOMForTesting> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPUDawnFakeBufferOOMForTesting>();
    ptr.ref.chain.sTypeAsInt = sType.value;
    ptr.ref.chain.next = next?.toNative(allocator).cast() ?? nullptr;
    ptr.ref.fakeOOMAtWireClientMap = (fakeOOMAtWireClientMap ? 1 : 0);
    ptr.ref.fakeOOMAtNativeMap = (fakeOOMAtNativeMap ? 1 : 0);
    ptr.ref.fakeOOMAtDevice = (fakeOOMAtDevice ? 1 : 0);

    return ptr;
  }

  factory DawnFakeBufferOOMForTesting.fromNative(bindings.WGPUDawnFakeBufferOOMForTesting v) {
    return DawnFakeBufferOOMForTesting(
      fakeOOMAtWireClientMap: (v.fakeOOMAtWireClientMap != 0),
      fakeOOMAtNativeMap: (v.fakeOOMAtNativeMap != 0),
      fakeOOMAtDevice: (v.fakeOOMAtDevice != 0),
    );
  }

  @override
  SType get sType => .dawnFakeBufferOOMForTesting;
}

class DawnFakeDeviceInitializeErrorForTesting extends ChainedStruct {
  DawnFakeDeviceInitializeErrorForTesting();

  @override
  Pointer<bindings.WGPUDawnFakeDeviceInitializeErrorForTesting> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPUDawnFakeDeviceInitializeErrorForTesting>();
    ptr.ref.chain.sTypeAsInt = sType.value;
    ptr.ref.chain.next = next?.toNative(allocator).cast() ?? nullptr;

    return ptr;
  }

  factory DawnFakeDeviceInitializeErrorForTesting.fromNative(bindings.WGPUDawnFakeDeviceInitializeErrorForTesting v) {
    return DawnFakeDeviceInitializeErrorForTesting(
    );
  }

  @override
  SType get sType => .dawnFakeDeviceInitializeErrorForTesting;
}

class DawnHostMappedPointerLimits extends ChainedStruct {
  const DawnHostMappedPointerLimits({
    this.hostMappedPointerAlignment = 0,
    super.next,
  });

  final int hostMappedPointerAlignment;

  @override
  Pointer<bindings.WGPUDawnHostMappedPointerLimits> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPUDawnHostMappedPointerLimits>();
    ptr.ref.chain.sTypeAsInt = sType.value;
    ptr.ref.chain.next = next?.toNative(allocator).cast() ?? nullptr;
    ptr.ref.hostMappedPointerAlignment = hostMappedPointerAlignment;

    return ptr;
  }

  factory DawnHostMappedPointerLimits.fromNative(bindings.WGPUDawnHostMappedPointerLimits v) {
    return DawnHostMappedPointerLimits(
      hostMappedPointerAlignment: v.hostMappedPointerAlignment,
    );
  }

  @override
  SType get sType => .dawnHostMappedPointerLimits;
}

class DawnInjectedInvalidSType extends ChainedStruct {
  const DawnInjectedInvalidSType({
    this.invalidSType = SType.shaderSourceSPIRV,
    super.next,
  });

  final SType invalidSType;

  @override
  Pointer<bindings.WGPUDawnInjectedInvalidSType> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPUDawnInjectedInvalidSType>();
    ptr.ref.chain.sTypeAsInt = sType.value;
    ptr.ref.chain.next = next?.toNative(allocator).cast() ?? nullptr;
    ptr.ref.invalidSTypeAsInt = invalidSType.value;

    return ptr;
  }

  factory DawnInjectedInvalidSType.fromNative(bindings.WGPUDawnInjectedInvalidSType v) {
    return DawnInjectedInvalidSType(
      invalidSType: SType.fromValue(v.invalidSTypeAsInt),
    );
  }

  @override
  SType get sType => .dawnInjectedInvalidSType;
}

class DawnRenderPassSampleCount extends ChainedStruct {
  const DawnRenderPassSampleCount({
    this.sampleCount = 0,
    super.next,
  });

  final int sampleCount;

  @override
  Pointer<bindings.WGPUDawnRenderPassSampleCount> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPUDawnRenderPassSampleCount>();
    ptr.ref.chain.sTypeAsInt = sType.value;
    ptr.ref.chain.next = next?.toNative(allocator).cast() ?? nullptr;
    ptr.ref.sampleCount = sampleCount;

    return ptr;
  }

  factory DawnRenderPassSampleCount.fromNative(bindings.WGPUDawnRenderPassSampleCount v) {
    return DawnRenderPassSampleCount(
      sampleCount: v.sampleCount,
    );
  }

  @override
  SType get sType => .dawnRenderPassSampleCount;
}

class DawnShaderModuleSPIRVOptionsDescriptor extends ChainedStruct {
  const DawnShaderModuleSPIRVOptionsDescriptor({
    this.allowNonUniformDerivatives = false,
    super.next,
  });

  final bool allowNonUniformDerivatives;

  @override
  Pointer<bindings.WGPUDawnShaderModuleSPIRVOptionsDescriptor> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPUDawnShaderModuleSPIRVOptionsDescriptor>();
    ptr.ref.chain.sTypeAsInt = sType.value;
    ptr.ref.chain.next = next?.toNative(allocator).cast() ?? nullptr;
    ptr.ref.allowNonUniformDerivatives = (allowNonUniformDerivatives ? 1 : 0);

    return ptr;
  }

  factory DawnShaderModuleSPIRVOptionsDescriptor.fromNative(bindings.WGPUDawnShaderModuleSPIRVOptionsDescriptor v) {
    return DawnShaderModuleSPIRVOptionsDescriptor(
      allowNonUniformDerivatives: (v.allowNonUniformDerivatives != 0),
    );
  }

  @override
  SType get sType => .dawnShaderModuleSPIRVOptionsDescriptor;
}

class DawnTexelCopyBufferRowAlignmentLimits extends ChainedStruct {
  const DawnTexelCopyBufferRowAlignmentLimits({
    this.minTexelCopyBufferRowAlignment = 0,
    super.next,
  });

  final int minTexelCopyBufferRowAlignment;

  @override
  Pointer<bindings.WGPUDawnTexelCopyBufferRowAlignmentLimits> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPUDawnTexelCopyBufferRowAlignmentLimits>();
    ptr.ref.chain.sTypeAsInt = sType.value;
    ptr.ref.chain.next = next?.toNative(allocator).cast() ?? nullptr;
    ptr.ref.minTexelCopyBufferRowAlignment = minTexelCopyBufferRowAlignment;

    return ptr;
  }

  factory DawnTexelCopyBufferRowAlignmentLimits.fromNative(bindings.WGPUDawnTexelCopyBufferRowAlignmentLimits v) {
    return DawnTexelCopyBufferRowAlignmentLimits(
      minTexelCopyBufferRowAlignment: v.minTexelCopyBufferRowAlignment,
    );
  }

  @override
  SType get sType => .dawnTexelCopyBufferRowAlignmentLimits;
}

class DawnTextureInternalUsageDescriptor extends ChainedStruct {
  const DawnTextureInternalUsageDescriptor({
    this.internalUsage = const TextureUsage(0),
    super.next,
  });

  final TextureUsage internalUsage;

  @override
  Pointer<bindings.WGPUDawnTextureInternalUsageDescriptor> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPUDawnTextureInternalUsageDescriptor>();
    ptr.ref.chain.sTypeAsInt = sType.value;
    ptr.ref.chain.next = next?.toNative(allocator).cast() ?? nullptr;
    ptr.ref.internalUsage = internalUsage.value;

    return ptr;
  }

  factory DawnTextureInternalUsageDescriptor.fromNative(bindings.WGPUDawnTextureInternalUsageDescriptor v) {
    return DawnTextureInternalUsageDescriptor(
      internalUsage: TextureUsage(v.internalUsage),
    );
  }

  @override
  SType get sType => .dawnTextureInternalUsageDescriptor;
}

class DawnTogglesDescriptor extends ChainedStruct {
  const DawnTogglesDescriptor({
    this.enabledToggles = const [],
    this.disabledToggles = const [],
    super.next,
  });

  final List<String> enabledToggles;
  final List<String> disabledToggles;

  @override
  Pointer<bindings.WGPUDawnTogglesDescriptor> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPUDawnTogglesDescriptor>();
    ptr.ref.chain.sTypeAsInt = sType.value;
    ptr.ref.chain.next = next?.toNative(allocator).cast() ?? nullptr;
    if (enabledToggles.isNotEmpty) {
      final arrayPtr = allocator<Pointer<ffi.Utf8>>(enabledToggles.length);
      for (var i = 0; i < enabledToggles.length; i++) {
        arrayPtr[i] = enabledToggles[i].toNativeUtf8(allocator: allocator).cast();
      }
      ptr.ref.enabledToggles = arrayPtr.cast();
      ptr.ref.enabledToggleCount = enabledToggles.length;
    } else {
      ptr.ref.enabledToggles = nullptr;
      ptr.ref.enabledToggleCount = 0;
    }

    if (disabledToggles.isNotEmpty) {
      final arrayPtr = allocator<Pointer<ffi.Utf8>>(disabledToggles.length);
      for (var i = 0; i < disabledToggles.length; i++) {
        arrayPtr[i] = disabledToggles[i].toNativeUtf8(allocator: allocator).cast();
      }
      ptr.ref.disabledToggles = arrayPtr.cast();
      ptr.ref.disabledToggleCount = disabledToggles.length;
    } else {
      ptr.ref.disabledToggles = nullptr;
      ptr.ref.disabledToggleCount = 0;
    }

    return ptr;
  }

  factory DawnTogglesDescriptor.fromNative(bindings.WGPUDawnTogglesDescriptor v) {
    final _enabledToggles = <String>[];
    if (v.enabledToggles != nullptr && v.enabledToggleCount > 0) {
      for (var i = 0; i < v.enabledToggleCount; i++) {
        _enabledToggles.add(v.enabledToggles[i].cast<ffi.Utf8>().toDartString());
      }
    }
    final _disabledToggles = <String>[];
    if (v.disabledToggles != nullptr && v.disabledToggleCount > 0) {
      for (var i = 0; i < v.disabledToggleCount; i++) {
        _disabledToggles.add(v.disabledToggles[i].cast<ffi.Utf8>().toDartString());
      }
    }
    return DawnTogglesDescriptor(
      enabledToggles: _enabledToggles,
      disabledToggles: _disabledToggles,
    );
  }

  @override
  SType get sType => .dawnTogglesDescriptor;
}

class DawnWGSLBlocklist extends ChainedStruct {
  const DawnWGSLBlocklist({
    this.blocklistedFeatures = const [],
    super.next,
  });

  final List<String> blocklistedFeatures;

  @override
  Pointer<bindings.WGPUDawnWGSLBlocklist> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPUDawnWGSLBlocklist>();
    ptr.ref.chain.sTypeAsInt = sType.value;
    ptr.ref.chain.next = next?.toNative(allocator).cast() ?? nullptr;
    if (blocklistedFeatures.isNotEmpty) {
      final arrayPtr = allocator<Pointer<ffi.Utf8>>(blocklistedFeatures.length);
      for (var i = 0; i < blocklistedFeatures.length; i++) {
        arrayPtr[i] = blocklistedFeatures[i].toNativeUtf8(allocator: allocator).cast();
      }
      ptr.ref.blocklistedFeatures = arrayPtr.cast();
      ptr.ref.blocklistedFeatureCount = blocklistedFeatures.length;
    } else {
      ptr.ref.blocklistedFeatures = nullptr;
      ptr.ref.blocklistedFeatureCount = 0;
    }

    return ptr;
  }

  factory DawnWGSLBlocklist.fromNative(bindings.WGPUDawnWGSLBlocklist v) {
    final _blocklistedFeatures = <String>[];
    if (v.blocklistedFeatures != nullptr && v.blocklistedFeatureCount > 0) {
      for (var i = 0; i < v.blocklistedFeatureCount; i++) {
        _blocklistedFeatures.add(v.blocklistedFeatures[i].cast<ffi.Utf8>().toDartString());
      }
    }
    return DawnWGSLBlocklist(
      blocklistedFeatures: _blocklistedFeatures,
    );
  }

  @override
  SType get sType => .dawnWGSLBlocklist;
}

class DawnWireWGSLControl extends ChainedStruct {
  const DawnWireWGSLControl({
    this.enableExperimental = false,
    this.enableUnsafe = false,
    this.enableTesting = false,
    super.next,
  });

  final bool enableExperimental;
  final bool enableUnsafe;
  final bool enableTesting;

  @override
  Pointer<bindings.WGPUDawnWireWGSLControl> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPUDawnWireWGSLControl>();
    ptr.ref.chain.sTypeAsInt = sType.value;
    ptr.ref.chain.next = next?.toNative(allocator).cast() ?? nullptr;
    ptr.ref.enableExperimental = (enableExperimental ? 1 : 0);
    ptr.ref.enableUnsafe = (enableUnsafe ? 1 : 0);
    ptr.ref.enableTesting = (enableTesting ? 1 : 0);

    return ptr;
  }

  factory DawnWireWGSLControl.fromNative(bindings.WGPUDawnWireWGSLControl v) {
    return DawnWireWGSLControl(
      enableExperimental: (v.enableExperimental != 0),
      enableUnsafe: (v.enableUnsafe != 0),
      enableTesting: (v.enableTesting != 0),
    );
  }

  @override
  SType get sType => .dawnWireWGSLControl;
}

class EmscriptenSurfaceSourceCanvasHTMLSelector extends ChainedStruct {
  const EmscriptenSurfaceSourceCanvasHTMLSelector({
    this.selector,
    super.next,
  });

  final String? selector;

  @override
  Pointer<bindings.WGPUEmscriptenSurfaceSourceCanvasHTMLSelector> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPUEmscriptenSurfaceSourceCanvasHTMLSelector>();
    ptr.ref.chain.sTypeAsInt = sType.value;
    ptr.ref.chain.next = next?.toNative(allocator).cast() ?? nullptr;
    if (selector != null) ptr.ref.selector = StringView(data: selector!, length: selector!.length).toNative(allocator).ref;

    return ptr;
  }

  factory EmscriptenSurfaceSourceCanvasHTMLSelector.fromNative(bindings.WGPUEmscriptenSurfaceSourceCanvasHTMLSelector v) {
    return EmscriptenSurfaceSourceCanvasHTMLSelector(
      selector: StringView.fromNative(v.selector).toDartString(),
    );
  }

  @override
  SType get sType => .emscriptenSurfaceSourceCanvasHTMLSelector;
}

class Extent2D {
  const Extent2D({
    this.width = 0,
    this.height = 0,
  });

  final int width;
  final int height;

  Pointer<bindings.WGPUExtent2D> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPUExtent2D>();
    ptr.ref.width = width;
    ptr.ref.height = height;

    return ptr;
  }

  factory Extent2D.fromNative(bindings.WGPUExtent2D v) {
    return Extent2D(
      width: v.width,
      height: v.height,
    );
  }
}

class Extent3D {
  const Extent3D({
    this.width = 0,
    this.height = 0,
    this.depthOrArrayLayers = 0,
  });

  final int width;
  final int height;
  final int depthOrArrayLayers;

  Pointer<bindings.WGPUExtent3D> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPUExtent3D>();
    ptr.ref.width = width;
    ptr.ref.height = height;
    ptr.ref.depthOrArrayLayers = depthOrArrayLayers;

    return ptr;
  }

  factory Extent3D.fromNative(bindings.WGPUExtent3D v) {
    return Extent3D(
      width: v.width,
      height: v.height,
      depthOrArrayLayers: v.depthOrArrayLayers,
    );
  }
}

class ExternalTextureBindingEntry extends ChainedStruct {
  const ExternalTextureBindingEntry({
    required this.externalTexture,
    super.next,
  });

  final ExternalTexture externalTexture;

  @override
  Pointer<bindings.WGPUExternalTextureBindingEntry> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPUExternalTextureBindingEntry>();
    ptr.ref.chain.sTypeAsInt = sType.value;
    ptr.ref.chain.next = next?.toNative(allocator).cast() ?? nullptr;
    ptr.ref.externalTexture = externalTexture._ptr;

    return ptr;
  }

  factory ExternalTextureBindingEntry.fromNative(bindings.WGPUExternalTextureBindingEntry v) {
    return ExternalTextureBindingEntry(
      externalTexture: ExternalTexture._(v.externalTexture),
    );
  }

  @override
  SType get sType => .externalTextureBindingEntry;
}

class ExternalTextureBindingLayout extends ChainedStruct {
  ExternalTextureBindingLayout();

  @override
  Pointer<bindings.WGPUExternalTextureBindingLayout> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPUExternalTextureBindingLayout>();
    ptr.ref.chain.sTypeAsInt = sType.value;
    ptr.ref.chain.next = next?.toNative(allocator).cast() ?? nullptr;

    return ptr;
  }

  factory ExternalTextureBindingLayout.fromNative(bindings.WGPUExternalTextureBindingLayout v) {
    return ExternalTextureBindingLayout(
    );
  }

  @override
  SType get sType => .externalTextureBindingLayout;
}

class WGPUFuture {
  const WGPUFuture({
    this.id = 0,
  });

  final int id;

  Pointer<bindings.WGPUFuture> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPUFuture>();
    ptr.ref.id = id;

    return ptr;
  }

  factory WGPUFuture.fromNative(bindings.WGPUFuture v) {
    return WGPUFuture(
      id: v.id,
    );
  }
}

class InstanceLimits {
  const InstanceLimits({
    this.next,
    this.timedWaitAnyMaxCount = 0,
  });

  final ChainedStruct? next;
  final int timedWaitAnyMaxCount;

  Pointer<bindings.WGPUInstanceLimits> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPUInstanceLimits>();
    ptr.ref.nextInChain = next?.toNative(allocator).cast() ?? nullptr;
    ptr.ref.timedWaitAnyMaxCount = timedWaitAnyMaxCount;

    return ptr;
  }

  factory InstanceLimits.fromNative(bindings.WGPUInstanceLimits v) {
    return InstanceLimits(
      next: ChainedStruct.fromNative(v.nextInChain),
      timedWaitAnyMaxCount: v.timedWaitAnyMaxCount,
    );
  }
}

class INTERNALHAVEEMDAWNWEBGPUHEADER {
  const INTERNALHAVEEMDAWNWEBGPUHEADER({
    this.unused = false,
  });

  final bool unused;

  Pointer<bindings.WGPUINTERNAL_HAVE_EMDAWNWEBGPU_HEADER> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPUINTERNAL_HAVE_EMDAWNWEBGPU_HEADER>();
    ptr.ref.unused = (unused ? 1 : 0);

    return ptr;
  }

  factory INTERNALHAVEEMDAWNWEBGPUHEADER.fromNative(bindings.WGPUINTERNAL_HAVE_EMDAWNWEBGPU_HEADER v) {
    return INTERNALHAVEEMDAWNWEBGPUHEADER(
      unused: (v.unused != 0),
    );
  }
}

class MemoryHeapInfo {
  const MemoryHeapInfo({
    this.properties = const HeapProperty(0),
    this.size = 0,
  });

  final HeapProperty properties;
  final int size;

  Pointer<bindings.WGPUMemoryHeapInfo> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPUMemoryHeapInfo>();
    ptr.ref.properties = properties.value;
    ptr.ref.size = size;

    return ptr;
  }

  factory MemoryHeapInfo.fromNative(bindings.WGPUMemoryHeapInfo v) {
    return MemoryHeapInfo(
      properties: HeapProperty(v.properties),
      size: v.size,
    );
  }
}

class MultisampleState {
  const MultisampleState({
    this.next,
    this.count = 0,
    this.mask = 0,
    this.alphaToCoverageEnabled = false,
  });

  final ChainedStruct? next;
  final int count;
  final int mask;
  final bool alphaToCoverageEnabled;

  Pointer<bindings.WGPUMultisampleState> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPUMultisampleState>();
    ptr.ref.nextInChain = next?.toNative(allocator).cast() ?? nullptr;
    ptr.ref.count = count;
    ptr.ref.mask = mask;
    ptr.ref.alphaToCoverageEnabled = (alphaToCoverageEnabled ? 1 : 0);

    return ptr;
  }

  factory MultisampleState.fromNative(bindings.WGPUMultisampleState v) {
    return MultisampleState(
      next: ChainedStruct.fromNative(v.nextInChain),
      count: v.count,
      mask: v.mask,
      alphaToCoverageEnabled: (v.alphaToCoverageEnabled != 0),
    );
  }
}

class Origin2D {
  const Origin2D({
    this.x = 0,
    this.y = 0,
  });

  final int x;
  final int y;

  Pointer<bindings.WGPUOrigin2D> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPUOrigin2D>();
    ptr.ref.x = x;
    ptr.ref.y = y;

    return ptr;
  }

  factory Origin2D.fromNative(bindings.WGPUOrigin2D v) {
    return Origin2D(
      x: v.x,
      y: v.y,
    );
  }
}

class Origin3D {
  const Origin3D({
    this.x = 0,
    this.y = 0,
    this.z = 0,
  });

  final int x;
  final int y;
  final int z;

  Pointer<bindings.WGPUOrigin3D> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPUOrigin3D>();
    ptr.ref.x = x;
    ptr.ref.y = y;
    ptr.ref.z = z;

    return ptr;
  }

  factory Origin3D.fromNative(bindings.WGPUOrigin3D v) {
    return Origin3D(
      x: v.x,
      y: v.y,
      z: v.z,
    );
  }
}

class PassTimestampWrites {
  const PassTimestampWrites({
    this.next,
    required this.querySet,
    this.beginningOfPassWriteIndex = 0,
    this.endOfPassWriteIndex = 0,
  });

  final ChainedStruct? next;
  final QuerySet querySet;
  final int beginningOfPassWriteIndex;
  final int endOfPassWriteIndex;

  Pointer<bindings.WGPUPassTimestampWrites> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPUPassTimestampWrites>();
    ptr.ref.nextInChain = next?.toNative(allocator).cast() ?? nullptr;
    ptr.ref.querySet = querySet._ptr;
    ptr.ref.beginningOfPassWriteIndex = beginningOfPassWriteIndex;
    ptr.ref.endOfPassWriteIndex = endOfPassWriteIndex;

    return ptr;
  }

  factory PassTimestampWrites.fromNative(bindings.WGPUPassTimestampWrites v) {
    return PassTimestampWrites(
      next: ChainedStruct.fromNative(v.nextInChain),
      querySet: QuerySet._(v.querySet),
      beginningOfPassWriteIndex: v.beginningOfPassWriteIndex,
      endOfPassWriteIndex: v.endOfPassWriteIndex,
    );
  }
}

class PipelineLayoutResourceTable extends ChainedStruct {
  const PipelineLayoutResourceTable({
    this.usesResourceTable = false,
    super.next,
  });

  final bool usesResourceTable;

  @override
  Pointer<bindings.WGPUPipelineLayoutResourceTable> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPUPipelineLayoutResourceTable>();
    ptr.ref.chain.sTypeAsInt = sType.value;
    ptr.ref.chain.next = next?.toNative(allocator).cast() ?? nullptr;
    ptr.ref.usesResourceTable = (usesResourceTable ? 1 : 0);

    return ptr;
  }

  factory PipelineLayoutResourceTable.fromNative(bindings.WGPUPipelineLayoutResourceTable v) {
    return PipelineLayoutResourceTable(
      usesResourceTable: (v.usesResourceTable != 0),
    );
  }

  @override
  SType get sType => .pipelineLayoutResourceTable;
}

class PipelineLayoutStorageAttachment {
  const PipelineLayoutStorageAttachment({
    this.next,
    this.offset = 0,
    this.format = TextureFormat.undefined,
  });

  final ChainedStruct? next;
  final int offset;
  final TextureFormat format;

  Pointer<bindings.WGPUPipelineLayoutStorageAttachment> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPUPipelineLayoutStorageAttachment>();
    ptr.ref.nextInChain = next?.toNative(allocator).cast() ?? nullptr;
    ptr.ref.offset = offset;
    ptr.ref.formatAsInt = format.value;

    return ptr;
  }

  factory PipelineLayoutStorageAttachment.fromNative(bindings.WGPUPipelineLayoutStorageAttachment v) {
    return PipelineLayoutStorageAttachment(
      next: ChainedStruct.fromNative(v.nextInChain),
      offset: v.offset,
      format: TextureFormat.fromValue(v.formatAsInt),
    );
  }
}

class PrimitiveState {
  const PrimitiveState({
    this.next,
    this.topology = PrimitiveTopology.undefined,
    this.stripIndexFormat = IndexFormat.undefined,
    this.frontFace = FrontFace.undefined,
    this.cullMode = CullMode.undefined,
    this.unclippedDepth = false,
  });

  final ChainedStruct? next;
  final PrimitiveTopology topology;
  final IndexFormat stripIndexFormat;
  final FrontFace frontFace;
  final CullMode cullMode;
  final bool unclippedDepth;

  Pointer<bindings.WGPUPrimitiveState> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPUPrimitiveState>();
    ptr.ref.nextInChain = next?.toNative(allocator).cast() ?? nullptr;
    ptr.ref.topologyAsInt = topology.value;
    ptr.ref.stripIndexFormatAsInt = stripIndexFormat.value;
    ptr.ref.frontFaceAsInt = frontFace.value;
    ptr.ref.cullModeAsInt = cullMode.value;
    ptr.ref.unclippedDepth = (unclippedDepth ? 1 : 0);

    return ptr;
  }

  factory PrimitiveState.fromNative(bindings.WGPUPrimitiveState v) {
    return PrimitiveState(
      next: ChainedStruct.fromNative(v.nextInChain),
      topology: PrimitiveTopology.fromValue(v.topologyAsInt),
      stripIndexFormat: IndexFormat.fromValue(v.stripIndexFormatAsInt),
      frontFace: FrontFace.fromValue(v.frontFaceAsInt),
      cullMode: CullMode.fromValue(v.cullModeAsInt),
      unclippedDepth: (v.unclippedDepth != 0),
    );
  }
}

class QuerySetDescriptor {
  const QuerySetDescriptor({
    this.next,
    this.label,
    this.type = QueryType.occlusion,
    this.count = 0,
  });

  final ChainedStruct? next;
  final String? label;
  final QueryType type;
  final int count;

  Pointer<bindings.WGPUQuerySetDescriptor> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPUQuerySetDescriptor>();
    ptr.ref.nextInChain = next?.toNative(allocator).cast() ?? nullptr;
    if (label != null) ptr.ref.label = StringView(data: label!, length: label!.length).toNative(allocator).ref;
    ptr.ref.typeAsInt = type.value;
    ptr.ref.count = count;

    return ptr;
  }

  factory QuerySetDescriptor.fromNative(bindings.WGPUQuerySetDescriptor v) {
    return QuerySetDescriptor(
      next: ChainedStruct.fromNative(v.nextInChain),
      label: StringView.fromNative(v.label).toDartString(),
      type: QueryType.fromValue(v.typeAsInt),
      count: v.count,
    );
  }
}

class QueueDescriptor {
  const QueueDescriptor({
    this.next,
    this.label,
  });

  final ChainedStruct? next;
  final String? label;

  Pointer<bindings.WGPUQueueDescriptor> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPUQueueDescriptor>();
    ptr.ref.nextInChain = next?.toNative(allocator).cast() ?? nullptr;
    if (label != null) ptr.ref.label = StringView(data: label!, length: label!.length).toNative(allocator).ref;

    return ptr;
  }

  factory QueueDescriptor.fromNative(bindings.WGPUQueueDescriptor v) {
    return QueueDescriptor(
      next: ChainedStruct.fromNative(v.nextInChain),
      label: StringView.fromNative(v.label).toDartString(),
    );
  }
}

class RenderBundleDescriptor {
  const RenderBundleDescriptor({
    this.next,
    this.label,
  });

  final ChainedStruct? next;
  final String? label;

  Pointer<bindings.WGPURenderBundleDescriptor> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPURenderBundleDescriptor>();
    ptr.ref.nextInChain = next?.toNative(allocator).cast() ?? nullptr;
    if (label != null) ptr.ref.label = StringView(data: label!, length: label!.length).toNative(allocator).ref;

    return ptr;
  }

  factory RenderBundleDescriptor.fromNative(bindings.WGPURenderBundleDescriptor v) {
    return RenderBundleDescriptor(
      next: ChainedStruct.fromNative(v.nextInChain),
      label: StringView.fromNative(v.label).toDartString(),
    );
  }
}

class RenderBundleEncoderDescriptor {
  const RenderBundleEncoderDescriptor({
    this.next,
    this.label,
    this.colorFormats = const [],
    this.depthStencilFormat = TextureFormat.undefined,
    this.sampleCount = 0,
    this.depthReadOnly = false,
    this.stencilReadOnly = false,
  });

  final ChainedStruct? next;
  final String? label;
  final List<TextureFormat> colorFormats;
  final TextureFormat depthStencilFormat;
  final int sampleCount;
  final bool depthReadOnly;
  final bool stencilReadOnly;

  Pointer<bindings.WGPURenderBundleEncoderDescriptor> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPURenderBundleEncoderDescriptor>();
    ptr.ref.nextInChain = next?.toNative(allocator).cast() ?? nullptr;
    if (label != null) ptr.ref.label = StringView(data: label!, length: label!.length).toNative(allocator).ref;
    if (colorFormats.isNotEmpty) {
      final arrayPtr = allocator<UnsignedInt>(colorFormats.length);
      for (var i = 0; i < colorFormats.length; i++) {
        arrayPtr[i] = colorFormats[i].value;
      }
      ptr.ref.colorFormats = arrayPtr;
      ptr.ref.colorFormatCount = colorFormats.length;
    } else {
      ptr.ref.colorFormats = nullptr;
      ptr.ref.colorFormatCount = 0;
    }

    ptr.ref.depthStencilFormatAsInt = depthStencilFormat.value;
    ptr.ref.sampleCount = sampleCount;
    ptr.ref.depthReadOnly = (depthReadOnly ? 1 : 0);
    ptr.ref.stencilReadOnly = (stencilReadOnly ? 1 : 0);

    return ptr;
  }

  factory RenderBundleEncoderDescriptor.fromNative(bindings.WGPURenderBundleEncoderDescriptor v) {
    final _colorFormats = <TextureFormat>[];
    if (v.colorFormats != nullptr && v.colorFormatCount > 0) {
      for (var i = 0; i < v.colorFormatCount; i++) {
        _colorFormats.add(TextureFormat.fromValue(v.colorFormats[i]));
      }
    }
    return RenderBundleEncoderDescriptor(
      next: ChainedStruct.fromNative(v.nextInChain),
      label: StringView.fromNative(v.label).toDartString(),
      colorFormats: _colorFormats,
      depthStencilFormat: TextureFormat.fromValue(v.depthStencilFormatAsInt),
      sampleCount: v.sampleCount,
      depthReadOnly: (v.depthReadOnly != 0),
      stencilReadOnly: (v.stencilReadOnly != 0),
    );
  }
}

class RenderPassDepthStencilAttachment {
  const RenderPassDepthStencilAttachment({
    this.next,
    required this.view,
    this.depthLoadOp = LoadOp.undefined,
    this.depthStoreOp = StoreOp.undefined,
    this.depthClearValue = bindings.WGPU_DEPTH_CLEAR_VALUE_UNDEFINED,
    this.depthReadOnly = false,
    this.stencilLoadOp = LoadOp.undefined,
    this.stencilStoreOp = StoreOp.undefined,
    this.stencilClearValue = 0,
    this.stencilReadOnly = false,
  });

  final ChainedStruct? next;
  final TextureView view;
  final LoadOp depthLoadOp;
  final StoreOp depthStoreOp;
  final double depthClearValue;
  final bool depthReadOnly;
  final LoadOp stencilLoadOp;
  final StoreOp stencilStoreOp;
  final int stencilClearValue;
  final bool stencilReadOnly;

  Pointer<bindings.WGPURenderPassDepthStencilAttachment> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPURenderPassDepthStencilAttachment>();
    ptr.ref.nextInChain = next?.toNative(allocator).cast() ?? nullptr;
    ptr.ref.view = view._ptr;
    ptr.ref.depthLoadOpAsInt = depthLoadOp.value;
    ptr.ref.depthStoreOpAsInt = depthStoreOp.value;
    ptr.ref.depthClearValue = depthClearValue;
    ptr.ref.depthReadOnly = (depthReadOnly ? 1 : 0);
    ptr.ref.stencilLoadOpAsInt = stencilLoadOp.value;
    ptr.ref.stencilStoreOpAsInt = stencilStoreOp.value;
    ptr.ref.stencilClearValue = stencilClearValue;
    ptr.ref.stencilReadOnly = (stencilReadOnly ? 1 : 0);

    return ptr;
  }

  factory RenderPassDepthStencilAttachment.fromNative(bindings.WGPURenderPassDepthStencilAttachment v) {
    return RenderPassDepthStencilAttachment(
      next: ChainedStruct.fromNative(v.nextInChain),
      view: TextureView._(v.view),
      depthLoadOp: LoadOp.fromValue(v.depthLoadOpAsInt),
      depthStoreOp: StoreOp.fromValue(v.depthStoreOpAsInt),
      depthClearValue: v.depthClearValue,
      depthReadOnly: (v.depthReadOnly != 0),
      stencilLoadOp: LoadOp.fromValue(v.stencilLoadOpAsInt),
      stencilStoreOp: StoreOp.fromValue(v.stencilStoreOpAsInt),
      stencilClearValue: v.stencilClearValue,
      stencilReadOnly: (v.stencilReadOnly != 0),
    );
  }
}

class RenderPassDescriptorResolveRect extends ChainedStruct {
  const RenderPassDescriptorResolveRect({
    this.colorOffsetX = 0,
    this.colorOffsetY = 0,
    this.resolveOffsetX = 0,
    this.resolveOffsetY = 0,
    this.width = 0,
    this.height = 0,
    super.next,
  });

  final int colorOffsetX;
  final int colorOffsetY;
  final int resolveOffsetX;
  final int resolveOffsetY;
  final int width;
  final int height;

  @override
  Pointer<bindings.WGPURenderPassDescriptorResolveRect> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPURenderPassDescriptorResolveRect>();
    ptr.ref.chain.sTypeAsInt = sType.value;
    ptr.ref.chain.next = next?.toNative(allocator).cast() ?? nullptr;
    ptr.ref.colorOffsetX = colorOffsetX;
    ptr.ref.colorOffsetY = colorOffsetY;
    ptr.ref.resolveOffsetX = resolveOffsetX;
    ptr.ref.resolveOffsetY = resolveOffsetY;
    ptr.ref.width = width;
    ptr.ref.height = height;

    return ptr;
  }

  factory RenderPassDescriptorResolveRect.fromNative(bindings.WGPURenderPassDescriptorResolveRect v) {
    return RenderPassDescriptorResolveRect(
      colorOffsetX: v.colorOffsetX,
      colorOffsetY: v.colorOffsetY,
      resolveOffsetX: v.resolveOffsetX,
      resolveOffsetY: v.resolveOffsetY,
      width: v.width,
      height: v.height,
    );
  }

  @override
  SType get sType => .renderPassDescriptorResolveRect;
}

class RenderPassMaxDrawCount extends ChainedStruct {
  const RenderPassMaxDrawCount({
    this.maxDrawCount = 0,
    super.next,
  });

  final int maxDrawCount;

  @override
  Pointer<bindings.WGPURenderPassMaxDrawCount> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPURenderPassMaxDrawCount>();
    ptr.ref.chain.sTypeAsInt = sType.value;
    ptr.ref.chain.next = next?.toNative(allocator).cast() ?? nullptr;
    ptr.ref.maxDrawCount = maxDrawCount;

    return ptr;
  }

  factory RenderPassMaxDrawCount.fromNative(bindings.WGPURenderPassMaxDrawCount v) {
    return RenderPassMaxDrawCount(
      maxDrawCount: v.maxDrawCount,
    );
  }

  @override
  SType get sType => .renderPassMaxDrawCount;
}

class RequestAdapterWebGPUBackendOptions extends ChainedStruct {
  RequestAdapterWebGPUBackendOptions();

  @override
  Pointer<bindings.WGPURequestAdapterWebGPUBackendOptions> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPURequestAdapterWebGPUBackendOptions>();
    ptr.ref.chain.sTypeAsInt = sType.value;
    ptr.ref.chain.next = next?.toNative(allocator).cast() ?? nullptr;

    return ptr;
  }

  factory RequestAdapterWebGPUBackendOptions.fromNative(bindings.WGPURequestAdapterWebGPUBackendOptions v) {
    return RequestAdapterWebGPUBackendOptions(
    );
  }

  @override
  SType get sType => .requestAdapterWebGPUBackendOptions;
}

class RequestAdapterWebXROptions extends ChainedStruct {
  const RequestAdapterWebXROptions({
    this.xrCompatible = false,
    super.next,
  });

  final bool xrCompatible;

  @override
  Pointer<bindings.WGPURequestAdapterWebXROptions> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPURequestAdapterWebXROptions>();
    ptr.ref.chain.sTypeAsInt = sType.value;
    ptr.ref.chain.next = next?.toNative(allocator).cast() ?? nullptr;
    ptr.ref.xrCompatible = (xrCompatible ? 1 : 0);

    return ptr;
  }

  factory RequestAdapterWebXROptions.fromNative(bindings.WGPURequestAdapterWebXROptions v) {
    return RequestAdapterWebXROptions(
      xrCompatible: (v.xrCompatible != 0),
    );
  }

  @override
  SType get sType => .requestAdapterWebXROptions;
}

class ResourceTableDescriptor {
  const ResourceTableDescriptor({
    this.next,
    this.label,
    this.size = 0,
  });

  final ChainedStruct? next;
  final String? label;
  final int size;

  Pointer<bindings.WGPUResourceTableDescriptor> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPUResourceTableDescriptor>();
    ptr.ref.nextInChain = next?.toNative(allocator).cast() ?? nullptr;
    if (label != null) ptr.ref.label = StringView(data: label!, length: label!.length).toNative(allocator).ref;
    ptr.ref.size = size;

    return ptr;
  }

  factory ResourceTableDescriptor.fromNative(bindings.WGPUResourceTableDescriptor v) {
    return ResourceTableDescriptor(
      next: ChainedStruct.fromNative(v.nextInChain),
      label: StringView.fromNative(v.label).toDartString(),
      size: v.size,
    );
  }
}

class SamplerBindingLayout {
  const SamplerBindingLayout({
    this.next,
    this.type = SamplerBindingType.bindingNotUsed,
  });

  final ChainedStruct? next;
  final SamplerBindingType type;

  Pointer<bindings.WGPUSamplerBindingLayout> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPUSamplerBindingLayout>();
    ptr.ref.nextInChain = next?.toNative(allocator).cast() ?? nullptr;
    ptr.ref.typeAsInt = type.value;

    return ptr;
  }

  factory SamplerBindingLayout.fromNative(bindings.WGPUSamplerBindingLayout v) {
    return SamplerBindingLayout(
      next: ChainedStruct.fromNative(v.nextInChain),
      type: SamplerBindingType.fromValue(v.typeAsInt),
    );
  }
}

class ShaderModuleCompilationOptions extends ChainedStruct {
  const ShaderModuleCompilationOptions({
    this.strictMath = false,
    super.next,
  });

  final bool strictMath;

  @override
  Pointer<bindings.WGPUShaderModuleCompilationOptions> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPUShaderModuleCompilationOptions>();
    ptr.ref.chain.sTypeAsInt = sType.value;
    ptr.ref.chain.next = next?.toNative(allocator).cast() ?? nullptr;
    ptr.ref.strictMath = (strictMath ? 1 : 0);

    return ptr;
  }

  factory ShaderModuleCompilationOptions.fromNative(bindings.WGPUShaderModuleCompilationOptions v) {
    return ShaderModuleCompilationOptions(
      strictMath: (v.strictMath != 0),
    );
  }

  @override
  SType get sType => .shaderModuleCompilationOptions;
}

class ShaderSourceSPIRV extends ChainedStruct {
  const ShaderSourceSPIRV({
    this.codeSize = 0,
    required this.code,
    super.next,
  });

  final int codeSize;
  final Pointer<Uint32> code;

  @override
  Pointer<bindings.WGPUShaderSourceSPIRV> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPUShaderSourceSPIRV>();
    ptr.ref.chain.sTypeAsInt = sType.value;
    ptr.ref.chain.next = next?.toNative(allocator).cast() ?? nullptr;
    ptr.ref.codeSize = codeSize;
    ptr.ref.code = code;

    return ptr;
  }

  factory ShaderSourceSPIRV.fromNative(bindings.WGPUShaderSourceSPIRV v) {
    return ShaderSourceSPIRV(
      codeSize: v.codeSize,
      code: v.code,
    );
  }

  @override
  SType get sType => .shaderSourceSPIRV;
}

class ShaderSourceWGSL extends ChainedStruct {
  const ShaderSourceWGSL({
    this.code,
    super.next,
  });

  final String? code;

  @override
  Pointer<bindings.WGPUShaderSourceWGSL> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPUShaderSourceWGSL>();
    ptr.ref.chain.sTypeAsInt = sType.value;
    ptr.ref.chain.next = next?.toNative(allocator).cast() ?? nullptr;
    if (code != null) ptr.ref.code = StringView(data: code!, length: code!.length).toNative(allocator).ref;

    return ptr;
  }

  factory ShaderSourceWGSL.fromNative(bindings.WGPUShaderSourceWGSL v) {
    return ShaderSourceWGSL(
      code: StringView.fromNative(v.code).toDartString(),
    );
  }

  @override
  SType get sType => .shaderSourceWGSL;
}

class SharedBufferMemoryBeginAccessDescriptor {
  const SharedBufferMemoryBeginAccessDescriptor({
    this.next,
    this.initialized = false,
    this.fences = const [],
    required this.signaledValues,
  });

  final ChainedStruct? next;
  final bool initialized;
  final List<SharedFence> fences;
  final Pointer<Uint64> signaledValues;

  Pointer<bindings.WGPUSharedBufferMemoryBeginAccessDescriptor> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPUSharedBufferMemoryBeginAccessDescriptor>();
    ptr.ref.nextInChain = next?.toNative(allocator).cast() ?? nullptr;
    ptr.ref.initialized = (initialized ? 1 : 0);
    if (fences.isNotEmpty) {
      final arrayPtr = allocator<Pointer<bindings.WGPUSharedFenceImpl>>(fences.length);
      for (var i = 0; i < fences.length; i++) {
        arrayPtr[i] = fences[i]._ptr;
      }
      ptr.ref.fences = arrayPtr;
      ptr.ref.fenceCount = fences.length;
    } else {
      ptr.ref.fences = nullptr;
      ptr.ref.fenceCount = 0;
    }

    ptr.ref.signaledValues = signaledValues;

    return ptr;
  }

  factory SharedBufferMemoryBeginAccessDescriptor.fromNative(bindings.WGPUSharedBufferMemoryBeginAccessDescriptor v) {
    final _fences = <SharedFence>[];
    if (v.fences != nullptr && v.fenceCount > 0) {
      for (var i = 0; i < v.fenceCount; i++) {
        _fences.add(SharedFence._(v.fences[i]));
      }
    }
    return SharedBufferMemoryBeginAccessDescriptor(
      next: ChainedStruct.fromNative(v.nextInChain),
      initialized: (v.initialized != 0),
      fences: _fences,
      signaledValues: v.signaledValues,
    );
  }
}

class SharedBufferMemoryD3D12SharedMemoryFileMappingHandleDescriptor extends ChainedStruct {
  const SharedBufferMemoryD3D12SharedMemoryFileMappingHandleDescriptor({
    required this.handle,
    this.size = 0,
    super.next,
  });

  final Pointer<Void> handle;
  final int size;

  @override
  Pointer<bindings.WGPUSharedBufferMemoryD3D12SharedMemoryFileMappingHandleDescriptor> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPUSharedBufferMemoryD3D12SharedMemoryFileMappingHandleDescriptor>();
    ptr.ref.chain.sTypeAsInt = sType.value;
    ptr.ref.chain.next = next?.toNative(allocator).cast() ?? nullptr;
    ptr.ref.handle = handle;
    ptr.ref.size = size;

    return ptr;
  }

  factory SharedBufferMemoryD3D12SharedMemoryFileMappingHandleDescriptor.fromNative(bindings.WGPUSharedBufferMemoryD3D12SharedMemoryFileMappingHandleDescriptor v) {
    return SharedBufferMemoryD3D12SharedMemoryFileMappingHandleDescriptor(
      handle: v.handle,
      size: v.size,
    );
  }

  @override
  SType get sType => .sharedBufferMemoryD3D12SharedMemoryFileMappingHandleDescriptor;
}

class SharedBufferMemoryEndAccessState {
  const SharedBufferMemoryEndAccessState({
    this.next,
    this.initialized = false,
    this.fences = const [],
    required this.signaledValues,
  });

  final ChainedStruct? next;
  final bool initialized;
  final List<SharedFence> fences;
  final Pointer<Uint64> signaledValues;

  Pointer<bindings.WGPUSharedBufferMemoryEndAccessState> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPUSharedBufferMemoryEndAccessState>();
    ptr.ref.nextInChain = next?.toNative(allocator).cast() ?? nullptr;
    ptr.ref.initialized = (initialized ? 1 : 0);
    if (fences.isNotEmpty) {
      final arrayPtr = allocator<Pointer<bindings.WGPUSharedFenceImpl>>(fences.length);
      for (var i = 0; i < fences.length; i++) {
        arrayPtr[i] = fences[i]._ptr;
      }
      ptr.ref.fences = arrayPtr;
      ptr.ref.fenceCount = fences.length;
    } else {
      ptr.ref.fences = nullptr;
      ptr.ref.fenceCount = 0;
    }

    ptr.ref.signaledValues = signaledValues;

    return ptr;
  }

  factory SharedBufferMemoryEndAccessState.fromNative(bindings.WGPUSharedBufferMemoryEndAccessState v) {
    final _fences = <SharedFence>[];
    if (v.fences != nullptr && v.fenceCount > 0) {
      for (var i = 0; i < v.fenceCount; i++) {
        _fences.add(SharedFence._(v.fences[i]));
      }
    }
    return SharedBufferMemoryEndAccessState(
      next: ChainedStruct.fromNative(v.nextInChain),
      initialized: (v.initialized != 0),
      fences: _fences,
      signaledValues: v.signaledValues,
    );
  }
}

class SharedBufferMemoryProperties {
  const SharedBufferMemoryProperties({
    this.next,
    this.usage = const BufferUsage(0),
    this.size = 0,
  });

  final ChainedStruct? next;
  final BufferUsage usage;
  final int size;

  Pointer<bindings.WGPUSharedBufferMemoryProperties> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPUSharedBufferMemoryProperties>();
    ptr.ref.nextInChain = next?.toNative(allocator).cast() ?? nullptr;
    ptr.ref.usage = usage.value;
    ptr.ref.size = size;

    return ptr;
  }

  factory SharedBufferMemoryProperties.fromNative(bindings.WGPUSharedBufferMemoryProperties v) {
    return SharedBufferMemoryProperties(
      next: ChainedStruct.fromNative(v.nextInChain),
      usage: BufferUsage(v.usage),
      size: v.size,
    );
  }
}

class SharedFenceDXGISharedHandleDescriptor extends ChainedStruct {
  const SharedFenceDXGISharedHandleDescriptor({
    required this.handle,
    super.next,
  });

  final Pointer<Void> handle;

  @override
  Pointer<bindings.WGPUSharedFenceDXGISharedHandleDescriptor> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPUSharedFenceDXGISharedHandleDescriptor>();
    ptr.ref.chain.sTypeAsInt = sType.value;
    ptr.ref.chain.next = next?.toNative(allocator).cast() ?? nullptr;
    ptr.ref.handle = handle;

    return ptr;
  }

  factory SharedFenceDXGISharedHandleDescriptor.fromNative(bindings.WGPUSharedFenceDXGISharedHandleDescriptor v) {
    return SharedFenceDXGISharedHandleDescriptor(
      handle: v.handle,
    );
  }

  @override
  SType get sType => .sharedFenceDXGISharedHandleDescriptor;
}

class SharedFenceDXGISharedHandleExportInfo extends ChainedStruct {
  const SharedFenceDXGISharedHandleExportInfo({
    required this.handle,
    super.next,
  });

  final Pointer<Void> handle;

  @override
  Pointer<bindings.WGPUSharedFenceDXGISharedHandleExportInfo> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPUSharedFenceDXGISharedHandleExportInfo>();
    ptr.ref.chain.sTypeAsInt = sType.value;
    ptr.ref.chain.next = next?.toNative(allocator).cast() ?? nullptr;
    ptr.ref.handle = handle;

    return ptr;
  }

  factory SharedFenceDXGISharedHandleExportInfo.fromNative(bindings.WGPUSharedFenceDXGISharedHandleExportInfo v) {
    return SharedFenceDXGISharedHandleExportInfo(
      handle: v.handle,
    );
  }

  @override
  SType get sType => .sharedFenceDXGISharedHandleExportInfo;
}

class SharedFenceEGLSyncDescriptor extends ChainedStruct {
  const SharedFenceEGLSyncDescriptor({
    required this.sync,
    super.next,
  });

  final Pointer<Void> sync;

  @override
  Pointer<bindings.WGPUSharedFenceEGLSyncDescriptor> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPUSharedFenceEGLSyncDescriptor>();
    ptr.ref.chain.sTypeAsInt = sType.value;
    ptr.ref.chain.next = next?.toNative(allocator).cast() ?? nullptr;
    ptr.ref.sync = sync;

    return ptr;
  }

  factory SharedFenceEGLSyncDescriptor.fromNative(bindings.WGPUSharedFenceEGLSyncDescriptor v) {
    return SharedFenceEGLSyncDescriptor(
      sync: v.sync,
    );
  }

  @override
  SType get sType => .sharedFenceEGLSyncDescriptor;
}

class SharedFenceEGLSyncExportInfo extends ChainedStruct {
  const SharedFenceEGLSyncExportInfo({
    required this.sync,
    super.next,
  });

  final Pointer<Void> sync;

  @override
  Pointer<bindings.WGPUSharedFenceEGLSyncExportInfo> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPUSharedFenceEGLSyncExportInfo>();
    ptr.ref.chain.sTypeAsInt = sType.value;
    ptr.ref.chain.next = next?.toNative(allocator).cast() ?? nullptr;
    ptr.ref.sync = sync;

    return ptr;
  }

  factory SharedFenceEGLSyncExportInfo.fromNative(bindings.WGPUSharedFenceEGLSyncExportInfo v) {
    return SharedFenceEGLSyncExportInfo(
      sync: v.sync,
    );
  }

  @override
  SType get sType => .sharedFenceEGLSyncExportInfo;
}

class SharedFenceMTLSharedEventDescriptor extends ChainedStruct {
  const SharedFenceMTLSharedEventDescriptor({
    required this.sharedEvent,
    super.next,
  });

  final Pointer<Void> sharedEvent;

  @override
  Pointer<bindings.WGPUSharedFenceMTLSharedEventDescriptor> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPUSharedFenceMTLSharedEventDescriptor>();
    ptr.ref.chain.sTypeAsInt = sType.value;
    ptr.ref.chain.next = next?.toNative(allocator).cast() ?? nullptr;
    ptr.ref.sharedEvent = sharedEvent;

    return ptr;
  }

  factory SharedFenceMTLSharedEventDescriptor.fromNative(bindings.WGPUSharedFenceMTLSharedEventDescriptor v) {
    return SharedFenceMTLSharedEventDescriptor(
      sharedEvent: v.sharedEvent,
    );
  }

  @override
  SType get sType => .sharedFenceMTLSharedEventDescriptor;
}

class SharedFenceMTLSharedEventExportInfo extends ChainedStruct {
  const SharedFenceMTLSharedEventExportInfo({
    required this.sharedEvent,
    super.next,
  });

  final Pointer<Void> sharedEvent;

  @override
  Pointer<bindings.WGPUSharedFenceMTLSharedEventExportInfo> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPUSharedFenceMTLSharedEventExportInfo>();
    ptr.ref.chain.sTypeAsInt = sType.value;
    ptr.ref.chain.next = next?.toNative(allocator).cast() ?? nullptr;
    ptr.ref.sharedEvent = sharedEvent;

    return ptr;
  }

  factory SharedFenceMTLSharedEventExportInfo.fromNative(bindings.WGPUSharedFenceMTLSharedEventExportInfo v) {
    return SharedFenceMTLSharedEventExportInfo(
      sharedEvent: v.sharedEvent,
    );
  }

  @override
  SType get sType => .sharedFenceMTLSharedEventExportInfo;
}

class SharedFenceSyncFDDescriptor extends ChainedStruct {
  const SharedFenceSyncFDDescriptor({
    this.handle = 0,
    super.next,
  });

  final int handle;

  @override
  Pointer<bindings.WGPUSharedFenceSyncFDDescriptor> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPUSharedFenceSyncFDDescriptor>();
    ptr.ref.chain.sTypeAsInt = sType.value;
    ptr.ref.chain.next = next?.toNative(allocator).cast() ?? nullptr;
    ptr.ref.handle = handle;

    return ptr;
  }

  factory SharedFenceSyncFDDescriptor.fromNative(bindings.WGPUSharedFenceSyncFDDescriptor v) {
    return SharedFenceSyncFDDescriptor(
      handle: v.handle,
    );
  }

  @override
  SType get sType => .sharedFenceSyncFDDescriptor;
}

class SharedFenceSyncFDExportInfo extends ChainedStruct {
  const SharedFenceSyncFDExportInfo({
    this.handle = 0,
    super.next,
  });

  final int handle;

  @override
  Pointer<bindings.WGPUSharedFenceSyncFDExportInfo> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPUSharedFenceSyncFDExportInfo>();
    ptr.ref.chain.sTypeAsInt = sType.value;
    ptr.ref.chain.next = next?.toNative(allocator).cast() ?? nullptr;
    ptr.ref.handle = handle;

    return ptr;
  }

  factory SharedFenceSyncFDExportInfo.fromNative(bindings.WGPUSharedFenceSyncFDExportInfo v) {
    return SharedFenceSyncFDExportInfo(
      handle: v.handle,
    );
  }

  @override
  SType get sType => .sharedFenceSyncFDExportInfo;
}

class SharedFenceVkSemaphoreOpaqueFDDescriptor extends ChainedStruct {
  const SharedFenceVkSemaphoreOpaqueFDDescriptor({
    this.handle = 0,
    super.next,
  });

  final int handle;

  @override
  Pointer<bindings.WGPUSharedFenceVkSemaphoreOpaqueFDDescriptor> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPUSharedFenceVkSemaphoreOpaqueFDDescriptor>();
    ptr.ref.chain.sTypeAsInt = sType.value;
    ptr.ref.chain.next = next?.toNative(allocator).cast() ?? nullptr;
    ptr.ref.handle = handle;

    return ptr;
  }

  factory SharedFenceVkSemaphoreOpaqueFDDescriptor.fromNative(bindings.WGPUSharedFenceVkSemaphoreOpaqueFDDescriptor v) {
    return SharedFenceVkSemaphoreOpaqueFDDescriptor(
      handle: v.handle,
    );
  }

  @override
  SType get sType => .sharedFenceVkSemaphoreOpaqueFDDescriptor;
}

class SharedFenceVkSemaphoreOpaqueFDExportInfo extends ChainedStruct {
  const SharedFenceVkSemaphoreOpaqueFDExportInfo({
    this.handle = 0,
    super.next,
  });

  final int handle;

  @override
  Pointer<bindings.WGPUSharedFenceVkSemaphoreOpaqueFDExportInfo> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPUSharedFenceVkSemaphoreOpaqueFDExportInfo>();
    ptr.ref.chain.sTypeAsInt = sType.value;
    ptr.ref.chain.next = next?.toNative(allocator).cast() ?? nullptr;
    ptr.ref.handle = handle;

    return ptr;
  }

  factory SharedFenceVkSemaphoreOpaqueFDExportInfo.fromNative(bindings.WGPUSharedFenceVkSemaphoreOpaqueFDExportInfo v) {
    return SharedFenceVkSemaphoreOpaqueFDExportInfo(
      handle: v.handle,
    );
  }

  @override
  SType get sType => .sharedFenceVkSemaphoreOpaqueFDExportInfo;
}

class SharedFenceVkSemaphoreZirconHandleDescriptor extends ChainedStruct {
  const SharedFenceVkSemaphoreZirconHandleDescriptor({
    this.handle = 0,
    super.next,
  });

  final int handle;

  @override
  Pointer<bindings.WGPUSharedFenceVkSemaphoreZirconHandleDescriptor> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPUSharedFenceVkSemaphoreZirconHandleDescriptor>();
    ptr.ref.chain.sTypeAsInt = sType.value;
    ptr.ref.chain.next = next?.toNative(allocator).cast() ?? nullptr;
    ptr.ref.handle = handle;

    return ptr;
  }

  factory SharedFenceVkSemaphoreZirconHandleDescriptor.fromNative(bindings.WGPUSharedFenceVkSemaphoreZirconHandleDescriptor v) {
    return SharedFenceVkSemaphoreZirconHandleDescriptor(
      handle: v.handle,
    );
  }

  @override
  SType get sType => .sharedFenceVkSemaphoreZirconHandleDescriptor;
}

class SharedFenceVkSemaphoreZirconHandleExportInfo extends ChainedStruct {
  const SharedFenceVkSemaphoreZirconHandleExportInfo({
    this.handle = 0,
    super.next,
  });

  final int handle;

  @override
  Pointer<bindings.WGPUSharedFenceVkSemaphoreZirconHandleExportInfo> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPUSharedFenceVkSemaphoreZirconHandleExportInfo>();
    ptr.ref.chain.sTypeAsInt = sType.value;
    ptr.ref.chain.next = next?.toNative(allocator).cast() ?? nullptr;
    ptr.ref.handle = handle;

    return ptr;
  }

  factory SharedFenceVkSemaphoreZirconHandleExportInfo.fromNative(bindings.WGPUSharedFenceVkSemaphoreZirconHandleExportInfo v) {
    return SharedFenceVkSemaphoreZirconHandleExportInfo(
      handle: v.handle,
    );
  }

  @override
  SType get sType => .sharedFenceVkSemaphoreZirconHandleExportInfo;
}

class SharedTextureMemoryAHardwareBufferDescriptor extends ChainedStruct {
  const SharedTextureMemoryAHardwareBufferDescriptor({
    required this.handle,
    super.next,
  });

  final Pointer<Void> handle;

  @override
  Pointer<bindings.WGPUSharedTextureMemoryAHardwareBufferDescriptor> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPUSharedTextureMemoryAHardwareBufferDescriptor>();
    ptr.ref.chain.sTypeAsInt = sType.value;
    ptr.ref.chain.next = next?.toNative(allocator).cast() ?? nullptr;
    ptr.ref.handle = handle;

    return ptr;
  }

  factory SharedTextureMemoryAHardwareBufferDescriptor.fromNative(bindings.WGPUSharedTextureMemoryAHardwareBufferDescriptor v) {
    return SharedTextureMemoryAHardwareBufferDescriptor(
      handle: v.handle,
    );
  }

  @override
  SType get sType => .sharedTextureMemoryAHardwareBufferDescriptor;
}

class SharedTextureMemoryD3D11BeginState extends ChainedStruct {
  const SharedTextureMemoryD3D11BeginState({
    this.requiresEndAccessFence = false,
    super.next,
  });

  final bool requiresEndAccessFence;

  @override
  Pointer<bindings.WGPUSharedTextureMemoryD3D11BeginState> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPUSharedTextureMemoryD3D11BeginState>();
    ptr.ref.chain.sTypeAsInt = sType.value;
    ptr.ref.chain.next = next?.toNative(allocator).cast() ?? nullptr;
    ptr.ref.requiresEndAccessFence = (requiresEndAccessFence ? 1 : 0);

    return ptr;
  }

  factory SharedTextureMemoryD3D11BeginState.fromNative(bindings.WGPUSharedTextureMemoryD3D11BeginState v) {
    return SharedTextureMemoryD3D11BeginState(
      requiresEndAccessFence: (v.requiresEndAccessFence != 0),
    );
  }

  @override
  SType get sType => .sharedTextureMemoryD3D11BeginState;
}

class SharedTextureMemoryD3DSwapchainBeginState extends ChainedStruct {
  const SharedTextureMemoryD3DSwapchainBeginState({
    this.isSwapchain = false,
    super.next,
  });

  final bool isSwapchain;

  @override
  Pointer<bindings.WGPUSharedTextureMemoryD3DSwapchainBeginState> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPUSharedTextureMemoryD3DSwapchainBeginState>();
    ptr.ref.chain.sTypeAsInt = sType.value;
    ptr.ref.chain.next = next?.toNative(allocator).cast() ?? nullptr;
    ptr.ref.isSwapchain = (isSwapchain ? 1 : 0);

    return ptr;
  }

  factory SharedTextureMemoryD3DSwapchainBeginState.fromNative(bindings.WGPUSharedTextureMemoryD3DSwapchainBeginState v) {
    return SharedTextureMemoryD3DSwapchainBeginState(
      isSwapchain: (v.isSwapchain != 0),
    );
  }

  @override
  SType get sType => .sharedTextureMemoryD3DSwapchainBeginState;
}

class SharedTextureMemoryDmaBufPlane {
  const SharedTextureMemoryDmaBufPlane({
    this.fd = 0,
    this.offset = 0,
    this.stride = 0,
  });

  final int fd;
  final int offset;
  final int stride;

  Pointer<bindings.WGPUSharedTextureMemoryDmaBufPlane> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPUSharedTextureMemoryDmaBufPlane>();
    ptr.ref.fd = fd;
    ptr.ref.offset = offset;
    ptr.ref.stride = stride;

    return ptr;
  }

  factory SharedTextureMemoryDmaBufPlane.fromNative(bindings.WGPUSharedTextureMemoryDmaBufPlane v) {
    return SharedTextureMemoryDmaBufPlane(
      fd: v.fd,
      offset: v.offset,
      stride: v.stride,
    );
  }
}

class SharedTextureMemoryDXGISharedHandleDescriptor extends ChainedStruct {
  const SharedTextureMemoryDXGISharedHandleDescriptor({
    required this.handle,
    this.useKeyedMutex = false,
    super.next,
  });

  final Pointer<Void> handle;
  final bool useKeyedMutex;

  @override
  Pointer<bindings.WGPUSharedTextureMemoryDXGISharedHandleDescriptor> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPUSharedTextureMemoryDXGISharedHandleDescriptor>();
    ptr.ref.chain.sTypeAsInt = sType.value;
    ptr.ref.chain.next = next?.toNative(allocator).cast() ?? nullptr;
    ptr.ref.handle = handle;
    ptr.ref.useKeyedMutex = (useKeyedMutex ? 1 : 0);

    return ptr;
  }

  factory SharedTextureMemoryDXGISharedHandleDescriptor.fromNative(bindings.WGPUSharedTextureMemoryDXGISharedHandleDescriptor v) {
    return SharedTextureMemoryDXGISharedHandleDescriptor(
      handle: v.handle,
      useKeyedMutex: (v.useKeyedMutex != 0),
    );
  }

  @override
  SType get sType => .sharedTextureMemoryDXGISharedHandleDescriptor;
}

class SharedTextureMemoryEGLImageDescriptor extends ChainedStruct {
  const SharedTextureMemoryEGLImageDescriptor({
    required this.image,
    super.next,
  });

  final Pointer<Void> image;

  @override
  Pointer<bindings.WGPUSharedTextureMemoryEGLImageDescriptor> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPUSharedTextureMemoryEGLImageDescriptor>();
    ptr.ref.chain.sTypeAsInt = sType.value;
    ptr.ref.chain.next = next?.toNative(allocator).cast() ?? nullptr;
    ptr.ref.image = image;

    return ptr;
  }

  factory SharedTextureMemoryEGLImageDescriptor.fromNative(bindings.WGPUSharedTextureMemoryEGLImageDescriptor v) {
    return SharedTextureMemoryEGLImageDescriptor(
      image: v.image,
    );
  }

  @override
  SType get sType => .sharedTextureMemoryEGLImageDescriptor;
}

class SharedTextureMemoryIOSurfaceDescriptor extends ChainedStruct {
  const SharedTextureMemoryIOSurfaceDescriptor({
    required this.ioSurface,
    this.allowStorageBinding = false,
    super.next,
  });

  final Pointer<Void> ioSurface;
  final bool allowStorageBinding;

  @override
  Pointer<bindings.WGPUSharedTextureMemoryIOSurfaceDescriptor> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPUSharedTextureMemoryIOSurfaceDescriptor>();
    ptr.ref.chain.sTypeAsInt = sType.value;
    ptr.ref.chain.next = next?.toNative(allocator).cast() ?? nullptr;
    ptr.ref.ioSurface = ioSurface;
    ptr.ref.allowStorageBinding = (allowStorageBinding ? 1 : 0);

    return ptr;
  }

  factory SharedTextureMemoryIOSurfaceDescriptor.fromNative(bindings.WGPUSharedTextureMemoryIOSurfaceDescriptor v) {
    return SharedTextureMemoryIOSurfaceDescriptor(
      ioSurface: v.ioSurface,
      allowStorageBinding: (v.allowStorageBinding != 0),
    );
  }

  @override
  SType get sType => .sharedTextureMemoryIOSurfaceDescriptor;
}

class SharedTextureMemoryOpaqueFDDescriptor extends ChainedStruct {
  const SharedTextureMemoryOpaqueFDDescriptor({
    required this.vkImageCreateInfo,
    this.memoryFD = 0,
    this.memoryTypeIndex = 0,
    this.allocationSize = 0,
    this.dedicatedAllocation = false,
    super.next,
  });

  final Pointer<Void> vkImageCreateInfo;
  final int memoryFD;
  final int memoryTypeIndex;
  final int allocationSize;
  final bool dedicatedAllocation;

  @override
  Pointer<bindings.WGPUSharedTextureMemoryOpaqueFDDescriptor> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPUSharedTextureMemoryOpaqueFDDescriptor>();
    ptr.ref.chain.sTypeAsInt = sType.value;
    ptr.ref.chain.next = next?.toNative(allocator).cast() ?? nullptr;
    ptr.ref.vkImageCreateInfo = vkImageCreateInfo;
    ptr.ref.memoryFD = memoryFD;
    ptr.ref.memoryTypeIndex = memoryTypeIndex;
    ptr.ref.allocationSize = allocationSize;
    ptr.ref.dedicatedAllocation = (dedicatedAllocation ? 1 : 0);

    return ptr;
  }

  factory SharedTextureMemoryOpaqueFDDescriptor.fromNative(bindings.WGPUSharedTextureMemoryOpaqueFDDescriptor v) {
    return SharedTextureMemoryOpaqueFDDescriptor(
      vkImageCreateInfo: v.vkImageCreateInfo,
      memoryFD: v.memoryFD,
      memoryTypeIndex: v.memoryTypeIndex,
      allocationSize: v.allocationSize,
      dedicatedAllocation: (v.dedicatedAllocation != 0),
    );
  }

  @override
  SType get sType => .sharedTextureMemoryOpaqueFDDescriptor;
}

class SharedTextureMemoryVkDedicatedAllocationDescriptor extends ChainedStruct {
  const SharedTextureMemoryVkDedicatedAllocationDescriptor({
    this.dedicatedAllocation = false,
    super.next,
  });

  final bool dedicatedAllocation;

  @override
  Pointer<bindings.WGPUSharedTextureMemoryVkDedicatedAllocationDescriptor> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPUSharedTextureMemoryVkDedicatedAllocationDescriptor>();
    ptr.ref.chain.sTypeAsInt = sType.value;
    ptr.ref.chain.next = next?.toNative(allocator).cast() ?? nullptr;
    ptr.ref.dedicatedAllocation = (dedicatedAllocation ? 1 : 0);

    return ptr;
  }

  factory SharedTextureMemoryVkDedicatedAllocationDescriptor.fromNative(bindings.WGPUSharedTextureMemoryVkDedicatedAllocationDescriptor v) {
    return SharedTextureMemoryVkDedicatedAllocationDescriptor(
      dedicatedAllocation: (v.dedicatedAllocation != 0),
    );
  }

  @override
  SType get sType => .sharedTextureMemoryVkDedicatedAllocationDescriptor;
}

class SharedTextureMemoryVkImageLayoutBeginState extends ChainedStruct {
  const SharedTextureMemoryVkImageLayoutBeginState({
    this.oldLayout = 0,
    this.newLayout = 0,
    super.next,
  });

  final int oldLayout;
  final int newLayout;

  @override
  Pointer<bindings.WGPUSharedTextureMemoryVkImageLayoutBeginState> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPUSharedTextureMemoryVkImageLayoutBeginState>();
    ptr.ref.chain.sTypeAsInt = sType.value;
    ptr.ref.chain.next = next?.toNative(allocator).cast() ?? nullptr;
    ptr.ref.oldLayout = oldLayout;
    ptr.ref.newLayout = newLayout;

    return ptr;
  }

  factory SharedTextureMemoryVkImageLayoutBeginState.fromNative(bindings.WGPUSharedTextureMemoryVkImageLayoutBeginState v) {
    return SharedTextureMemoryVkImageLayoutBeginState(
      oldLayout: v.oldLayout,
      newLayout: v.newLayout,
    );
  }

  @override
  SType get sType => .sharedTextureMemoryVkImageLayoutBeginState;
}

class SharedTextureMemoryVkImageLayoutEndState extends ChainedStruct {
  const SharedTextureMemoryVkImageLayoutEndState({
    this.oldLayout = 0,
    this.newLayout = 0,
    super.next,
  });

  final int oldLayout;
  final int newLayout;

  @override
  Pointer<bindings.WGPUSharedTextureMemoryVkImageLayoutEndState> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPUSharedTextureMemoryVkImageLayoutEndState>();
    ptr.ref.chain.sTypeAsInt = sType.value;
    ptr.ref.chain.next = next?.toNative(allocator).cast() ?? nullptr;
    ptr.ref.oldLayout = oldLayout;
    ptr.ref.newLayout = newLayout;

    return ptr;
  }

  factory SharedTextureMemoryVkImageLayoutEndState.fromNative(bindings.WGPUSharedTextureMemoryVkImageLayoutEndState v) {
    return SharedTextureMemoryVkImageLayoutEndState(
      oldLayout: v.oldLayout,
      newLayout: v.newLayout,
    );
  }

  @override
  SType get sType => .sharedTextureMemoryVkImageLayoutEndState;
}

class SharedTextureMemoryZirconHandleDescriptor extends ChainedStruct {
  const SharedTextureMemoryZirconHandleDescriptor({
    this.memoryFD = 0,
    this.allocationSize = 0,
    super.next,
  });

  final int memoryFD;
  final int allocationSize;

  @override
  Pointer<bindings.WGPUSharedTextureMemoryZirconHandleDescriptor> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPUSharedTextureMemoryZirconHandleDescriptor>();
    ptr.ref.chain.sTypeAsInt = sType.value;
    ptr.ref.chain.next = next?.toNative(allocator).cast() ?? nullptr;
    ptr.ref.memoryFD = memoryFD;
    ptr.ref.allocationSize = allocationSize;

    return ptr;
  }

  factory SharedTextureMemoryZirconHandleDescriptor.fromNative(bindings.WGPUSharedTextureMemoryZirconHandleDescriptor v) {
    return SharedTextureMemoryZirconHandleDescriptor(
      memoryFD: v.memoryFD,
      allocationSize: v.allocationSize,
    );
  }

  @override
  SType get sType => .sharedTextureMemoryZirconHandleDescriptor;
}

class StaticSamplerBindingLayout extends ChainedStruct {
  const StaticSamplerBindingLayout({
    required this.sampler,
    this.sampledTextureBinding = 0,
    super.next,
  });

  final Sampler sampler;
  final int sampledTextureBinding;

  @override
  Pointer<bindings.WGPUStaticSamplerBindingLayout> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPUStaticSamplerBindingLayout>();
    ptr.ref.chain.sTypeAsInt = sType.value;
    ptr.ref.chain.next = next?.toNative(allocator).cast() ?? nullptr;
    ptr.ref.sampler = sampler._ptr;
    ptr.ref.sampledTextureBinding = sampledTextureBinding;

    return ptr;
  }

  factory StaticSamplerBindingLayout.fromNative(bindings.WGPUStaticSamplerBindingLayout v) {
    return StaticSamplerBindingLayout(
      sampler: Sampler._(v.sampler),
      sampledTextureBinding: v.sampledTextureBinding,
    );
  }

  @override
  SType get sType => .staticSamplerBindingLayout;
}

class StencilFaceState {
  const StencilFaceState({
    this.compare = CompareFunction.undefined,
    this.failOp = StencilOperation.undefined,
    this.depthFailOp = StencilOperation.undefined,
    this.passOp = StencilOperation.undefined,
  });

  final CompareFunction compare;
  final StencilOperation failOp;
  final StencilOperation depthFailOp;
  final StencilOperation passOp;

  Pointer<bindings.WGPUStencilFaceState> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPUStencilFaceState>();
    ptr.ref.compareAsInt = compare.value;
    ptr.ref.failOpAsInt = failOp.value;
    ptr.ref.depthFailOpAsInt = depthFailOp.value;
    ptr.ref.passOpAsInt = passOp.value;

    return ptr;
  }

  factory StencilFaceState.fromNative(bindings.WGPUStencilFaceState v) {
    return StencilFaceState(
      compare: CompareFunction.fromValue(v.compareAsInt),
      failOp: StencilOperation.fromValue(v.failOpAsInt),
      depthFailOp: StencilOperation.fromValue(v.depthFailOpAsInt),
      passOp: StencilOperation.fromValue(v.passOpAsInt),
    );
  }
}

class StorageTextureBindingLayout {
  const StorageTextureBindingLayout({
    this.next,
    this.access = StorageTextureAccess.bindingNotUsed,
    this.format = TextureFormat.undefined,
    this.viewDimension = TextureViewDimension.undefined,
  });

  final ChainedStruct? next;
  final StorageTextureAccess access;
  final TextureFormat format;
  final TextureViewDimension viewDimension;

  Pointer<bindings.WGPUStorageTextureBindingLayout> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPUStorageTextureBindingLayout>();
    ptr.ref.nextInChain = next?.toNative(allocator).cast() ?? nullptr;
    ptr.ref.accessAsInt = access.value;
    ptr.ref.formatAsInt = format.value;
    ptr.ref.viewDimensionAsInt = viewDimension.value;

    return ptr;
  }

  factory StorageTextureBindingLayout.fromNative(bindings.WGPUStorageTextureBindingLayout v) {
    return StorageTextureBindingLayout(
      next: ChainedStruct.fromNative(v.nextInChain),
      access: StorageTextureAccess.fromValue(v.accessAsInt),
      format: TextureFormat.fromValue(v.formatAsInt),
      viewDimension: TextureViewDimension.fromValue(v.viewDimensionAsInt),
    );
  }
}

class SubgroupMatrixConfig {
  const SubgroupMatrixConfig({
    this.componentType = SubgroupMatrixComponentType.f32,
    this.resultComponentType = SubgroupMatrixComponentType.f32,
    this.m = 0,
    this.n = 0,
    this.k = 0,
  });

  final SubgroupMatrixComponentType componentType;
  final SubgroupMatrixComponentType resultComponentType;
  final int m;
  final int n;
  final int k;

  Pointer<bindings.WGPUSubgroupMatrixConfig> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPUSubgroupMatrixConfig>();
    ptr.ref.componentTypeAsInt = componentType.value;
    ptr.ref.resultComponentTypeAsInt = resultComponentType.value;
    ptr.ref.M = m;
    ptr.ref.N = n;
    ptr.ref.K = k;

    return ptr;
  }

  factory SubgroupMatrixConfig.fromNative(bindings.WGPUSubgroupMatrixConfig v) {
    return SubgroupMatrixConfig(
      componentType: SubgroupMatrixComponentType.fromValue(v.componentTypeAsInt),
      resultComponentType: SubgroupMatrixComponentType.fromValue(v.resultComponentTypeAsInt),
      m: v.M,
      n: v.N,
      k: v.K,
    );
  }
}

class SupportedFeatures {
  const SupportedFeatures({
    this.features = const [],
  });

  final List<FeatureName> features;

  Pointer<bindings.WGPUSupportedFeatures> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPUSupportedFeatures>();
    if (features.isNotEmpty) {
      final arrayPtr = allocator<UnsignedInt>(features.length);
      for (var i = 0; i < features.length; i++) {
        arrayPtr[i] = features[i].value;
      }
      ptr.ref.features = arrayPtr;
      ptr.ref.featureCount = features.length;
    } else {
      ptr.ref.features = nullptr;
      ptr.ref.featureCount = 0;
    }

    return ptr;
  }

  factory SupportedFeatures.fromNative(bindings.WGPUSupportedFeatures v) {
    final _features = <FeatureName>[];
    if (v.features != nullptr && v.featureCount > 0) {
      for (var i = 0; i < v.featureCount; i++) {
        _features.add(FeatureName.fromValue(v.features[i]));
      }
    }
    return SupportedFeatures(
      features: _features,
    );
  }
}

class SupportedInstanceFeatures {
  const SupportedInstanceFeatures({
    this.features = const [],
  });

  final List<InstanceFeatureName> features;

  Pointer<bindings.WGPUSupportedInstanceFeatures> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPUSupportedInstanceFeatures>();
    if (features.isNotEmpty) {
      final arrayPtr = allocator<UnsignedInt>(features.length);
      for (var i = 0; i < features.length; i++) {
        arrayPtr[i] = features[i].value;
      }
      ptr.ref.features = arrayPtr;
      ptr.ref.featureCount = features.length;
    } else {
      ptr.ref.features = nullptr;
      ptr.ref.featureCount = 0;
    }

    return ptr;
  }

  factory SupportedInstanceFeatures.fromNative(bindings.WGPUSupportedInstanceFeatures v) {
    final _features = <InstanceFeatureName>[];
    if (v.features != nullptr && v.featureCount > 0) {
      for (var i = 0; i < v.featureCount; i++) {
        _features.add(InstanceFeatureName.fromValue(v.features[i]));
      }
    }
    return SupportedInstanceFeatures(
      features: _features,
    );
  }
}

class SupportedWGSLLanguageFeatures {
  const SupportedWGSLLanguageFeatures({
    this.features = const [],
  });

  final List<WGSLLanguageFeatureName> features;

  Pointer<bindings.WGPUSupportedWGSLLanguageFeatures> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPUSupportedWGSLLanguageFeatures>();
    if (features.isNotEmpty) {
      final arrayPtr = allocator<UnsignedInt>(features.length);
      for (var i = 0; i < features.length; i++) {
        arrayPtr[i] = features[i].value;
      }
      ptr.ref.features = arrayPtr;
      ptr.ref.featureCount = features.length;
    } else {
      ptr.ref.features = nullptr;
      ptr.ref.featureCount = 0;
    }

    return ptr;
  }

  factory SupportedWGSLLanguageFeatures.fromNative(bindings.WGPUSupportedWGSLLanguageFeatures v) {
    final _features = <WGSLLanguageFeatureName>[];
    if (v.features != nullptr && v.featureCount > 0) {
      for (var i = 0; i < v.featureCount; i++) {
        _features.add(WGSLLanguageFeatureName.fromValue(v.features[i]));
      }
    }
    return SupportedWGSLLanguageFeatures(
      features: _features,
    );
  }
}

class SurfaceCapabilities {
  const SurfaceCapabilities({
    this.next,
    this.usages = const TextureUsage(0),
    this.formats = const [],
    this.presentModes = const [],
    this.alphaModes = const [],
  });

  final ChainedStruct? next;
  final TextureUsage usages;
  final List<TextureFormat> formats;
  final List<PresentMode> presentModes;
  final List<CompositeAlphaMode> alphaModes;

  Pointer<bindings.WGPUSurfaceCapabilities> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPUSurfaceCapabilities>();
    ptr.ref.nextInChain = next?.toNative(allocator).cast() ?? nullptr;
    ptr.ref.usages = usages.value;
    if (formats.isNotEmpty) {
      final arrayPtr = allocator<UnsignedInt>(formats.length);
      for (var i = 0; i < formats.length; i++) {
        arrayPtr[i] = formats[i].value;
      }
      ptr.ref.formats = arrayPtr;
      ptr.ref.formatCount = formats.length;
    } else {
      ptr.ref.formats = nullptr;
      ptr.ref.formatCount = 0;
    }

    if (presentModes.isNotEmpty) {
      final arrayPtr = allocator<UnsignedInt>(presentModes.length);
      for (var i = 0; i < presentModes.length; i++) {
        arrayPtr[i] = presentModes[i].value;
      }
      ptr.ref.presentModes = arrayPtr;
      ptr.ref.presentModeCount = presentModes.length;
    } else {
      ptr.ref.presentModes = nullptr;
      ptr.ref.presentModeCount = 0;
    }

    if (alphaModes.isNotEmpty) {
      final arrayPtr = allocator<UnsignedInt>(alphaModes.length);
      for (var i = 0; i < alphaModes.length; i++) {
        arrayPtr[i] = alphaModes[i].value;
      }
      ptr.ref.alphaModes = arrayPtr;
      ptr.ref.alphaModeCount = alphaModes.length;
    } else {
      ptr.ref.alphaModes = nullptr;
      ptr.ref.alphaModeCount = 0;
    }

    return ptr;
  }

  factory SurfaceCapabilities.fromNative(bindings.WGPUSurfaceCapabilities v) {
    final _formats = <TextureFormat>[];
    if (v.formats != nullptr && v.formatCount > 0) {
      for (var i = 0; i < v.formatCount; i++) {
        _formats.add(TextureFormat.fromValue(v.formats[i]));
      }
    }
    final _presentModes = <PresentMode>[];
    if (v.presentModes != nullptr && v.presentModeCount > 0) {
      for (var i = 0; i < v.presentModeCount; i++) {
        _presentModes.add(PresentMode.fromValue(v.presentModes[i]));
      }
    }
    final _alphaModes = <CompositeAlphaMode>[];
    if (v.alphaModes != nullptr && v.alphaModeCount > 0) {
      for (var i = 0; i < v.alphaModeCount; i++) {
        _alphaModes.add(CompositeAlphaMode.fromValue(v.alphaModes[i]));
      }
    }
    return SurfaceCapabilities(
      next: ChainedStruct.fromNative(v.nextInChain),
      usages: TextureUsage(v.usages),
      formats: _formats,
      presentModes: _presentModes,
      alphaModes: _alphaModes,
    );
  }
}

class SurfaceColorManagement extends ChainedStruct {
  const SurfaceColorManagement({
    this.colorSpace = PredefinedColorSpace.sRGB,
    this.toneMappingMode = ToneMappingMode.standard,
    super.next,
  });

  final PredefinedColorSpace colorSpace;
  final ToneMappingMode toneMappingMode;

  @override
  Pointer<bindings.WGPUSurfaceColorManagement> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPUSurfaceColorManagement>();
    ptr.ref.chain.sTypeAsInt = sType.value;
    ptr.ref.chain.next = next?.toNative(allocator).cast() ?? nullptr;
    ptr.ref.colorSpaceAsInt = colorSpace.value;
    ptr.ref.toneMappingModeAsInt = toneMappingMode.value;

    return ptr;
  }

  factory SurfaceColorManagement.fromNative(bindings.WGPUSurfaceColorManagement v) {
    return SurfaceColorManagement(
      colorSpace: PredefinedColorSpace.fromValue(v.colorSpaceAsInt),
      toneMappingMode: ToneMappingMode.fromValue(v.toneMappingModeAsInt),
    );
  }

  @override
  SType get sType => .surfaceColorManagement;
}

class SurfaceConfiguration {
  const SurfaceConfiguration({
    this.next,
    required this.device,
    this.format = TextureFormat.undefined,
    this.usage = const TextureUsage(0),
    this.width = 0,
    this.height = 0,
    this.viewFormats = const [],
    this.alphaMode = CompositeAlphaMode.auto,
    this.presentMode = PresentMode.undefined,
  });

  final ChainedStruct? next;
  final Device device;
  final TextureFormat format;
  final TextureUsage usage;
  final int width;
  final int height;
  final List<TextureFormat> viewFormats;
  final CompositeAlphaMode alphaMode;
  final PresentMode presentMode;

  Pointer<bindings.WGPUSurfaceConfiguration> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPUSurfaceConfiguration>();
    ptr.ref.nextInChain = next?.toNative(allocator).cast() ?? nullptr;
    ptr.ref.device = device._ptr;
    ptr.ref.formatAsInt = format.value;
    ptr.ref.usage = usage.value;
    ptr.ref.width = width;
    ptr.ref.height = height;
    if (viewFormats.isNotEmpty) {
      final arrayPtr = allocator<UnsignedInt>(viewFormats.length);
      for (var i = 0; i < viewFormats.length; i++) {
        arrayPtr[i] = viewFormats[i].value;
      }
      ptr.ref.viewFormats = arrayPtr;
      ptr.ref.viewFormatCount = viewFormats.length;
    } else {
      ptr.ref.viewFormats = nullptr;
      ptr.ref.viewFormatCount = 0;
    }

    ptr.ref.alphaModeAsInt = alphaMode.value;
    ptr.ref.presentModeAsInt = presentMode.value;

    return ptr;
  }

  factory SurfaceConfiguration.fromNative(bindings.WGPUSurfaceConfiguration v) {
    final _viewFormats = <TextureFormat>[];
    if (v.viewFormats != nullptr && v.viewFormatCount > 0) {
      for (var i = 0; i < v.viewFormatCount; i++) {
        _viewFormats.add(TextureFormat.fromValue(v.viewFormats[i]));
      }
    }
    return SurfaceConfiguration(
      next: ChainedStruct.fromNative(v.nextInChain),
      device: Device._(v.device),
      format: TextureFormat.fromValue(v.formatAsInt),
      usage: TextureUsage(v.usage),
      width: v.width,
      height: v.height,
      viewFormats: _viewFormats,
      alphaMode: CompositeAlphaMode.fromValue(v.alphaModeAsInt),
      presentMode: PresentMode.fromValue(v.presentModeAsInt),
    );
  }
}

class SurfaceDescriptorFromWindowsCoreWindow extends ChainedStruct {
  const SurfaceDescriptorFromWindowsCoreWindow({
    required this.coreWindow,
    super.next,
  });

  final Pointer<Void> coreWindow;

  @override
  Pointer<bindings.WGPUSurfaceDescriptorFromWindowsCoreWindow> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPUSurfaceDescriptorFromWindowsCoreWindow>();
    ptr.ref.chain.sTypeAsInt = sType.value;
    ptr.ref.chain.next = next?.toNative(allocator).cast() ?? nullptr;
    ptr.ref.coreWindow = coreWindow;

    return ptr;
  }

  factory SurfaceDescriptorFromWindowsCoreWindow.fromNative(bindings.WGPUSurfaceDescriptorFromWindowsCoreWindow v) {
    return SurfaceDescriptorFromWindowsCoreWindow(
      coreWindow: v.coreWindow,
    );
  }

  @override
  SType get sType => .surfaceDescriptorFromWindowsCoreWindow;
}

class SurfaceDescriptorFromWindowsUWPSwapChainPanel extends ChainedStruct {
  const SurfaceDescriptorFromWindowsUWPSwapChainPanel({
    required this.swapChainPanel,
    super.next,
  });

  final Pointer<Void> swapChainPanel;

  @override
  Pointer<bindings.WGPUSurfaceDescriptorFromWindowsUWPSwapChainPanel> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPUSurfaceDescriptorFromWindowsUWPSwapChainPanel>();
    ptr.ref.chain.sTypeAsInt = sType.value;
    ptr.ref.chain.next = next?.toNative(allocator).cast() ?? nullptr;
    ptr.ref.swapChainPanel = swapChainPanel;

    return ptr;
  }

  factory SurfaceDescriptorFromWindowsUWPSwapChainPanel.fromNative(bindings.WGPUSurfaceDescriptorFromWindowsUWPSwapChainPanel v) {
    return SurfaceDescriptorFromWindowsUWPSwapChainPanel(
      swapChainPanel: v.swapChainPanel,
    );
  }

  @override
  SType get sType => .surfaceDescriptorFromWindowsUWPSwapChainPanel;
}

class SurfaceDescriptorFromWindowsWinUISwapChainPanel extends ChainedStruct {
  const SurfaceDescriptorFromWindowsWinUISwapChainPanel({
    required this.swapChainPanel,
    super.next,
  });

  final Pointer<Void> swapChainPanel;

  @override
  Pointer<bindings.WGPUSurfaceDescriptorFromWindowsWinUISwapChainPanel> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPUSurfaceDescriptorFromWindowsWinUISwapChainPanel>();
    ptr.ref.chain.sTypeAsInt = sType.value;
    ptr.ref.chain.next = next?.toNative(allocator).cast() ?? nullptr;
    ptr.ref.swapChainPanel = swapChainPanel;

    return ptr;
  }

  factory SurfaceDescriptorFromWindowsWinUISwapChainPanel.fromNative(bindings.WGPUSurfaceDescriptorFromWindowsWinUISwapChainPanel v) {
    return SurfaceDescriptorFromWindowsWinUISwapChainPanel(
      swapChainPanel: v.swapChainPanel,
    );
  }

  @override
  SType get sType => .surfaceDescriptorFromWindowsWinUISwapChainPanel;
}

class SurfaceSourceAndroidNativeWindow extends ChainedStruct {
  const SurfaceSourceAndroidNativeWindow({
    required this.window,
    super.next,
  });

  final Pointer<Void> window;

  @override
  Pointer<bindings.WGPUSurfaceSourceAndroidNativeWindow> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPUSurfaceSourceAndroidNativeWindow>();
    ptr.ref.chain.sTypeAsInt = sType.value;
    ptr.ref.chain.next = next?.toNative(allocator).cast() ?? nullptr;
    ptr.ref.window = window;

    return ptr;
  }

  factory SurfaceSourceAndroidNativeWindow.fromNative(bindings.WGPUSurfaceSourceAndroidNativeWindow v) {
    return SurfaceSourceAndroidNativeWindow(
      window: v.window,
    );
  }

  @override
  SType get sType => .surfaceSourceAndroidNativeWindow;
}

class SurfaceSourceMetalLayer extends ChainedStruct {
  const SurfaceSourceMetalLayer({
    required this.layer,
    super.next,
  });

  final Pointer<Void> layer;

  @override
  Pointer<bindings.WGPUSurfaceSourceMetalLayer> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPUSurfaceSourceMetalLayer>();
    ptr.ref.chain.sTypeAsInt = sType.value;
    ptr.ref.chain.next = next?.toNative(allocator).cast() ?? nullptr;
    ptr.ref.layer = layer;

    return ptr;
  }

  factory SurfaceSourceMetalLayer.fromNative(bindings.WGPUSurfaceSourceMetalLayer v) {
    return SurfaceSourceMetalLayer(
      layer: v.layer,
    );
  }

  @override
  SType get sType => .surfaceSourceMetalLayer;
}

class SurfaceSourceWaylandSurface extends ChainedStruct {
  const SurfaceSourceWaylandSurface({
    required this.display,
    required this.surface,
    super.next,
  });

  final Pointer<Void> display;
  final Pointer<Void> surface;

  @override
  Pointer<bindings.WGPUSurfaceSourceWaylandSurface> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPUSurfaceSourceWaylandSurface>();
    ptr.ref.chain.sTypeAsInt = sType.value;
    ptr.ref.chain.next = next?.toNative(allocator).cast() ?? nullptr;
    ptr.ref.display = display;
    ptr.ref.surface = surface;

    return ptr;
  }

  factory SurfaceSourceWaylandSurface.fromNative(bindings.WGPUSurfaceSourceWaylandSurface v) {
    return SurfaceSourceWaylandSurface(
      display: v.display,
      surface: v.surface,
    );
  }

  @override
  SType get sType => .surfaceSourceWaylandSurface;
}

class SurfaceSourceWindowsHWND extends ChainedStruct {
  const SurfaceSourceWindowsHWND({
    required this.hinstance,
    required this.hwnd,
    super.next,
  });

  final Pointer<Void> hinstance;
  final Pointer<Void> hwnd;

  @override
  Pointer<bindings.WGPUSurfaceSourceWindowsHWND> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPUSurfaceSourceWindowsHWND>();
    ptr.ref.chain.sTypeAsInt = sType.value;
    ptr.ref.chain.next = next?.toNative(allocator).cast() ?? nullptr;
    ptr.ref.hinstance = hinstance;
    ptr.ref.hwnd = hwnd;

    return ptr;
  }

  factory SurfaceSourceWindowsHWND.fromNative(bindings.WGPUSurfaceSourceWindowsHWND v) {
    return SurfaceSourceWindowsHWND(
      hinstance: v.hinstance,
      hwnd: v.hwnd,
    );
  }

  @override
  SType get sType => .surfaceSourceWindowsHWND;
}

class SurfaceSourceXCBWindow extends ChainedStruct {
  const SurfaceSourceXCBWindow({
    required this.connection,
    this.window = 0,
    super.next,
  });

  final Pointer<Void> connection;
  final int window;

  @override
  Pointer<bindings.WGPUSurfaceSourceXCBWindow> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPUSurfaceSourceXCBWindow>();
    ptr.ref.chain.sTypeAsInt = sType.value;
    ptr.ref.chain.next = next?.toNative(allocator).cast() ?? nullptr;
    ptr.ref.connection = connection;
    ptr.ref.window = window;

    return ptr;
  }

  factory SurfaceSourceXCBWindow.fromNative(bindings.WGPUSurfaceSourceXCBWindow v) {
    return SurfaceSourceXCBWindow(
      connection: v.connection,
      window: v.window,
    );
  }

  @override
  SType get sType => .surfaceSourceXCBWindow;
}

class SurfaceSourceXlibWindow extends ChainedStruct {
  const SurfaceSourceXlibWindow({
    required this.display,
    this.window = 0,
    super.next,
  });

  final Pointer<Void> display;
  final int window;

  @override
  Pointer<bindings.WGPUSurfaceSourceXlibWindow> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPUSurfaceSourceXlibWindow>();
    ptr.ref.chain.sTypeAsInt = sType.value;
    ptr.ref.chain.next = next?.toNative(allocator).cast() ?? nullptr;
    ptr.ref.display = display;
    ptr.ref.window = window;

    return ptr;
  }

  factory SurfaceSourceXlibWindow.fromNative(bindings.WGPUSurfaceSourceXlibWindow v) {
    return SurfaceSourceXlibWindow(
      display: v.display,
      window: v.window,
    );
  }

  @override
  SType get sType => .surfaceSourceXlibWindow;
}

class SurfaceTexture {
  const SurfaceTexture({
    this.next,
    required this.texture,
    this.status = SurfaceGetCurrentTextureStatus.successOptimal,
  });

  final ChainedStruct? next;
  final Texture texture;
  final SurfaceGetCurrentTextureStatus status;

  Pointer<bindings.WGPUSurfaceTexture> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPUSurfaceTexture>();
    ptr.ref.nextInChain = next?.toNative(allocator).cast() ?? nullptr;
    ptr.ref.texture = texture._ptr;
    ptr.ref.statusAsInt = status.value;

    return ptr;
  }

  factory SurfaceTexture.fromNative(bindings.WGPUSurfaceTexture v) {
    return SurfaceTexture(
      next: ChainedStruct.fromNative(v.nextInChain),
      texture: Texture._(v.texture),
      status: SurfaceGetCurrentTextureStatus.fromValue(v.statusAsInt),
    );
  }
}

class TexelBufferBindingEntry extends ChainedStruct {
  const TexelBufferBindingEntry({
    required this.texelBufferView,
    super.next,
  });

  final TexelBufferView texelBufferView;

  @override
  Pointer<bindings.WGPUTexelBufferBindingEntry> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPUTexelBufferBindingEntry>();
    ptr.ref.chain.sTypeAsInt = sType.value;
    ptr.ref.chain.next = next?.toNative(allocator).cast() ?? nullptr;
    ptr.ref.texelBufferView = texelBufferView._ptr;

    return ptr;
  }

  factory TexelBufferBindingEntry.fromNative(bindings.WGPUTexelBufferBindingEntry v) {
    return TexelBufferBindingEntry(
      texelBufferView: TexelBufferView._(v.texelBufferView),
    );
  }

  @override
  SType get sType => .texelBufferBindingEntry;
}

class TexelBufferBindingLayout extends ChainedStruct {
  const TexelBufferBindingLayout({
    this.access = TexelBufferAccess.undefined,
    this.format = TextureFormat.undefined,
    super.next,
  });

  final TexelBufferAccess access;
  final TextureFormat format;

  @override
  Pointer<bindings.WGPUTexelBufferBindingLayout> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPUTexelBufferBindingLayout>();
    ptr.ref.chain.sTypeAsInt = sType.value;
    ptr.ref.chain.next = next?.toNative(allocator).cast() ?? nullptr;
    ptr.ref.accessAsInt = access.value;
    ptr.ref.formatAsInt = format.value;

    return ptr;
  }

  factory TexelBufferBindingLayout.fromNative(bindings.WGPUTexelBufferBindingLayout v) {
    return TexelBufferBindingLayout(
      access: TexelBufferAccess.fromValue(v.accessAsInt),
      format: TextureFormat.fromValue(v.formatAsInt),
    );
  }

  @override
  SType get sType => .texelBufferBindingLayout;
}

class TexelBufferViewDescriptor {
  const TexelBufferViewDescriptor({
    this.next,
    this.label,
    this.format = TextureFormat.undefined,
    this.offset = 0,
    this.size = 0,
  });

  final ChainedStruct? next;
  final String? label;
  final TextureFormat format;
  final int offset;
  final int size;

  Pointer<bindings.WGPUTexelBufferViewDescriptor> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPUTexelBufferViewDescriptor>();
    ptr.ref.nextInChain = next?.toNative(allocator).cast() ?? nullptr;
    if (label != null) ptr.ref.label = StringView(data: label!, length: label!.length).toNative(allocator).ref;
    ptr.ref.formatAsInt = format.value;
    ptr.ref.offset = offset;
    ptr.ref.size = size;

    return ptr;
  }

  factory TexelBufferViewDescriptor.fromNative(bindings.WGPUTexelBufferViewDescriptor v) {
    return TexelBufferViewDescriptor(
      next: ChainedStruct.fromNative(v.nextInChain),
      label: StringView.fromNative(v.label).toDartString(),
      format: TextureFormat.fromValue(v.formatAsInt),
      offset: v.offset,
      size: v.size,
    );
  }
}

class TexelCopyBufferLayout {
  const TexelCopyBufferLayout({
    this.offset = 0,
    this.bytesPerRow = bindings.WGPU_COPY_STRIDE_UNDEFINED,
    this.rowsPerImage = bindings.WGPU_COPY_STRIDE_UNDEFINED,
  });

  final int offset;
  final int bytesPerRow;
  final int rowsPerImage;

  Pointer<bindings.WGPUTexelCopyBufferLayout> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPUTexelCopyBufferLayout>();
    ptr.ref.offset = offset;
    ptr.ref.bytesPerRow = bytesPerRow;
    ptr.ref.rowsPerImage = rowsPerImage;

    return ptr;
  }

  factory TexelCopyBufferLayout.fromNative(bindings.WGPUTexelCopyBufferLayout v) {
    return TexelCopyBufferLayout(
      offset: v.offset,
      bytesPerRow: v.bytesPerRow,
      rowsPerImage: v.rowsPerImage,
    );
  }
}

class TextureBindingLayout {
  const TextureBindingLayout({
    this.next,
    this.sampleType = TextureSampleType.bindingNotUsed,
    this.viewDimension = TextureViewDimension.undefined,
    this.multisampled = false,
  });

  final ChainedStruct? next;
  final TextureSampleType sampleType;
  final TextureViewDimension viewDimension;
  final bool multisampled;

  Pointer<bindings.WGPUTextureBindingLayout> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPUTextureBindingLayout>();
    ptr.ref.nextInChain = next?.toNative(allocator).cast() ?? nullptr;
    ptr.ref.sampleTypeAsInt = sampleType.value;
    ptr.ref.viewDimensionAsInt = viewDimension.value;
    ptr.ref.multisampled = (multisampled ? 1 : 0);

    return ptr;
  }

  factory TextureBindingLayout.fromNative(bindings.WGPUTextureBindingLayout v) {
    return TextureBindingLayout(
      next: ChainedStruct.fromNative(v.nextInChain),
      sampleType: TextureSampleType.fromValue(v.sampleTypeAsInt),
      viewDimension: TextureViewDimension.fromValue(v.viewDimensionAsInt),
      multisampled: (v.multisampled != 0),
    );
  }
}

class TextureBindingViewDimension extends ChainedStruct {
  const TextureBindingViewDimension({
    this.textureBindingViewDimension = TextureViewDimension.undefined,
    super.next,
  });

  final TextureViewDimension textureBindingViewDimension;

  @override
  Pointer<bindings.WGPUTextureBindingViewDimension> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPUTextureBindingViewDimension>();
    ptr.ref.chain.sTypeAsInt = sType.value;
    ptr.ref.chain.next = next?.toNative(allocator).cast() ?? nullptr;
    ptr.ref.textureBindingViewDimensionAsInt = textureBindingViewDimension.value;

    return ptr;
  }

  factory TextureBindingViewDimension.fromNative(bindings.WGPUTextureBindingViewDimension v) {
    return TextureBindingViewDimension(
      textureBindingViewDimension: TextureViewDimension.fromValue(v.textureBindingViewDimensionAsInt),
    );
  }

  @override
  SType get sType => .textureBindingViewDimension;
}

class TextureComponentSwizzle {
  const TextureComponentSwizzle({
    this.r = ComponentSwizzle.undefined,
    this.g = ComponentSwizzle.undefined,
    this.b = ComponentSwizzle.undefined,
    this.a = ComponentSwizzle.undefined,
  });

  final ComponentSwizzle r;
  final ComponentSwizzle g;
  final ComponentSwizzle b;
  final ComponentSwizzle a;

  Pointer<bindings.WGPUTextureComponentSwizzle> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPUTextureComponentSwizzle>();
    ptr.ref.rAsInt = r.value;
    ptr.ref.gAsInt = g.value;
    ptr.ref.bAsInt = b.value;
    ptr.ref.aAsInt = a.value;

    return ptr;
  }

  factory TextureComponentSwizzle.fromNative(bindings.WGPUTextureComponentSwizzle v) {
    return TextureComponentSwizzle(
      r: ComponentSwizzle.fromValue(v.rAsInt),
      g: ComponentSwizzle.fromValue(v.gAsInt),
      b: ComponentSwizzle.fromValue(v.bAsInt),
      a: ComponentSwizzle.fromValue(v.aAsInt),
    );
  }
}

class VertexAttribute {
  const VertexAttribute({
    this.next,
    this.format = VertexFormat.uint8,
    this.offset = 0,
    this.shaderLocation = 0,
  });

  final ChainedStruct? next;
  final VertexFormat format;
  final int offset;
  final int shaderLocation;

  Pointer<bindings.WGPUVertexAttribute> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPUVertexAttribute>();
    ptr.ref.nextInChain = next?.toNative(allocator).cast() ?? nullptr;
    ptr.ref.formatAsInt = format.value;
    ptr.ref.offset = offset;
    ptr.ref.shaderLocation = shaderLocation;

    return ptr;
  }

  factory VertexAttribute.fromNative(bindings.WGPUVertexAttribute v) {
    return VertexAttribute(
      next: ChainedStruct.fromNative(v.nextInChain),
      format: VertexFormat.fromValue(v.formatAsInt),
      offset: v.offset,
      shaderLocation: v.shaderLocation,
    );
  }
}

class YCbCrVkDescriptor extends ChainedStruct {
  const YCbCrVkDescriptor({
    this.vkFormat = 0,
    this.vkYCbCrModel = 0,
    this.vkYCbCrRange = 0,
    this.vkComponentSwizzleRed = 0,
    this.vkComponentSwizzleGreen = 0,
    this.vkComponentSwizzleBlue = 0,
    this.vkComponentSwizzleAlpha = 0,
    this.vkXChromaOffset = 0,
    this.vkYChromaOffset = 0,
    this.vkChromaFilter = FilterMode.undefined,
    this.forceExplicitReconstruction = false,
    this.externalFormat = 0,
    super.next,
  });

  final int vkFormat;
  final int vkYCbCrModel;
  final int vkYCbCrRange;
  final int vkComponentSwizzleRed;
  final int vkComponentSwizzleGreen;
  final int vkComponentSwizzleBlue;
  final int vkComponentSwizzleAlpha;
  final int vkXChromaOffset;
  final int vkYChromaOffset;
  final FilterMode vkChromaFilter;
  final bool forceExplicitReconstruction;
  final int externalFormat;

  @override
  Pointer<bindings.WGPUYCbCrVkDescriptor> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPUYCbCrVkDescriptor>();
    ptr.ref.chain.sTypeAsInt = sType.value;
    ptr.ref.chain.next = next?.toNative(allocator).cast() ?? nullptr;
    ptr.ref.vkFormat = vkFormat;
    ptr.ref.vkYCbCrModel = vkYCbCrModel;
    ptr.ref.vkYCbCrRange = vkYCbCrRange;
    ptr.ref.vkComponentSwizzleRed = vkComponentSwizzleRed;
    ptr.ref.vkComponentSwizzleGreen = vkComponentSwizzleGreen;
    ptr.ref.vkComponentSwizzleBlue = vkComponentSwizzleBlue;
    ptr.ref.vkComponentSwizzleAlpha = vkComponentSwizzleAlpha;
    ptr.ref.vkXChromaOffset = vkXChromaOffset;
    ptr.ref.vkYChromaOffset = vkYChromaOffset;
    ptr.ref.vkChromaFilterAsInt = vkChromaFilter.value;
    ptr.ref.forceExplicitReconstruction = (forceExplicitReconstruction ? 1 : 0);
    ptr.ref.externalFormat = externalFormat;

    return ptr;
  }

  factory YCbCrVkDescriptor.fromNative(bindings.WGPUYCbCrVkDescriptor v) {
    return YCbCrVkDescriptor(
      vkFormat: v.vkFormat,
      vkYCbCrModel: v.vkYCbCrModel,
      vkYCbCrRange: v.vkYCbCrRange,
      vkComponentSwizzleRed: v.vkComponentSwizzleRed,
      vkComponentSwizzleGreen: v.vkComponentSwizzleGreen,
      vkComponentSwizzleBlue: v.vkComponentSwizzleBlue,
      vkComponentSwizzleAlpha: v.vkComponentSwizzleAlpha,
      vkXChromaOffset: v.vkXChromaOffset,
      vkYChromaOffset: v.vkYChromaOffset,
      vkChromaFilter: FilterMode.fromValue(v.vkChromaFilterAsInt),
      forceExplicitReconstruction: (v.forceExplicitReconstruction != 0),
      externalFormat: v.externalFormat,
    );
  }

  @override
  SType get sType => .yCbCrVkDescriptor;
}

class AdapterPropertiesMemoryHeaps extends ChainedStruct {
  const AdapterPropertiesMemoryHeaps({
    this.heapCount = 0,
    required this.heapInfo,
    super.next,
  });

  final int heapCount;
  final MemoryHeapInfo heapInfo;

  @override
  Pointer<bindings.WGPUAdapterPropertiesMemoryHeaps> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPUAdapterPropertiesMemoryHeaps>();
    ptr.ref.chain.sTypeAsInt = sType.value;
    ptr.ref.chain.next = next?.toNative(allocator).cast() ?? nullptr;
    ptr.ref.heapCount = heapCount;
    ptr.ref.heapInfo = heapInfo.toNative(allocator);

    return ptr;
  }

  factory AdapterPropertiesMemoryHeaps.fromNative(bindings.WGPUAdapterPropertiesMemoryHeaps v) {
    return AdapterPropertiesMemoryHeaps(
      heapCount: v.heapCount,
      heapInfo: MemoryHeapInfo.fromNative(v.heapInfo.ref),
    );
  }

  @override
  SType get sType => .adapterPropertiesMemoryHeaps;
}

class AdapterPropertiesSubgroupMatrixConfigs extends ChainedStruct {
  const AdapterPropertiesSubgroupMatrixConfigs({
    this.configs = const [],
    super.next,
  });

  final List<SubgroupMatrixConfig> configs;

  @override
  Pointer<bindings.WGPUAdapterPropertiesSubgroupMatrixConfigs> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPUAdapterPropertiesSubgroupMatrixConfigs>();
    ptr.ref.chain.sTypeAsInt = sType.value;
    ptr.ref.chain.next = next?.toNative(allocator).cast() ?? nullptr;
    if (configs.isNotEmpty) {
      final arrayPtr = allocator<bindings.WGPUSubgroupMatrixConfig>(configs.length);
      for (var i = 0; i < configs.length; i++) {
        arrayPtr[i] = configs[i].toNative(allocator).ref;
      }
      ptr.ref.configs = arrayPtr;
      ptr.ref.configCount = configs.length;
    } else {
      ptr.ref.configs = nullptr;
      ptr.ref.configCount = 0;
    }

    return ptr;
  }

  factory AdapterPropertiesSubgroupMatrixConfigs.fromNative(bindings.WGPUAdapterPropertiesSubgroupMatrixConfigs v) {
    final _configs = <SubgroupMatrixConfig>[];
    if (v.configs != nullptr && v.configCount > 0) {
      for (var i = 0; i < v.configCount; i++) {
        _configs.add(SubgroupMatrixConfig.fromNative(v.configs[i]));
      }
    }
    return AdapterPropertiesSubgroupMatrixConfigs(
      configs: _configs,
    );
  }

  @override
  SType get sType => .adapterPropertiesSubgroupMatrixConfigs;
}

class AHardwareBufferProperties {
  const AHardwareBufferProperties({
    this.yCbCrInfo,
  });

  final YCbCrVkDescriptor? yCbCrInfo;

  Pointer<bindings.WGPUAHardwareBufferProperties> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPUAHardwareBufferProperties>();
    if (yCbCrInfo != null) ptr.ref.yCbCrInfo = yCbCrInfo!.toNative(allocator).ref;

    return ptr;
  }

  factory AHardwareBufferProperties.fromNative(bindings.WGPUAHardwareBufferProperties v) {
    return AHardwareBufferProperties(
      yCbCrInfo: YCbCrVkDescriptor.fromNative(v.yCbCrInfo),
    );
  }
}

class BindGroupEntry {
  const BindGroupEntry({
    this.next,
    this.binding = 0,
    this.buffer,
    this.offset = 0,
    this.size = 0,
    this.sampler,
    this.textureView,
  });

  final ChainedStruct? next;
  final int binding;
  final Buffer? buffer;
  final int offset;
  final int size;
  final Sampler? sampler;
  final TextureView? textureView;

  Pointer<bindings.WGPUBindGroupEntry> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPUBindGroupEntry>();
    ptr.ref.nextInChain = next?.toNative(allocator).cast() ?? nullptr;
    ptr.ref.binding = binding;
    ptr.ref.buffer = (buffer?._ptr ?? nullptr);
    ptr.ref.offset = offset;
    ptr.ref.size = size;
    ptr.ref.sampler = (sampler?._ptr ?? nullptr);
    ptr.ref.textureView = (textureView?._ptr ?? nullptr);

    return ptr;
  }

  factory BindGroupEntry.fromNative(bindings.WGPUBindGroupEntry v) {
    return BindGroupEntry(
      next: ChainedStruct.fromNative(v.nextInChain),
      binding: v.binding,
      buffer: (v.buffer != nullptr ? Buffer._(v.buffer) : null),
      offset: v.offset,
      size: v.size,
      sampler: (v.sampler != nullptr ? Sampler._(v.sampler) : null),
      textureView: (v.textureView != nullptr ? TextureView._(v.textureView) : null),
    );
  }
}

class BindGroupLayoutEntry {
  const BindGroupLayoutEntry({
    this.next,
    this.binding = 0,
    this.visibility = const ShaderStage(0),
    this.bindingArraySize = 0,
    this.buffer,
    this.sampler,
    this.texture,
    this.storageTexture,
  });

  final ChainedStruct? next;
  final int binding;
  final ShaderStage visibility;
  final int bindingArraySize;
  final BufferBindingLayout? buffer;
  final SamplerBindingLayout? sampler;
  final TextureBindingLayout? texture;
  final StorageTextureBindingLayout? storageTexture;

  Pointer<bindings.WGPUBindGroupLayoutEntry> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPUBindGroupLayoutEntry>();
    ptr.ref.nextInChain = next?.toNative(allocator).cast() ?? nullptr;
    ptr.ref.binding = binding;
    ptr.ref.visibility = visibility.value;
    ptr.ref.bindingArraySize = bindingArraySize;
    if (buffer != null) ptr.ref.buffer = buffer!.toNative(allocator).ref;
    if (sampler != null) ptr.ref.sampler = sampler!.toNative(allocator).ref;
    if (texture != null) ptr.ref.texture = texture!.toNative(allocator).ref;
    if (storageTexture != null) ptr.ref.storageTexture = storageTexture!.toNative(allocator).ref;

    return ptr;
  }

  factory BindGroupLayoutEntry.fromNative(bindings.WGPUBindGroupLayoutEntry v) {
    return BindGroupLayoutEntry(
      next: ChainedStruct.fromNative(v.nextInChain),
      binding: v.binding,
      visibility: ShaderStage(v.visibility),
      bindingArraySize: v.bindingArraySize,
      buffer: BufferBindingLayout.fromNative(v.buffer),
      sampler: SamplerBindingLayout.fromNative(v.sampler),
      texture: TextureBindingLayout.fromNative(v.texture),
      storageTexture: StorageTextureBindingLayout.fromNative(v.storageTexture),
    );
  }
}

class BlendState {
  const BlendState({
    this.color,
    this.alpha,
  });

  final BlendComponent? color;
  final BlendComponent? alpha;

  Pointer<bindings.WGPUBlendState> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPUBlendState>();
    if (color != null) ptr.ref.color = color!.toNative(allocator).ref;
    if (alpha != null) ptr.ref.alpha = alpha!.toNative(allocator).ref;

    return ptr;
  }

  factory BlendState.fromNative(bindings.WGPUBlendState v) {
    return BlendState(
      color: BlendComponent.fromNative(v.color),
      alpha: BlendComponent.fromNative(v.alpha),
    );
  }
}

class BufferDescriptor {
  const BufferDescriptor({
    this.next,
    this.label,
    this.usage = const BufferUsage(0),
    this.size = 0,
    this.mappedAtCreation = false,
  });

  final ChainedStruct? next;
  final String? label;
  final BufferUsage usage;
  final int size;
  final bool mappedAtCreation;

  Pointer<bindings.WGPUBufferDescriptor> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPUBufferDescriptor>();
    ptr.ref.nextInChain = next?.toNative(allocator).cast() ?? nullptr;
    if (label != null) ptr.ref.label = StringView(data: label!, length: label!.length).toNative(allocator).ref;
    ptr.ref.usage = usage.value;
    ptr.ref.size = size;
    ptr.ref.mappedAtCreation = (mappedAtCreation ? 1 : 0);

    return ptr;
  }

  factory BufferDescriptor.fromNative(bindings.WGPUBufferDescriptor v) {
    return BufferDescriptor(
      next: ChainedStruct.fromNative(v.nextInChain),
      label: StringView.fromNative(v.label).toDartString(),
      usage: BufferUsage(v.usage),
      size: v.size,
      mappedAtCreation: (v.mappedAtCreation != 0),
    );
  }
}

class CommandEncoderDescriptor {
  const CommandEncoderDescriptor({
    this.next,
    this.label,
  });

  final ChainedStruct? next;
  final String? label;

  Pointer<bindings.WGPUCommandEncoderDescriptor> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPUCommandEncoderDescriptor>();
    ptr.ref.nextInChain = next?.toNative(allocator).cast() ?? nullptr;
    if (label != null) ptr.ref.label = StringView(data: label!, length: label!.length).toNative(allocator).ref;

    return ptr;
  }

  factory CommandEncoderDescriptor.fromNative(bindings.WGPUCommandEncoderDescriptor v) {
    return CommandEncoderDescriptor(
      next: ChainedStruct.fromNative(v.nextInChain),
      label: StringView.fromNative(v.label).toDartString(),
    );
  }
}

class CompilationMessage {
  const CompilationMessage({
    this.next,
    this.message,
    this.type = CompilationMessageType.error,
    this.lineNum = 0,
    this.linePos = 0,
    this.offset = 0,
    this.length = 0,
  });

  final ChainedStruct? next;
  final String? message;
  final CompilationMessageType type;
  final int lineNum;
  final int linePos;
  final int offset;
  final int length;

  Pointer<bindings.WGPUCompilationMessage> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPUCompilationMessage>();
    ptr.ref.nextInChain = next?.toNative(allocator).cast() ?? nullptr;
    if (message != null) ptr.ref.message = StringView(data: message!, length: message!.length).toNative(allocator).ref;
    ptr.ref.typeAsInt = type.value;
    ptr.ref.lineNum = lineNum;
    ptr.ref.linePos = linePos;
    ptr.ref.offset = offset;
    ptr.ref.length = length;

    return ptr;
  }

  factory CompilationMessage.fromNative(bindings.WGPUCompilationMessage v) {
    return CompilationMessage(
      next: ChainedStruct.fromNative(v.nextInChain),
      message: StringView.fromNative(v.message).toDartString(),
      type: CompilationMessageType.fromValue(v.typeAsInt),
      lineNum: v.lineNum,
      linePos: v.linePos,
      offset: v.offset,
      length: v.length,
    );
  }
}

class ComputePassDescriptor {
  const ComputePassDescriptor({
    this.next,
    this.label,
    this.timestampWrites,
  });

  final ChainedStruct? next;
  final String? label;
  final PassTimestampWrites? timestampWrites;

  Pointer<bindings.WGPUComputePassDescriptor> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPUComputePassDescriptor>();
    ptr.ref.nextInChain = next?.toNative(allocator).cast() ?? nullptr;
    if (label != null) ptr.ref.label = StringView(data: label!, length: label!.length).toNative(allocator).ref;
    ptr.ref.timestampWrites = (timestampWrites?.toNative(allocator) ?? nullptr);

    return ptr;
  }

  factory ComputePassDescriptor.fromNative(bindings.WGPUComputePassDescriptor v) {
    return ComputePassDescriptor(
      next: ChainedStruct.fromNative(v.nextInChain),
      label: StringView.fromNative(v.label).toDartString(),
      timestampWrites: (v.timestampWrites != nullptr ? PassTimestampWrites.fromNative(v.timestampWrites.ref) : null),
    );
  }
}

class ComputeState {
  const ComputeState({
    this.next,
    required this.module,
    this.entryPoint,
    this.constants = const [],
  });

  final ChainedStruct? next;
  final ShaderModule module;
  final String? entryPoint;
  final List<ConstantEntry> constants;

  Pointer<bindings.WGPUComputeState> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPUComputeState>();
    ptr.ref.nextInChain = next?.toNative(allocator).cast() ?? nullptr;
    ptr.ref.module = module._ptr;
    if (entryPoint != null) ptr.ref.entryPoint = StringView(data: entryPoint!, length: entryPoint!.length).toNative(allocator).ref;
    if (constants.isNotEmpty) {
      final arrayPtr = allocator<bindings.WGPUConstantEntry>(constants.length);
      for (var i = 0; i < constants.length; i++) {
        arrayPtr[i] = constants[i].toNative(allocator).ref;
      }
      ptr.ref.constants = arrayPtr;
      ptr.ref.constantCount = constants.length;
    } else {
      ptr.ref.constants = nullptr;
      ptr.ref.constantCount = 0;
    }

    return ptr;
  }

  factory ComputeState.fromNative(bindings.WGPUComputeState v) {
    final _constants = <ConstantEntry>[];
    if (v.constants != nullptr && v.constantCount > 0) {
      for (var i = 0; i < v.constantCount; i++) {
        _constants.add(ConstantEntry.fromNative(v.constants[i]));
      }
    }
    return ComputeState(
      next: ChainedStruct.fromNative(v.nextInChain),
      module: ShaderModule._(v.module),
      entryPoint: StringView.fromNative(v.entryPoint).toDartString(),
      constants: _constants,
    );
  }
}

class DawnDrmFormatCapabilities extends ChainedStruct {
  const DawnDrmFormatCapabilities({
    this.properties = const [],
    super.next,
  });

  final List<DawnDrmFormatProperties> properties;

  @override
  Pointer<bindings.WGPUDawnDrmFormatCapabilities> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPUDawnDrmFormatCapabilities>();
    ptr.ref.chain.sTypeAsInt = sType.value;
    ptr.ref.chain.next = next?.toNative(allocator).cast() ?? nullptr;
    if (properties.isNotEmpty) {
      final arrayPtr = allocator<bindings.WGPUDawnDrmFormatProperties>(properties.length);
      for (var i = 0; i < properties.length; i++) {
        arrayPtr[i] = properties[i].toNative(allocator).ref;
      }
      ptr.ref.properties = arrayPtr;
      ptr.ref.propertiesCount = properties.length;
    } else {
      ptr.ref.properties = nullptr;
      ptr.ref.propertiesCount = 0;
    }

    return ptr;
  }

  factory DawnDrmFormatCapabilities.fromNative(bindings.WGPUDawnDrmFormatCapabilities v) {
    final _properties = <DawnDrmFormatProperties>[];
    if (v.properties != nullptr && v.propertiesCount > 0) {
      for (var i = 0; i < v.propertiesCount; i++) {
        _properties.add(DawnDrmFormatProperties.fromNative(v.properties[i]));
      }
    }
    return DawnDrmFormatCapabilities(
      properties: _properties,
    );
  }

  @override
  SType get sType => .dawnDrmFormatCapabilities;
}

class DepthStencilState {
  const DepthStencilState({
    this.next,
    this.format = TextureFormat.undefined,
    this.depthWriteEnabled = OptionalBool.false_,
    this.depthCompare = CompareFunction.undefined,
    this.stencilFront,
    this.stencilBack,
    this.stencilReadMask = 0,
    this.stencilWriteMask = 0,
    this.depthBias = 0,
    this.depthBiasSlopeScale = 0,
    this.depthBiasClamp = 0,
  });

  final ChainedStruct? next;
  final TextureFormat format;
  final OptionalBool depthWriteEnabled;
  final CompareFunction depthCompare;
  final StencilFaceState? stencilFront;
  final StencilFaceState? stencilBack;
  final int stencilReadMask;
  final int stencilWriteMask;
  final int depthBias;
  final double depthBiasSlopeScale;
  final double depthBiasClamp;

  Pointer<bindings.WGPUDepthStencilState> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPUDepthStencilState>();
    ptr.ref.nextInChain = next?.toNative(allocator).cast() ?? nullptr;
    ptr.ref.formatAsInt = format.value;
    ptr.ref.depthWriteEnabledAsInt = depthWriteEnabled.value;
    ptr.ref.depthCompareAsInt = depthCompare.value;
    if (stencilFront != null) ptr.ref.stencilFront = stencilFront!.toNative(allocator).ref;
    if (stencilBack != null) ptr.ref.stencilBack = stencilBack!.toNative(allocator).ref;
    ptr.ref.stencilReadMask = stencilReadMask;
    ptr.ref.stencilWriteMask = stencilWriteMask;
    ptr.ref.depthBias = depthBias;
    ptr.ref.depthBiasSlopeScale = depthBiasSlopeScale;
    ptr.ref.depthBiasClamp = depthBiasClamp;

    return ptr;
  }

  factory DepthStencilState.fromNative(bindings.WGPUDepthStencilState v) {
    return DepthStencilState(
      next: ChainedStruct.fromNative(v.nextInChain),
      format: TextureFormat.fromValue(v.formatAsInt),
      depthWriteEnabled: OptionalBool.fromValue(v.depthWriteEnabledAsInt),
      depthCompare: CompareFunction.fromValue(v.depthCompareAsInt),
      stencilFront: StencilFaceState.fromNative(v.stencilFront),
      stencilBack: StencilFaceState.fromNative(v.stencilBack),
      stencilReadMask: v.stencilReadMask,
      stencilWriteMask: v.stencilWriteMask,
      depthBias: v.depthBias,
      depthBiasSlopeScale: v.depthBiasSlopeScale,
      depthBiasClamp: v.depthBiasClamp,
    );
  }
}

class ExternalTextureDescriptor {
  const ExternalTextureDescriptor({
    this.next,
    this.label,
    required this.plane0,
    this.plane1,
    this.cropOrigin,
    this.cropSize,
    this.apparentSize,
    this.doYuvToRgbConversionOnly = false,
    this.yuvToRgbConversionMatrix,
    required this.srcTransferFunctionParameters,
    required this.dstTransferFunctionParameters,
    required this.gamutConversionMatrix,
    this.mirrored = false,
    this.rotation = ExternalTextureRotation.rotate0Degrees,
  });

  final ChainedStruct? next;
  final String? label;
  final TextureView plane0;
  final TextureView? plane1;
  final Origin2D? cropOrigin;
  final Extent2D? cropSize;
  final Extent2D? apparentSize;
  final bool doYuvToRgbConversionOnly;
  final Pointer<Float>? yuvToRgbConversionMatrix;
  final Pointer<Float> srcTransferFunctionParameters;
  final Pointer<Float> dstTransferFunctionParameters;
  final Pointer<Float> gamutConversionMatrix;
  final bool mirrored;
  final ExternalTextureRotation rotation;

  Pointer<bindings.WGPUExternalTextureDescriptor> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPUExternalTextureDescriptor>();
    ptr.ref.nextInChain = next?.toNative(allocator).cast() ?? nullptr;
    if (label != null) ptr.ref.label = StringView(data: label!, length: label!.length).toNative(allocator).ref;
    ptr.ref.plane0 = plane0._ptr;
    ptr.ref.plane1 = (plane1?._ptr ?? nullptr);
    if (cropOrigin != null) ptr.ref.cropOrigin = cropOrigin!.toNative(allocator).ref;
    if (cropSize != null) ptr.ref.cropSize = cropSize!.toNative(allocator).ref;
    if (apparentSize != null) ptr.ref.apparentSize = apparentSize!.toNative(allocator).ref;
    ptr.ref.doYuvToRgbConversionOnly = (doYuvToRgbConversionOnly ? 1 : 0);
    ptr.ref.yuvToRgbConversionMatrix = (yuvToRgbConversionMatrix ?? nullptr);
    ptr.ref.srcTransferFunctionParameters = srcTransferFunctionParameters;
    ptr.ref.dstTransferFunctionParameters = dstTransferFunctionParameters;
    ptr.ref.gamutConversionMatrix = gamutConversionMatrix;
    ptr.ref.mirrored = (mirrored ? 1 : 0);
    ptr.ref.rotationAsInt = rotation.value;

    return ptr;
  }

  factory ExternalTextureDescriptor.fromNative(bindings.WGPUExternalTextureDescriptor v) {
    return ExternalTextureDescriptor(
      next: ChainedStruct.fromNative(v.nextInChain),
      label: StringView.fromNative(v.label).toDartString(),
      plane0: TextureView._(v.plane0),
      plane1: (v.plane1 != nullptr ? TextureView._(v.plane1) : null),
      cropOrigin: Origin2D.fromNative(v.cropOrigin),
      cropSize: Extent2D.fromNative(v.cropSize),
      apparentSize: Extent2D.fromNative(v.apparentSize),
      doYuvToRgbConversionOnly: (v.doYuvToRgbConversionOnly != 0),
      yuvToRgbConversionMatrix: (v.yuvToRgbConversionMatrix != nullptr ? v.yuvToRgbConversionMatrix : null),
      srcTransferFunctionParameters: v.srcTransferFunctionParameters,
      dstTransferFunctionParameters: v.dstTransferFunctionParameters,
      gamutConversionMatrix: v.gamutConversionMatrix,
      mirrored: (v.mirrored != 0),
      rotation: ExternalTextureRotation.fromValue(v.rotationAsInt),
    );
  }
}

class FutureWaitInfo {
  const FutureWaitInfo({
    this.future,
    this.completed = false,
  });

  final WGPUFuture? future;
  final bool completed;

  Pointer<bindings.WGPUFutureWaitInfo> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPUFutureWaitInfo>();
    if (future != null) ptr.ref.future = future!.toNative(allocator).ref;
    ptr.ref.completed = (completed ? 1 : 0);

    return ptr;
  }

  factory FutureWaitInfo.fromNative(bindings.WGPUFutureWaitInfo v) {
    return FutureWaitInfo(
      future: WGPUFuture.fromNative(v.future),
      completed: (v.completed != 0),
    );
  }
}

class ImageCopyExternalTexture {
  const ImageCopyExternalTexture({
    this.next,
    required this.externalTexture,
    this.origin,
    this.naturalSize,
  });

  final ChainedStruct? next;
  final ExternalTexture externalTexture;
  final Origin3D? origin;
  final Extent2D? naturalSize;

  Pointer<bindings.WGPUImageCopyExternalTexture> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPUImageCopyExternalTexture>();
    ptr.ref.nextInChain = next?.toNative(allocator).cast() ?? nullptr;
    ptr.ref.externalTexture = externalTexture._ptr;
    if (origin != null) ptr.ref.origin = origin!.toNative(allocator).ref;
    if (naturalSize != null) ptr.ref.naturalSize = naturalSize!.toNative(allocator).ref;

    return ptr;
  }

  factory ImageCopyExternalTexture.fromNative(bindings.WGPUImageCopyExternalTexture v) {
    return ImageCopyExternalTexture(
      next: ChainedStruct.fromNative(v.nextInChain),
      externalTexture: ExternalTexture._(v.externalTexture),
      origin: Origin3D.fromNative(v.origin),
      naturalSize: Extent2D.fromNative(v.naturalSize),
    );
  }
}

class InstanceDescriptor {
  const InstanceDescriptor({
    this.next,
    this.requiredFeatures = const [],
    this.requiredLimits,
  });

  final ChainedStruct? next;
  final List<InstanceFeatureName> requiredFeatures;
  final InstanceLimits? requiredLimits;

  Pointer<bindings.WGPUInstanceDescriptor> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPUInstanceDescriptor>();
    ptr.ref.nextInChain = next?.toNative(allocator).cast() ?? nullptr;
    if (requiredFeatures.isNotEmpty) {
      final arrayPtr = allocator<UnsignedInt>(requiredFeatures.length);
      for (var i = 0; i < requiredFeatures.length; i++) {
        arrayPtr[i] = requiredFeatures[i].value;
      }
      ptr.ref.requiredFeatures = arrayPtr;
      ptr.ref.requiredFeatureCount = requiredFeatures.length;
    } else {
      ptr.ref.requiredFeatures = nullptr;
      ptr.ref.requiredFeatureCount = 0;
    }

    ptr.ref.requiredLimits = (requiredLimits?.toNative(allocator) ?? nullptr);

    return ptr;
  }

  factory InstanceDescriptor.fromNative(bindings.WGPUInstanceDescriptor v) {
    final _requiredFeatures = <InstanceFeatureName>[];
    if (v.requiredFeatures != nullptr && v.requiredFeatureCount > 0) {
      for (var i = 0; i < v.requiredFeatureCount; i++) {
        _requiredFeatures.add(InstanceFeatureName.fromValue(v.requiredFeatures[i]));
      }
    }
    return InstanceDescriptor(
      next: ChainedStruct.fromNative(v.nextInChain),
      requiredFeatures: _requiredFeatures,
      requiredLimits: (v.requiredLimits != nullptr ? InstanceLimits.fromNative(v.requiredLimits.ref) : null),
    );
  }
}

class Limits {
  const Limits({
    this.next,
    this.maxTextureDimension1D = 0,
    this.maxTextureDimension2D = 0,
    this.maxTextureDimension3D = 0,
    this.maxTextureArrayLayers = 0,
    this.maxBindGroups = 0,
    this.maxBindGroupsPlusVertexBuffers = 0,
    this.maxBindingsPerBindGroup = 0,
    this.maxDynamicUniformBuffersPerPipelineLayout = 0,
    this.maxDynamicStorageBuffersPerPipelineLayout = 0,
    this.maxSampledTexturesPerShaderStage = 0,
    this.maxSamplersPerShaderStage = 0,
    this.maxStorageBuffersPerShaderStage = 0,
    this.maxStorageTexturesPerShaderStage = 0,
    this.maxUniformBuffersPerShaderStage = 0,
    this.maxUniformBufferBindingSize = 0,
    this.maxStorageBufferBindingSize = 0,
    this.minUniformBufferOffsetAlignment = 0,
    this.minStorageBufferOffsetAlignment = 0,
    this.maxVertexBuffers = 0,
    this.maxBufferSize = 0,
    this.maxVertexAttributes = 0,
    this.maxVertexBufferArrayStride = 0,
    this.maxInterStageShaderVariables = 0,
    this.maxColorAttachments = 0,
    this.maxColorAttachmentBytesPerSample = 0,
    this.maxComputeWorkgroupStorageSize = 0,
    this.maxComputeInvocationsPerWorkgroup = 0,
    this.maxComputeWorkgroupSizeX = 0,
    this.maxComputeWorkgroupSizeY = 0,
    this.maxComputeWorkgroupSizeZ = 0,
    this.maxComputeWorkgroupsPerDimension = 0,
    this.maxImmediateSize = 0,
  });

  final ChainedStruct? next;
  final int maxTextureDimension1D;
  final int maxTextureDimension2D;
  final int maxTextureDimension3D;
  final int maxTextureArrayLayers;
  final int maxBindGroups;
  final int maxBindGroupsPlusVertexBuffers;
  final int maxBindingsPerBindGroup;
  final int maxDynamicUniformBuffersPerPipelineLayout;
  final int maxDynamicStorageBuffersPerPipelineLayout;
  final int maxSampledTexturesPerShaderStage;
  final int maxSamplersPerShaderStage;
  final int maxStorageBuffersPerShaderStage;
  final int maxStorageTexturesPerShaderStage;
  final int maxUniformBuffersPerShaderStage;
  final int maxUniformBufferBindingSize;
  final int maxStorageBufferBindingSize;
  final int minUniformBufferOffsetAlignment;
  final int minStorageBufferOffsetAlignment;
  final int maxVertexBuffers;
  final int maxBufferSize;
  final int maxVertexAttributes;
  final int maxVertexBufferArrayStride;
  final int maxInterStageShaderVariables;
  final int maxColorAttachments;
  final int maxColorAttachmentBytesPerSample;
  final int maxComputeWorkgroupStorageSize;
  final int maxComputeInvocationsPerWorkgroup;
  final int maxComputeWorkgroupSizeX;
  final int maxComputeWorkgroupSizeY;
  final int maxComputeWorkgroupSizeZ;
  final int maxComputeWorkgroupsPerDimension;
  final int maxImmediateSize;

  Pointer<bindings.WGPULimits> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPULimits>();
    ptr.ref.nextInChain = next?.toNative(allocator).cast() ?? nullptr;
    ptr.ref.maxTextureDimension1D = maxTextureDimension1D;
    ptr.ref.maxTextureDimension2D = maxTextureDimension2D;
    ptr.ref.maxTextureDimension3D = maxTextureDimension3D;
    ptr.ref.maxTextureArrayLayers = maxTextureArrayLayers;
    ptr.ref.maxBindGroups = maxBindGroups;
    ptr.ref.maxBindGroupsPlusVertexBuffers = maxBindGroupsPlusVertexBuffers;
    ptr.ref.maxBindingsPerBindGroup = maxBindingsPerBindGroup;
    ptr.ref.maxDynamicUniformBuffersPerPipelineLayout = maxDynamicUniformBuffersPerPipelineLayout;
    ptr.ref.maxDynamicStorageBuffersPerPipelineLayout = maxDynamicStorageBuffersPerPipelineLayout;
    ptr.ref.maxSampledTexturesPerShaderStage = maxSampledTexturesPerShaderStage;
    ptr.ref.maxSamplersPerShaderStage = maxSamplersPerShaderStage;
    ptr.ref.maxStorageBuffersPerShaderStage = maxStorageBuffersPerShaderStage;
    ptr.ref.maxStorageTexturesPerShaderStage = maxStorageTexturesPerShaderStage;
    ptr.ref.maxUniformBuffersPerShaderStage = maxUniformBuffersPerShaderStage;
    ptr.ref.maxUniformBufferBindingSize = maxUniformBufferBindingSize;
    ptr.ref.maxStorageBufferBindingSize = maxStorageBufferBindingSize;
    ptr.ref.minUniformBufferOffsetAlignment = minUniformBufferOffsetAlignment;
    ptr.ref.minStorageBufferOffsetAlignment = minStorageBufferOffsetAlignment;
    ptr.ref.maxVertexBuffers = maxVertexBuffers;
    ptr.ref.maxBufferSize = maxBufferSize;
    ptr.ref.maxVertexAttributes = maxVertexAttributes;
    ptr.ref.maxVertexBufferArrayStride = maxVertexBufferArrayStride;
    ptr.ref.maxInterStageShaderVariables = maxInterStageShaderVariables;
    ptr.ref.maxColorAttachments = maxColorAttachments;
    ptr.ref.maxColorAttachmentBytesPerSample = maxColorAttachmentBytesPerSample;
    ptr.ref.maxComputeWorkgroupStorageSize = maxComputeWorkgroupStorageSize;
    ptr.ref.maxComputeInvocationsPerWorkgroup = maxComputeInvocationsPerWorkgroup;
    ptr.ref.maxComputeWorkgroupSizeX = maxComputeWorkgroupSizeX;
    ptr.ref.maxComputeWorkgroupSizeY = maxComputeWorkgroupSizeY;
    ptr.ref.maxComputeWorkgroupSizeZ = maxComputeWorkgroupSizeZ;
    ptr.ref.maxComputeWorkgroupsPerDimension = maxComputeWorkgroupsPerDimension;
    ptr.ref.maxImmediateSize = maxImmediateSize;

    return ptr;
  }

  factory Limits.fromNative(bindings.WGPULimits v) {
    return Limits(
      next: ChainedStruct.fromNative(v.nextInChain),
      maxTextureDimension1D: v.maxTextureDimension1D,
      maxTextureDimension2D: v.maxTextureDimension2D,
      maxTextureDimension3D: v.maxTextureDimension3D,
      maxTextureArrayLayers: v.maxTextureArrayLayers,
      maxBindGroups: v.maxBindGroups,
      maxBindGroupsPlusVertexBuffers: v.maxBindGroupsPlusVertexBuffers,
      maxBindingsPerBindGroup: v.maxBindingsPerBindGroup,
      maxDynamicUniformBuffersPerPipelineLayout: v.maxDynamicUniformBuffersPerPipelineLayout,
      maxDynamicStorageBuffersPerPipelineLayout: v.maxDynamicStorageBuffersPerPipelineLayout,
      maxSampledTexturesPerShaderStage: v.maxSampledTexturesPerShaderStage,
      maxSamplersPerShaderStage: v.maxSamplersPerShaderStage,
      maxStorageBuffersPerShaderStage: v.maxStorageBuffersPerShaderStage,
      maxStorageTexturesPerShaderStage: v.maxStorageTexturesPerShaderStage,
      maxUniformBuffersPerShaderStage: v.maxUniformBuffersPerShaderStage,
      maxUniformBufferBindingSize: v.maxUniformBufferBindingSize,
      maxStorageBufferBindingSize: v.maxStorageBufferBindingSize,
      minUniformBufferOffsetAlignment: v.minUniformBufferOffsetAlignment,
      minStorageBufferOffsetAlignment: v.minStorageBufferOffsetAlignment,
      maxVertexBuffers: v.maxVertexBuffers,
      maxBufferSize: v.maxBufferSize,
      maxVertexAttributes: v.maxVertexAttributes,
      maxVertexBufferArrayStride: v.maxVertexBufferArrayStride,
      maxInterStageShaderVariables: v.maxInterStageShaderVariables,
      maxColorAttachments: v.maxColorAttachments,
      maxColorAttachmentBytesPerSample: v.maxColorAttachmentBytesPerSample,
      maxComputeWorkgroupStorageSize: v.maxComputeWorkgroupStorageSize,
      maxComputeInvocationsPerWorkgroup: v.maxComputeInvocationsPerWorkgroup,
      maxComputeWorkgroupSizeX: v.maxComputeWorkgroupSizeX,
      maxComputeWorkgroupSizeY: v.maxComputeWorkgroupSizeY,
      maxComputeWorkgroupSizeZ: v.maxComputeWorkgroupSizeZ,
      maxComputeWorkgroupsPerDimension: v.maxComputeWorkgroupsPerDimension,
      maxImmediateSize: v.maxImmediateSize,
    );
  }
}

class PipelineLayoutPixelLocalStorage extends ChainedStruct {
  const PipelineLayoutPixelLocalStorage({
    this.totalPixelLocalStorageSize = 0,
    this.storageAttachments = const [],
    super.next,
  });

  final int totalPixelLocalStorageSize;
  final List<PipelineLayoutStorageAttachment> storageAttachments;

  @override
  Pointer<bindings.WGPUPipelineLayoutPixelLocalStorage> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPUPipelineLayoutPixelLocalStorage>();
    ptr.ref.chain.sTypeAsInt = sType.value;
    ptr.ref.chain.next = next?.toNative(allocator).cast() ?? nullptr;
    ptr.ref.totalPixelLocalStorageSize = totalPixelLocalStorageSize;
    if (storageAttachments.isNotEmpty) {
      final arrayPtr = allocator<bindings.WGPUPipelineLayoutStorageAttachment>(storageAttachments.length);
      for (var i = 0; i < storageAttachments.length; i++) {
        arrayPtr[i] = storageAttachments[i].toNative(allocator).ref;
      }
      ptr.ref.storageAttachments = arrayPtr;
      ptr.ref.storageAttachmentCount = storageAttachments.length;
    } else {
      ptr.ref.storageAttachments = nullptr;
      ptr.ref.storageAttachmentCount = 0;
    }

    return ptr;
  }

  factory PipelineLayoutPixelLocalStorage.fromNative(bindings.WGPUPipelineLayoutPixelLocalStorage v) {
    final _storageAttachments = <PipelineLayoutStorageAttachment>[];
    if (v.storageAttachments != nullptr && v.storageAttachmentCount > 0) {
      for (var i = 0; i < v.storageAttachmentCount; i++) {
        _storageAttachments.add(PipelineLayoutStorageAttachment.fromNative(v.storageAttachments[i]));
      }
    }
    return PipelineLayoutPixelLocalStorage(
      totalPixelLocalStorageSize: v.totalPixelLocalStorageSize,
      storageAttachments: _storageAttachments,
    );
  }

  @override
  SType get sType => .pipelineLayoutPixelLocalStorage;
}

class RenderPassColorAttachment {
  const RenderPassColorAttachment({
    this.next,
    this.view,
    this.depthSlice = bindings.WGPU_DEPTH_SLICE_UNDEFINED,
    this.resolveTarget,
    this.loadOp = LoadOp.undefined,
    this.storeOp = StoreOp.undefined,
    this.clearValue,
  });

  final ChainedStruct? next;
  final TextureView? view;
  final int depthSlice;
  final TextureView? resolveTarget;
  final LoadOp loadOp;
  final StoreOp storeOp;
  final Color? clearValue;

  Pointer<bindings.WGPURenderPassColorAttachment> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPURenderPassColorAttachment>();
    ptr.ref.nextInChain = next?.toNative(allocator).cast() ?? nullptr;
    ptr.ref.view = (view?._ptr ?? nullptr);
    ptr.ref.depthSlice = depthSlice;
    ptr.ref.resolveTarget = (resolveTarget?._ptr ?? nullptr);
    ptr.ref.loadOpAsInt = loadOp.value;
    ptr.ref.storeOpAsInt = storeOp.value;
    if (clearValue != null) ptr.ref.clearValue = clearValue!.toNative(allocator).ref;

    return ptr;
  }

  factory RenderPassColorAttachment.fromNative(bindings.WGPURenderPassColorAttachment v) {
    return RenderPassColorAttachment(
      next: ChainedStruct.fromNative(v.nextInChain),
      view: (v.view != nullptr ? TextureView._(v.view) : null),
      depthSlice: v.depthSlice,
      resolveTarget: (v.resolveTarget != nullptr ? TextureView._(v.resolveTarget) : null),
      loadOp: LoadOp.fromValue(v.loadOpAsInt),
      storeOp: StoreOp.fromValue(v.storeOpAsInt),
      clearValue: Color.fromNative(v.clearValue),
    );
  }
}

class RenderPassRenderAreaRect extends ChainedStruct {
  const RenderPassRenderAreaRect({
    this.origin,
    this.size,
    super.next,
  });

  final Origin2D? origin;
  final Extent2D? size;

  @override
  Pointer<bindings.WGPURenderPassRenderAreaRect> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPURenderPassRenderAreaRect>();
    ptr.ref.chain.sTypeAsInt = sType.value;
    ptr.ref.chain.next = next?.toNative(allocator).cast() ?? nullptr;
    if (origin != null) ptr.ref.origin = origin!.toNative(allocator).ref;
    if (size != null) ptr.ref.size = size!.toNative(allocator).ref;

    return ptr;
  }

  factory RenderPassRenderAreaRect.fromNative(bindings.WGPURenderPassRenderAreaRect v) {
    return RenderPassRenderAreaRect(
      origin: Origin2D.fromNative(v.origin),
      size: Extent2D.fromNative(v.size),
    );
  }

  @override
  SType get sType => .renderPassRenderAreaRect;
}

class RenderPassStorageAttachment {
  const RenderPassStorageAttachment({
    this.next,
    this.offset = 0,
    required this.storage,
    this.loadOp = LoadOp.undefined,
    this.storeOp = StoreOp.undefined,
    this.clearValue,
  });

  final ChainedStruct? next;
  final int offset;
  final TextureView storage;
  final LoadOp loadOp;
  final StoreOp storeOp;
  final Color? clearValue;

  Pointer<bindings.WGPURenderPassStorageAttachment> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPURenderPassStorageAttachment>();
    ptr.ref.nextInChain = next?.toNative(allocator).cast() ?? nullptr;
    ptr.ref.offset = offset;
    ptr.ref.storage = storage._ptr;
    ptr.ref.loadOpAsInt = loadOp.value;
    ptr.ref.storeOpAsInt = storeOp.value;
    if (clearValue != null) ptr.ref.clearValue = clearValue!.toNative(allocator).ref;

    return ptr;
  }

  factory RenderPassStorageAttachment.fromNative(bindings.WGPURenderPassStorageAttachment v) {
    return RenderPassStorageAttachment(
      next: ChainedStruct.fromNative(v.nextInChain),
      offset: v.offset,
      storage: TextureView._(v.storage),
      loadOp: LoadOp.fromValue(v.loadOpAsInt),
      storeOp: StoreOp.fromValue(v.storeOpAsInt),
      clearValue: Color.fromNative(v.clearValue),
    );
  }
}

class RequestAdapterOptions {
  const RequestAdapterOptions({
    this.next,
    this.featureLevel = FeatureLevel.undefined,
    this.powerPreference = PowerPreference.undefined,
    this.forceFallbackAdapter = false,
    this.backendType = BackendType.undefined,
    this.compatibleSurface,
  });

  final ChainedStruct? next;
  final FeatureLevel featureLevel;
  final PowerPreference powerPreference;
  final bool forceFallbackAdapter;
  final BackendType backendType;
  final Surface? compatibleSurface;

  Pointer<bindings.WGPURequestAdapterOptions> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPURequestAdapterOptions>();
    ptr.ref.nextInChain = next?.toNative(allocator).cast() ?? nullptr;
    ptr.ref.featureLevelAsInt = featureLevel.value;
    ptr.ref.powerPreferenceAsInt = powerPreference.value;
    ptr.ref.forceFallbackAdapter = (forceFallbackAdapter ? 1 : 0);
    ptr.ref.backendTypeAsInt = backendType.value;
    ptr.ref.compatibleSurface = (compatibleSurface?._ptr ?? nullptr);

    return ptr;
  }

  factory RequestAdapterOptions.fromNative(bindings.WGPURequestAdapterOptions v) {
    return RequestAdapterOptions(
      next: ChainedStruct.fromNative(v.nextInChain),
      featureLevel: FeatureLevel.fromValue(v.featureLevelAsInt),
      powerPreference: PowerPreference.fromValue(v.powerPreferenceAsInt),
      forceFallbackAdapter: (v.forceFallbackAdapter != 0),
      backendType: BackendType.fromValue(v.backendTypeAsInt),
      compatibleSurface: (v.compatibleSurface != nullptr ? Surface._(v.compatibleSurface) : null),
    );
  }
}

class SamplerDescriptor {
  const SamplerDescriptor({
    this.next,
    this.label,
    this.addressModeU = AddressMode.undefined,
    this.addressModeV = AddressMode.undefined,
    this.addressModeW = AddressMode.undefined,
    this.magFilter = FilterMode.undefined,
    this.minFilter = FilterMode.undefined,
    this.mipmapFilter = MipmapFilterMode.undefined,
    this.lodMinClamp = 0,
    this.lodMaxClamp = 0,
    this.compare = CompareFunction.undefined,
    this.maxAnisotropy = 0,
  });

  final ChainedStruct? next;
  final String? label;
  final AddressMode addressModeU;
  final AddressMode addressModeV;
  final AddressMode addressModeW;
  final FilterMode magFilter;
  final FilterMode minFilter;
  final MipmapFilterMode mipmapFilter;
  final double lodMinClamp;
  final double lodMaxClamp;
  final CompareFunction compare;
  final int maxAnisotropy;

  Pointer<bindings.WGPUSamplerDescriptor> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPUSamplerDescriptor>();
    ptr.ref.nextInChain = next?.toNative(allocator).cast() ?? nullptr;
    if (label != null) ptr.ref.label = StringView(data: label!, length: label!.length).toNative(allocator).ref;
    ptr.ref.addressModeUAsInt = addressModeU.value;
    ptr.ref.addressModeVAsInt = addressModeV.value;
    ptr.ref.addressModeWAsInt = addressModeW.value;
    ptr.ref.magFilterAsInt = magFilter.value;
    ptr.ref.minFilterAsInt = minFilter.value;
    ptr.ref.mipmapFilterAsInt = mipmapFilter.value;
    ptr.ref.lodMinClamp = lodMinClamp;
    ptr.ref.lodMaxClamp = lodMaxClamp;
    ptr.ref.compareAsInt = compare.value;
    ptr.ref.maxAnisotropy = maxAnisotropy;

    return ptr;
  }

  factory SamplerDescriptor.fromNative(bindings.WGPUSamplerDescriptor v) {
    return SamplerDescriptor(
      next: ChainedStruct.fromNative(v.nextInChain),
      label: StringView.fromNative(v.label).toDartString(),
      addressModeU: AddressMode.fromValue(v.addressModeUAsInt),
      addressModeV: AddressMode.fromValue(v.addressModeVAsInt),
      addressModeW: AddressMode.fromValue(v.addressModeWAsInt),
      magFilter: FilterMode.fromValue(v.magFilterAsInt),
      minFilter: FilterMode.fromValue(v.minFilterAsInt),
      mipmapFilter: MipmapFilterMode.fromValue(v.mipmapFilterAsInt),
      lodMinClamp: v.lodMinClamp,
      lodMaxClamp: v.lodMaxClamp,
      compare: CompareFunction.fromValue(v.compareAsInt),
      maxAnisotropy: v.maxAnisotropy,
    );
  }
}

class ShaderModuleDescriptor {
  const ShaderModuleDescriptor({
    this.next,
    this.label,
  });

  final ChainedStruct? next;
  final String? label;

  Pointer<bindings.WGPUShaderModuleDescriptor> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPUShaderModuleDescriptor>();
    ptr.ref.nextInChain = next?.toNative(allocator).cast() ?? nullptr;
    if (label != null) ptr.ref.label = StringView(data: label!, length: label!.length).toNative(allocator).ref;

    return ptr;
  }

  factory ShaderModuleDescriptor.fromNative(bindings.WGPUShaderModuleDescriptor v) {
    return ShaderModuleDescriptor(
      next: ChainedStruct.fromNative(v.nextInChain),
      label: StringView.fromNative(v.label).toDartString(),
    );
  }
}

class SharedBufferMemoryDescriptor {
  const SharedBufferMemoryDescriptor({
    this.next,
    this.label,
  });

  final ChainedStruct? next;
  final String? label;

  Pointer<bindings.WGPUSharedBufferMemoryDescriptor> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPUSharedBufferMemoryDescriptor>();
    ptr.ref.nextInChain = next?.toNative(allocator).cast() ?? nullptr;
    if (label != null) ptr.ref.label = StringView(data: label!, length: label!.length).toNative(allocator).ref;

    return ptr;
  }

  factory SharedBufferMemoryDescriptor.fromNative(bindings.WGPUSharedBufferMemoryDescriptor v) {
    return SharedBufferMemoryDescriptor(
      next: ChainedStruct.fromNative(v.nextInChain),
      label: StringView.fromNative(v.label).toDartString(),
    );
  }
}

class SharedFenceDescriptor {
  const SharedFenceDescriptor({
    this.next,
    this.label,
  });

  final ChainedStruct? next;
  final String? label;

  Pointer<bindings.WGPUSharedFenceDescriptor> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPUSharedFenceDescriptor>();
    ptr.ref.nextInChain = next?.toNative(allocator).cast() ?? nullptr;
    if (label != null) ptr.ref.label = StringView(data: label!, length: label!.length).toNative(allocator).ref;

    return ptr;
  }

  factory SharedFenceDescriptor.fromNative(bindings.WGPUSharedFenceDescriptor v) {
    return SharedFenceDescriptor(
      next: ChainedStruct.fromNative(v.nextInChain),
      label: StringView.fromNative(v.label).toDartString(),
    );
  }
}

class SharedFenceExportInfo {
  const SharedFenceExportInfo({
    this.next,
    this.type = SharedFenceType.vkSemaphoreOpaqueFD,
  });

  final ChainedStruct? next;
  final SharedFenceType type;

  Pointer<bindings.WGPUSharedFenceExportInfo> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPUSharedFenceExportInfo>();
    ptr.ref.nextInChain = next?.toNative(allocator).cast() ?? nullptr;
    ptr.ref.typeAsInt = type.value;

    return ptr;
  }

  factory SharedFenceExportInfo.fromNative(bindings.WGPUSharedFenceExportInfo v) {
    return SharedFenceExportInfo(
      next: ChainedStruct.fromNative(v.nextInChain),
      type: SharedFenceType.fromValue(v.typeAsInt),
    );
  }
}

class SharedTextureMemoryAHardwareBufferProperties extends ChainedStruct {
  const SharedTextureMemoryAHardwareBufferProperties({
    this.yCbCrInfo,
    super.next,
  });

  final YCbCrVkDescriptor? yCbCrInfo;

  @override
  Pointer<bindings.WGPUSharedTextureMemoryAHardwareBufferProperties> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPUSharedTextureMemoryAHardwareBufferProperties>();
    ptr.ref.chain.sTypeAsInt = sType.value;
    ptr.ref.chain.next = next?.toNative(allocator).cast() ?? nullptr;
    if (yCbCrInfo != null) ptr.ref.yCbCrInfo = yCbCrInfo!.toNative(allocator).ref;

    return ptr;
  }

  factory SharedTextureMemoryAHardwareBufferProperties.fromNative(bindings.WGPUSharedTextureMemoryAHardwareBufferProperties v) {
    return SharedTextureMemoryAHardwareBufferProperties(
      yCbCrInfo: YCbCrVkDescriptor.fromNative(v.yCbCrInfo),
    );
  }

  @override
  SType get sType => .sharedTextureMemoryAHardwareBufferProperties;
}

class SharedTextureMemoryBeginAccessDescriptor {
  const SharedTextureMemoryBeginAccessDescriptor({
    this.next,
    this.concurrentRead = false,
    this.initialized = false,
    this.fences = const [],
    required this.signaledValues,
  });

  final ChainedStruct? next;
  final bool concurrentRead;
  final bool initialized;
  final List<SharedFence> fences;
  final Pointer<Uint64> signaledValues;

  Pointer<bindings.WGPUSharedTextureMemoryBeginAccessDescriptor> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPUSharedTextureMemoryBeginAccessDescriptor>();
    ptr.ref.nextInChain = next?.toNative(allocator).cast() ?? nullptr;
    ptr.ref.concurrentRead = (concurrentRead ? 1 : 0);
    ptr.ref.initialized = (initialized ? 1 : 0);
    if (fences.isNotEmpty) {
      final arrayPtr = allocator<Pointer<bindings.WGPUSharedFenceImpl>>(fences.length);
      for (var i = 0; i < fences.length; i++) {
        arrayPtr[i] = fences[i]._ptr;
      }
      ptr.ref.fences = arrayPtr;
      ptr.ref.fenceCount = fences.length;
    } else {
      ptr.ref.fences = nullptr;
      ptr.ref.fenceCount = 0;
    }

    ptr.ref.signaledValues = signaledValues;

    return ptr;
  }

  factory SharedTextureMemoryBeginAccessDescriptor.fromNative(bindings.WGPUSharedTextureMemoryBeginAccessDescriptor v) {
    final _fences = <SharedFence>[];
    if (v.fences != nullptr && v.fenceCount > 0) {
      for (var i = 0; i < v.fenceCount; i++) {
        _fences.add(SharedFence._(v.fences[i]));
      }
    }
    return SharedTextureMemoryBeginAccessDescriptor(
      next: ChainedStruct.fromNative(v.nextInChain),
      concurrentRead: (v.concurrentRead != 0),
      initialized: (v.initialized != 0),
      fences: _fences,
      signaledValues: v.signaledValues,
    );
  }
}

class SharedTextureMemoryDmaBufDescriptor extends ChainedStruct {
  const SharedTextureMemoryDmaBufDescriptor({
    this.size,
    this.drmFormat = 0,
    this.drmModifier = 0,
    this.planes = const [],
    super.next,
  });

  final Extent3D? size;
  final int drmFormat;
  final int drmModifier;
  final List<SharedTextureMemoryDmaBufPlane> planes;

  @override
  Pointer<bindings.WGPUSharedTextureMemoryDmaBufDescriptor> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPUSharedTextureMemoryDmaBufDescriptor>();
    ptr.ref.chain.sTypeAsInt = sType.value;
    ptr.ref.chain.next = next?.toNative(allocator).cast() ?? nullptr;
    if (size != null) ptr.ref.size = size!.toNative(allocator).ref;
    ptr.ref.drmFormat = drmFormat;
    ptr.ref.drmModifier = drmModifier;
    if (planes.isNotEmpty) {
      final arrayPtr = allocator<bindings.WGPUSharedTextureMemoryDmaBufPlane>(planes.length);
      for (var i = 0; i < planes.length; i++) {
        arrayPtr[i] = planes[i].toNative(allocator).ref;
      }
      ptr.ref.planes = arrayPtr;
      ptr.ref.planeCount = planes.length;
    } else {
      ptr.ref.planes = nullptr;
      ptr.ref.planeCount = 0;
    }

    return ptr;
  }

  factory SharedTextureMemoryDmaBufDescriptor.fromNative(bindings.WGPUSharedTextureMemoryDmaBufDescriptor v) {
    final _planes = <SharedTextureMemoryDmaBufPlane>[];
    if (v.planes != nullptr && v.planeCount > 0) {
      for (var i = 0; i < v.planeCount; i++) {
        _planes.add(SharedTextureMemoryDmaBufPlane.fromNative(v.planes[i]));
      }
    }
    return SharedTextureMemoryDmaBufDescriptor(
      size: Extent3D.fromNative(v.size),
      drmFormat: v.drmFormat,
      drmModifier: v.drmModifier,
      planes: _planes,
    );
  }

  @override
  SType get sType => .sharedTextureMemoryDmaBufDescriptor;
}

class SharedTextureMemoryMetalEndAccessState extends ChainedStruct {
  const SharedTextureMemoryMetalEndAccessState({
    this.commandsScheduledFuture,
    super.next,
  });

  final WGPUFuture? commandsScheduledFuture;

  @override
  Pointer<bindings.WGPUSharedTextureMemoryMetalEndAccessState> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPUSharedTextureMemoryMetalEndAccessState>();
    ptr.ref.chain.sTypeAsInt = sType.value;
    ptr.ref.chain.next = next?.toNative(allocator).cast() ?? nullptr;
    if (commandsScheduledFuture != null) ptr.ref.commandsScheduledFuture = commandsScheduledFuture!.toNative(allocator).ref;

    return ptr;
  }

  factory SharedTextureMemoryMetalEndAccessState.fromNative(bindings.WGPUSharedTextureMemoryMetalEndAccessState v) {
    return SharedTextureMemoryMetalEndAccessState(
      commandsScheduledFuture: WGPUFuture.fromNative(v.commandsScheduledFuture),
    );
  }

  @override
  SType get sType => .sharedTextureMemoryMetalEndAccessState;
}

class SurfaceDescriptor {
  const SurfaceDescriptor({
    this.next,
    this.label,
  });

  final ChainedStruct? next;
  final String? label;

  Pointer<bindings.WGPUSurfaceDescriptor> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPUSurfaceDescriptor>();
    ptr.ref.nextInChain = next?.toNative(allocator).cast() ?? nullptr;
    if (label != null) ptr.ref.label = StringView(data: label!, length: label!.length).toNative(allocator).ref;

    return ptr;
  }

  factory SurfaceDescriptor.fromNative(bindings.WGPUSurfaceDescriptor v) {
    return SurfaceDescriptor(
      next: ChainedStruct.fromNative(v.nextInChain),
      label: StringView.fromNative(v.label).toDartString(),
    );
  }
}

class TexelCopyBufferInfo {
  const TexelCopyBufferInfo({
    this.layout,
    required this.buffer,
  });

  final TexelCopyBufferLayout? layout;
  final Buffer buffer;

  Pointer<bindings.WGPUTexelCopyBufferInfo> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPUTexelCopyBufferInfo>();
    if (layout != null) ptr.ref.layout = layout!.toNative(allocator).ref;
    ptr.ref.buffer = buffer._ptr;

    return ptr;
  }

  factory TexelCopyBufferInfo.fromNative(bindings.WGPUTexelCopyBufferInfo v) {
    return TexelCopyBufferInfo(
      layout: TexelCopyBufferLayout.fromNative(v.layout),
      buffer: Buffer._(v.buffer),
    );
  }
}

class TexelCopyTextureInfo {
  const TexelCopyTextureInfo({
    required this.texture,
    this.mipLevel = 0,
    this.origin,
    this.aspect = TextureAspect.undefined,
  });

  final Texture texture;
  final int mipLevel;
  final Origin3D? origin;
  final TextureAspect aspect;

  Pointer<bindings.WGPUTexelCopyTextureInfo> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPUTexelCopyTextureInfo>();
    ptr.ref.texture = texture._ptr;
    ptr.ref.mipLevel = mipLevel;
    if (origin != null) ptr.ref.origin = origin!.toNative(allocator).ref;
    ptr.ref.aspectAsInt = aspect.value;

    return ptr;
  }

  factory TexelCopyTextureInfo.fromNative(bindings.WGPUTexelCopyTextureInfo v) {
    return TexelCopyTextureInfo(
      texture: Texture._(v.texture),
      mipLevel: v.mipLevel,
      origin: Origin3D.fromNative(v.origin),
      aspect: TextureAspect.fromValue(v.aspectAsInt),
    );
  }
}

class TextureComponentSwizzleDescriptor extends ChainedStruct {
  const TextureComponentSwizzleDescriptor({
    this.swizzle,
    super.next,
  });

  final TextureComponentSwizzle? swizzle;

  @override
  Pointer<bindings.WGPUTextureComponentSwizzleDescriptor> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPUTextureComponentSwizzleDescriptor>();
    ptr.ref.chain.sTypeAsInt = sType.value;
    ptr.ref.chain.next = next?.toNative(allocator).cast() ?? nullptr;
    if (swizzle != null) ptr.ref.swizzle = swizzle!.toNative(allocator).ref;

    return ptr;
  }

  factory TextureComponentSwizzleDescriptor.fromNative(bindings.WGPUTextureComponentSwizzleDescriptor v) {
    return TextureComponentSwizzleDescriptor(
      swizzle: TextureComponentSwizzle.fromNative(v.swizzle),
    );
  }

  @override
  SType get sType => .textureComponentSwizzleDescriptor;
}

class TextureDescriptor {
  const TextureDescriptor({
    this.next,
    this.label,
    this.usage = const TextureUsage(0),
    this.dimension = TextureDimension.undefined,
    this.size,
    this.format = TextureFormat.undefined,
    this.mipLevelCount = bindings.WGPU_MIP_LEVEL_COUNT_UNDEFINED,
    this.sampleCount = 0,
    this.viewFormats = const [],
  });

  final ChainedStruct? next;
  final String? label;
  final TextureUsage usage;
  final TextureDimension dimension;
  final Extent3D? size;
  final TextureFormat format;
  final int mipLevelCount;
  final int sampleCount;
  final List<TextureFormat> viewFormats;

  Pointer<bindings.WGPUTextureDescriptor> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPUTextureDescriptor>();
    ptr.ref.nextInChain = next?.toNative(allocator).cast() ?? nullptr;
    if (label != null) ptr.ref.label = StringView(data: label!, length: label!.length).toNative(allocator).ref;
    ptr.ref.usage = usage.value;
    ptr.ref.dimensionAsInt = dimension.value;
    if (size != null) ptr.ref.size = size!.toNative(allocator).ref;
    ptr.ref.formatAsInt = format.value;
    ptr.ref.mipLevelCount = mipLevelCount;
    ptr.ref.sampleCount = sampleCount;
    if (viewFormats.isNotEmpty) {
      final arrayPtr = allocator<UnsignedInt>(viewFormats.length);
      for (var i = 0; i < viewFormats.length; i++) {
        arrayPtr[i] = viewFormats[i].value;
      }
      ptr.ref.viewFormats = arrayPtr;
      ptr.ref.viewFormatCount = viewFormats.length;
    } else {
      ptr.ref.viewFormats = nullptr;
      ptr.ref.viewFormatCount = 0;
    }

    return ptr;
  }

  factory TextureDescriptor.fromNative(bindings.WGPUTextureDescriptor v) {
    final _viewFormats = <TextureFormat>[];
    if (v.viewFormats != nullptr && v.viewFormatCount > 0) {
      for (var i = 0; i < v.viewFormatCount; i++) {
        _viewFormats.add(TextureFormat.fromValue(v.viewFormats[i]));
      }
    }
    return TextureDescriptor(
      next: ChainedStruct.fromNative(v.nextInChain),
      label: StringView.fromNative(v.label).toDartString(),
      usage: TextureUsage(v.usage),
      dimension: TextureDimension.fromValue(v.dimensionAsInt),
      size: Extent3D.fromNative(v.size),
      format: TextureFormat.fromValue(v.formatAsInt),
      mipLevelCount: v.mipLevelCount,
      sampleCount: v.sampleCount,
      viewFormats: _viewFormats,
    );
  }
}

class VertexBufferLayout {
  const VertexBufferLayout({
    this.next,
    this.stepMode = VertexStepMode.undefined,
    this.arrayStride = 0,
    this.attributes = const [],
  });

  final ChainedStruct? next;
  final VertexStepMode stepMode;
  final int arrayStride;
  final List<VertexAttribute> attributes;

  Pointer<bindings.WGPUVertexBufferLayout> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPUVertexBufferLayout>();
    ptr.ref.nextInChain = next?.toNative(allocator).cast() ?? nullptr;
    ptr.ref.stepModeAsInt = stepMode.value;
    ptr.ref.arrayStride = arrayStride;
    if (attributes.isNotEmpty) {
      final arrayPtr = allocator<bindings.WGPUVertexAttribute>(attributes.length);
      for (var i = 0; i < attributes.length; i++) {
        arrayPtr[i] = attributes[i].toNative(allocator).ref;
      }
      ptr.ref.attributes = arrayPtr;
      ptr.ref.attributeCount = attributes.length;
    } else {
      ptr.ref.attributes = nullptr;
      ptr.ref.attributeCount = 0;
    }

    return ptr;
  }

  factory VertexBufferLayout.fromNative(bindings.WGPUVertexBufferLayout v) {
    final _attributes = <VertexAttribute>[];
    if (v.attributes != nullptr && v.attributeCount > 0) {
      for (var i = 0; i < v.attributeCount; i++) {
        _attributes.add(VertexAttribute.fromNative(v.attributes[i]));
      }
    }
    return VertexBufferLayout(
      next: ChainedStruct.fromNative(v.nextInChain),
      stepMode: VertexStepMode.fromValue(v.stepModeAsInt),
      arrayStride: v.arrayStride,
      attributes: _attributes,
    );
  }
}

class AdapterInfo {
  const AdapterInfo({
    this.next,
    this.vendor,
    this.architecture,
    this.device,
    this.description,
    this.backendType = BackendType.undefined,
    this.adapterType = AdapterType.discreteGPU,
    this.vendorID = 0,
    this.deviceID = 0,
    this.subgroupMinSize = 0,
    this.subgroupMaxSize = 0,
  });

  final ChainedStruct? next;
  final String? vendor;
  final String? architecture;
  final String? device;
  final String? description;
  final BackendType backendType;
  final AdapterType adapterType;
  final int vendorID;
  final int deviceID;
  final int subgroupMinSize;
  final int subgroupMaxSize;

  Pointer<bindings.WGPUAdapterInfo> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPUAdapterInfo>();
    ptr.ref.nextInChain = next?.toNative(allocator).cast() ?? nullptr;
    if (vendor != null) ptr.ref.vendor = StringView(data: vendor!, length: vendor!.length).toNative(allocator).ref;
    if (architecture != null) ptr.ref.architecture = StringView(data: architecture!, length: architecture!.length).toNative(allocator).ref;
    if (device != null) ptr.ref.device = StringView(data: device!, length: device!.length).toNative(allocator).ref;
    if (description != null) ptr.ref.description = StringView(data: description!, length: description!.length).toNative(allocator).ref;
    ptr.ref.backendTypeAsInt = backendType.value;
    ptr.ref.adapterTypeAsInt = adapterType.value;
    ptr.ref.vendorID = vendorID;
    ptr.ref.deviceID = deviceID;
    ptr.ref.subgroupMinSize = subgroupMinSize;
    ptr.ref.subgroupMaxSize = subgroupMaxSize;

    return ptr;
  }

  factory AdapterInfo.fromNative(bindings.WGPUAdapterInfo v) {
    return AdapterInfo(
      next: ChainedStruct.fromNative(v.nextInChain),
      vendor: StringView.fromNative(v.vendor).toDartString(),
      architecture: StringView.fromNative(v.architecture).toDartString(),
      device: StringView.fromNative(v.device).toDartString(),
      description: StringView.fromNative(v.description).toDartString(),
      backendType: BackendType.fromValue(v.backendTypeAsInt),
      adapterType: AdapterType.fromValue(v.adapterTypeAsInt),
      vendorID: v.vendorID,
      deviceID: v.deviceID,
      subgroupMinSize: v.subgroupMinSize,
      subgroupMaxSize: v.subgroupMaxSize,
    );
  }
}

class BindGroupDescriptor {
  const BindGroupDescriptor({
    this.next,
    this.label,
    required this.layout,
    this.entries = const [],
  });

  final ChainedStruct? next;
  final String? label;
  final BindGroupLayout layout;
  final List<BindGroupEntry> entries;

  Pointer<bindings.WGPUBindGroupDescriptor> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPUBindGroupDescriptor>();
    ptr.ref.nextInChain = next?.toNative(allocator).cast() ?? nullptr;
    if (label != null) ptr.ref.label = StringView(data: label!, length: label!.length).toNative(allocator).ref;
    ptr.ref.layout = layout._ptr;
    if (entries.isNotEmpty) {
      final arrayPtr = allocator<bindings.WGPUBindGroupEntry>(entries.length);
      for (var i = 0; i < entries.length; i++) {
        arrayPtr[i] = entries[i].toNative(allocator).ref;
      }
      ptr.ref.entries = arrayPtr;
      ptr.ref.entryCount = entries.length;
    } else {
      ptr.ref.entries = nullptr;
      ptr.ref.entryCount = 0;
    }

    return ptr;
  }

  factory BindGroupDescriptor.fromNative(bindings.WGPUBindGroupDescriptor v) {
    final _entries = <BindGroupEntry>[];
    if (v.entries != nullptr && v.entryCount > 0) {
      for (var i = 0; i < v.entryCount; i++) {
        _entries.add(BindGroupEntry.fromNative(v.entries[i]));
      }
    }
    return BindGroupDescriptor(
      next: ChainedStruct.fromNative(v.nextInChain),
      label: StringView.fromNative(v.label).toDartString(),
      layout: BindGroupLayout._(v.layout),
      entries: _entries,
    );
  }
}

class BindGroupLayoutDescriptor {
  const BindGroupLayoutDescriptor({
    this.next,
    this.label,
    this.entries = const [],
  });

  final ChainedStruct? next;
  final String? label;
  final List<BindGroupLayoutEntry> entries;

  Pointer<bindings.WGPUBindGroupLayoutDescriptor> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPUBindGroupLayoutDescriptor>();
    ptr.ref.nextInChain = next?.toNative(allocator).cast() ?? nullptr;
    if (label != null) ptr.ref.label = StringView(data: label!, length: label!.length).toNative(allocator).ref;
    if (entries.isNotEmpty) {
      final arrayPtr = allocator<bindings.WGPUBindGroupLayoutEntry>(entries.length);
      for (var i = 0; i < entries.length; i++) {
        arrayPtr[i] = entries[i].toNative(allocator).ref;
      }
      ptr.ref.entries = arrayPtr;
      ptr.ref.entryCount = entries.length;
    } else {
      ptr.ref.entries = nullptr;
      ptr.ref.entryCount = 0;
    }

    return ptr;
  }

  factory BindGroupLayoutDescriptor.fromNative(bindings.WGPUBindGroupLayoutDescriptor v) {
    final _entries = <BindGroupLayoutEntry>[];
    if (v.entries != nullptr && v.entryCount > 0) {
      for (var i = 0; i < v.entryCount; i++) {
        _entries.add(BindGroupLayoutEntry.fromNative(v.entries[i]));
      }
    }
    return BindGroupLayoutDescriptor(
      next: ChainedStruct.fromNative(v.nextInChain),
      label: StringView.fromNative(v.label).toDartString(),
      entries: _entries,
    );
  }
}

class ColorTargetState {
  const ColorTargetState({
    this.next,
    this.format = TextureFormat.undefined,
    this.blend,
    this.writeMask = const ColorWriteMask(0),
  });

  final ChainedStruct? next;
  final TextureFormat format;
  final BlendState? blend;
  final ColorWriteMask writeMask;

  Pointer<bindings.WGPUColorTargetState> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPUColorTargetState>();
    ptr.ref.nextInChain = next?.toNative(allocator).cast() ?? nullptr;
    ptr.ref.formatAsInt = format.value;
    ptr.ref.blend = (blend?.toNative(allocator) ?? nullptr);
    ptr.ref.writeMask = writeMask.value;

    return ptr;
  }

  factory ColorTargetState.fromNative(bindings.WGPUColorTargetState v) {
    return ColorTargetState(
      next: ChainedStruct.fromNative(v.nextInChain),
      format: TextureFormat.fromValue(v.formatAsInt),
      blend: (v.blend != nullptr ? BlendState.fromNative(v.blend.ref) : null),
      writeMask: ColorWriteMask(v.writeMask),
    );
  }
}

class CompilationInfo {
  const CompilationInfo({
    this.next,
    this.messages = const [],
  });

  final ChainedStruct? next;
  final List<CompilationMessage> messages;

  Pointer<bindings.WGPUCompilationInfo> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPUCompilationInfo>();
    ptr.ref.nextInChain = next?.toNative(allocator).cast() ?? nullptr;
    if (messages.isNotEmpty) {
      final arrayPtr = allocator<bindings.WGPUCompilationMessage>(messages.length);
      for (var i = 0; i < messages.length; i++) {
        arrayPtr[i] = messages[i].toNative(allocator).ref;
      }
      ptr.ref.messages = arrayPtr;
      ptr.ref.messageCount = messages.length;
    } else {
      ptr.ref.messages = nullptr;
      ptr.ref.messageCount = 0;
    }

    return ptr;
  }

  factory CompilationInfo.fromNative(bindings.WGPUCompilationInfo v) {
    final _messages = <CompilationMessage>[];
    if (v.messages != nullptr && v.messageCount > 0) {
      for (var i = 0; i < v.messageCount; i++) {
        _messages.add(CompilationMessage.fromNative(v.messages[i]));
      }
    }
    return CompilationInfo(
      next: ChainedStruct.fromNative(v.nextInChain),
      messages: _messages,
    );
  }
}

class ComputePipelineDescriptor {
  const ComputePipelineDescriptor({
    this.next,
    this.label,
    this.layout,
    this.compute,
  });

  final ChainedStruct? next;
  final String? label;
  final PipelineLayout? layout;
  final ComputeState? compute;

  Pointer<bindings.WGPUComputePipelineDescriptor> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPUComputePipelineDescriptor>();
    ptr.ref.nextInChain = next?.toNative(allocator).cast() ?? nullptr;
    if (label != null) ptr.ref.label = StringView(data: label!, length: label!.length).toNative(allocator).ref;
    ptr.ref.layout = (layout?._ptr ?? nullptr);
    if (compute != null) ptr.ref.compute = compute!.toNative(allocator).ref;

    return ptr;
  }

  factory ComputePipelineDescriptor.fromNative(bindings.WGPUComputePipelineDescriptor v) {
    return ComputePipelineDescriptor(
      next: ChainedStruct.fromNative(v.nextInChain),
      label: StringView.fromNative(v.label).toDartString(),
      layout: (v.layout != nullptr ? PipelineLayout._(v.layout) : null),
      compute: ComputeState.fromNative(v.compute),
    );
  }
}

class DawnFormatCapabilities {
  const DawnFormatCapabilities({
    this.next,
  });

  final ChainedStruct? next;

  Pointer<bindings.WGPUDawnFormatCapabilities> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPUDawnFormatCapabilities>();
    ptr.ref.nextInChain = next?.toNative(allocator).cast() ?? nullptr;

    return ptr;
  }

  factory DawnFormatCapabilities.fromNative(bindings.WGPUDawnFormatCapabilities v) {
    return DawnFormatCapabilities(
      next: ChainedStruct.fromNative(v.nextInChain),
    );
  }
}

class DeviceDescriptor {
  const DeviceDescriptor({
    this.next,
    this.label,
    this.requiredFeatures = const [],
    this.requiredLimits,
    this.defaultQueue,
    this.deviceLostCallbackInfo,
    this.uncapturedErrorCallbackInfo,
  });

  final ChainedStruct? next;
  final String? label;
  final List<FeatureName> requiredFeatures;
  final Limits? requiredLimits;
  final QueueDescriptor? defaultQueue;
  final DeviceLostCallbackInfo? deviceLostCallbackInfo;
  final UncapturedErrorCallbackInfo? uncapturedErrorCallbackInfo;

  Pointer<bindings.WGPUDeviceDescriptor> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPUDeviceDescriptor>();
    ptr.ref.nextInChain = next?.toNative(allocator).cast() ?? nullptr;
    if (label != null) ptr.ref.label = StringView(data: label!, length: label!.length).toNative(allocator).ref;
    if (requiredFeatures.isNotEmpty) {
      final arrayPtr = allocator<UnsignedInt>(requiredFeatures.length);
      for (var i = 0; i < requiredFeatures.length; i++) {
        arrayPtr[i] = requiredFeatures[i].value;
      }
      ptr.ref.requiredFeatures = arrayPtr;
      ptr.ref.requiredFeatureCount = requiredFeatures.length;
    } else {
      ptr.ref.requiredFeatures = nullptr;
      ptr.ref.requiredFeatureCount = 0;
    }

    ptr.ref.requiredLimits = (requiredLimits?.toNative(allocator) ?? nullptr);
    if (defaultQueue != null) ptr.ref.defaultQueue = defaultQueue!.toNative(allocator).ref;
    if (deviceLostCallbackInfo != null) ptr.ref.deviceLostCallbackInfo = deviceLostCallbackInfo!.toNative(allocator).ref;
    if (uncapturedErrorCallbackInfo != null) ptr.ref.uncapturedErrorCallbackInfo = uncapturedErrorCallbackInfo!.toNative(allocator).ref;

    return ptr;
  }

  factory DeviceDescriptor.fromNative(bindings.WGPUDeviceDescriptor v) {
    final _requiredFeatures = <FeatureName>[];
    if (v.requiredFeatures != nullptr && v.requiredFeatureCount > 0) {
      for (var i = 0; i < v.requiredFeatureCount; i++) {
        _requiredFeatures.add(FeatureName.fromValue(v.requiredFeatures[i]));
      }
    }
    return DeviceDescriptor(
      next: ChainedStruct.fromNative(v.nextInChain),
      label: StringView.fromNative(v.label).toDartString(),
      requiredFeatures: _requiredFeatures,
      requiredLimits: (v.requiredLimits != nullptr ? Limits.fromNative(v.requiredLimits.ref) : null),
      defaultQueue: QueueDescriptor.fromNative(v.defaultQueue),
      deviceLostCallbackInfo: DeviceLostCallbackInfo.fromNative(v.deviceLostCallbackInfo),
      uncapturedErrorCallbackInfo: UncapturedErrorCallbackInfo.fromNative(v.uncapturedErrorCallbackInfo),
    );
  }
}

class PipelineLayoutDescriptor {
  const PipelineLayoutDescriptor({
    this.next,
    this.label,
    this.bindGroupLayouts = const [],
    this.immediateSize = 0,
  });

  final ChainedStruct? next;
  final String? label;
  final List<BindGroupLayout> bindGroupLayouts;
  final int immediateSize;

  Pointer<bindings.WGPUPipelineLayoutDescriptor> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPUPipelineLayoutDescriptor>();
    ptr.ref.nextInChain = next?.toNative(allocator).cast() ?? nullptr;
    if (label != null) ptr.ref.label = StringView(data: label!, length: label!.length).toNative(allocator).ref;
    if (bindGroupLayouts.isNotEmpty) {
      final arrayPtr = allocator<Pointer<bindings.WGPUBindGroupLayoutImpl>>(bindGroupLayouts.length);
      for (var i = 0; i < bindGroupLayouts.length; i++) {
        arrayPtr[i] = bindGroupLayouts[i]._ptr;
      }
      ptr.ref.bindGroupLayouts = arrayPtr;
      ptr.ref.bindGroupLayoutCount = bindGroupLayouts.length;
    } else {
      ptr.ref.bindGroupLayouts = nullptr;
      ptr.ref.bindGroupLayoutCount = 0;
    }

    ptr.ref.immediateSize = immediateSize;

    return ptr;
  }

  factory PipelineLayoutDescriptor.fromNative(bindings.WGPUPipelineLayoutDescriptor v) {
    final _bindGroupLayouts = <BindGroupLayout>[];
    if (v.bindGroupLayouts != nullptr && v.bindGroupLayoutCount > 0) {
      for (var i = 0; i < v.bindGroupLayoutCount; i++) {
        _bindGroupLayouts.add(BindGroupLayout._(v.bindGroupLayouts[i]));
      }
    }
    return PipelineLayoutDescriptor(
      next: ChainedStruct.fromNative(v.nextInChain),
      label: StringView.fromNative(v.label).toDartString(),
      bindGroupLayouts: _bindGroupLayouts,
      immediateSize: v.immediateSize,
    );
  }
}

class RenderPassPixelLocalStorage extends ChainedStruct {
  const RenderPassPixelLocalStorage({
    this.totalPixelLocalStorageSize = 0,
    this.storageAttachments = const [],
    super.next,
  });

  final int totalPixelLocalStorageSize;
  final List<RenderPassStorageAttachment> storageAttachments;

  @override
  Pointer<bindings.WGPURenderPassPixelLocalStorage> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPURenderPassPixelLocalStorage>();
    ptr.ref.chain.sTypeAsInt = sType.value;
    ptr.ref.chain.next = next?.toNative(allocator).cast() ?? nullptr;
    ptr.ref.totalPixelLocalStorageSize = totalPixelLocalStorageSize;
    if (storageAttachments.isNotEmpty) {
      final arrayPtr = allocator<bindings.WGPURenderPassStorageAttachment>(storageAttachments.length);
      for (var i = 0; i < storageAttachments.length; i++) {
        arrayPtr[i] = storageAttachments[i].toNative(allocator).ref;
      }
      ptr.ref.storageAttachments = arrayPtr;
      ptr.ref.storageAttachmentCount = storageAttachments.length;
    } else {
      ptr.ref.storageAttachments = nullptr;
      ptr.ref.storageAttachmentCount = 0;
    }

    return ptr;
  }

  factory RenderPassPixelLocalStorage.fromNative(bindings.WGPURenderPassPixelLocalStorage v) {
    final _storageAttachments = <RenderPassStorageAttachment>[];
    if (v.storageAttachments != nullptr && v.storageAttachmentCount > 0) {
      for (var i = 0; i < v.storageAttachmentCount; i++) {
        _storageAttachments.add(RenderPassStorageAttachment.fromNative(v.storageAttachments[i]));
      }
    }
    return RenderPassPixelLocalStorage(
      totalPixelLocalStorageSize: v.totalPixelLocalStorageSize,
      storageAttachments: _storageAttachments,
    );
  }

  @override
  SType get sType => .renderPassPixelLocalStorage;
}

class SharedTextureMemoryDescriptor {
  const SharedTextureMemoryDescriptor({
    this.next,
    this.label,
  });

  final ChainedStruct? next;
  final String? label;

  Pointer<bindings.WGPUSharedTextureMemoryDescriptor> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPUSharedTextureMemoryDescriptor>();
    ptr.ref.nextInChain = next?.toNative(allocator).cast() ?? nullptr;
    if (label != null) ptr.ref.label = StringView(data: label!, length: label!.length).toNative(allocator).ref;

    return ptr;
  }

  factory SharedTextureMemoryDescriptor.fromNative(bindings.WGPUSharedTextureMemoryDescriptor v) {
    return SharedTextureMemoryDescriptor(
      next: ChainedStruct.fromNative(v.nextInChain),
      label: StringView.fromNative(v.label).toDartString(),
    );
  }
}

class SharedTextureMemoryEndAccessState {
  const SharedTextureMemoryEndAccessState({
    this.next,
    this.initialized = false,
    this.fences = const [],
    required this.signaledValues,
  });

  final ChainedStruct? next;
  final bool initialized;
  final List<SharedFence> fences;
  final Pointer<Uint64> signaledValues;

  Pointer<bindings.WGPUSharedTextureMemoryEndAccessState> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPUSharedTextureMemoryEndAccessState>();
    ptr.ref.nextInChain = next?.toNative(allocator).cast() ?? nullptr;
    ptr.ref.initialized = (initialized ? 1 : 0);
    if (fences.isNotEmpty) {
      final arrayPtr = allocator<Pointer<bindings.WGPUSharedFenceImpl>>(fences.length);
      for (var i = 0; i < fences.length; i++) {
        arrayPtr[i] = fences[i]._ptr;
      }
      ptr.ref.fences = arrayPtr;
      ptr.ref.fenceCount = fences.length;
    } else {
      ptr.ref.fences = nullptr;
      ptr.ref.fenceCount = 0;
    }

    ptr.ref.signaledValues = signaledValues;

    return ptr;
  }

  factory SharedTextureMemoryEndAccessState.fromNative(bindings.WGPUSharedTextureMemoryEndAccessState v) {
    final _fences = <SharedFence>[];
    if (v.fences != nullptr && v.fenceCount > 0) {
      for (var i = 0; i < v.fenceCount; i++) {
        _fences.add(SharedFence._(v.fences[i]));
      }
    }
    return SharedTextureMemoryEndAccessState(
      next: ChainedStruct.fromNative(v.nextInChain),
      initialized: (v.initialized != 0),
      fences: _fences,
      signaledValues: v.signaledValues,
    );
  }
}

class SharedTextureMemoryProperties {
  const SharedTextureMemoryProperties({
    this.next,
    this.usage = const TextureUsage(0),
    this.size,
    this.format = TextureFormat.undefined,
  });

  final ChainedStruct? next;
  final TextureUsage usage;
  final Extent3D? size;
  final TextureFormat format;

  Pointer<bindings.WGPUSharedTextureMemoryProperties> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPUSharedTextureMemoryProperties>();
    ptr.ref.nextInChain = next?.toNative(allocator).cast() ?? nullptr;
    ptr.ref.usage = usage.value;
    if (size != null) ptr.ref.size = size!.toNative(allocator).ref;
    ptr.ref.formatAsInt = format.value;

    return ptr;
  }

  factory SharedTextureMemoryProperties.fromNative(bindings.WGPUSharedTextureMemoryProperties v) {
    return SharedTextureMemoryProperties(
      next: ChainedStruct.fromNative(v.nextInChain),
      usage: TextureUsage(v.usage),
      size: Extent3D.fromNative(v.size),
      format: TextureFormat.fromValue(v.formatAsInt),
    );
  }
}

class TextureViewDescriptor {
  const TextureViewDescriptor({
    this.next,
    this.label,
    this.format = TextureFormat.undefined,
    this.dimension = TextureViewDimension.undefined,
    this.baseMipLevel = 0,
    this.mipLevelCount = bindings.WGPU_MIP_LEVEL_COUNT_UNDEFINED,
    this.baseArrayLayer = 0,
    this.arrayLayerCount = bindings.WGPU_ARRAY_LAYER_COUNT_UNDEFINED,
    this.aspect = TextureAspect.undefined,
    this.usage = const TextureUsage(0),
  });

  final ChainedStruct? next;
  final String? label;
  final TextureFormat format;
  final TextureViewDimension dimension;
  final int baseMipLevel;
  final int mipLevelCount;
  final int baseArrayLayer;
  final int arrayLayerCount;
  final TextureAspect aspect;
  final TextureUsage usage;

  Pointer<bindings.WGPUTextureViewDescriptor> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPUTextureViewDescriptor>();
    ptr.ref.nextInChain = next?.toNative(allocator).cast() ?? nullptr;
    if (label != null) ptr.ref.label = StringView(data: label!, length: label!.length).toNative(allocator).ref;
    ptr.ref.formatAsInt = format.value;
    ptr.ref.dimensionAsInt = dimension.value;
    ptr.ref.baseMipLevel = baseMipLevel;
    ptr.ref.mipLevelCount = mipLevelCount;
    ptr.ref.baseArrayLayer = baseArrayLayer;
    ptr.ref.arrayLayerCount = arrayLayerCount;
    ptr.ref.aspectAsInt = aspect.value;
    ptr.ref.usage = usage.value;

    return ptr;
  }

  factory TextureViewDescriptor.fromNative(bindings.WGPUTextureViewDescriptor v) {
    return TextureViewDescriptor(
      next: ChainedStruct.fromNative(v.nextInChain),
      label: StringView.fromNative(v.label).toDartString(),
      format: TextureFormat.fromValue(v.formatAsInt),
      dimension: TextureViewDimension.fromValue(v.dimensionAsInt),
      baseMipLevel: v.baseMipLevel,
      mipLevelCount: v.mipLevelCount,
      baseArrayLayer: v.baseArrayLayer,
      arrayLayerCount: v.arrayLayerCount,
      aspect: TextureAspect.fromValue(v.aspectAsInt),
      usage: TextureUsage(v.usage),
    );
  }
}

class VertexState {
  const VertexState({
    this.next,
    required this.module,
    this.entryPoint,
    this.constants = const [],
    this.buffers = const [],
  });

  final ChainedStruct? next;
  final ShaderModule module;
  final String? entryPoint;
  final List<ConstantEntry> constants;
  final List<VertexBufferLayout> buffers;

  Pointer<bindings.WGPUVertexState> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPUVertexState>();
    ptr.ref.nextInChain = next?.toNative(allocator).cast() ?? nullptr;
    ptr.ref.module = module._ptr;
    if (entryPoint != null) ptr.ref.entryPoint = StringView(data: entryPoint!, length: entryPoint!.length).toNative(allocator).ref;
    if (constants.isNotEmpty) {
      final arrayPtr = allocator<bindings.WGPUConstantEntry>(constants.length);
      for (var i = 0; i < constants.length; i++) {
        arrayPtr[i] = constants[i].toNative(allocator).ref;
      }
      ptr.ref.constants = arrayPtr;
      ptr.ref.constantCount = constants.length;
    } else {
      ptr.ref.constants = nullptr;
      ptr.ref.constantCount = 0;
    }

    if (buffers.isNotEmpty) {
      final arrayPtr = allocator<bindings.WGPUVertexBufferLayout>(buffers.length);
      for (var i = 0; i < buffers.length; i++) {
        arrayPtr[i] = buffers[i].toNative(allocator).ref;
      }
      ptr.ref.buffers = arrayPtr;
      ptr.ref.bufferCount = buffers.length;
    } else {
      ptr.ref.buffers = nullptr;
      ptr.ref.bufferCount = 0;
    }

    return ptr;
  }

  factory VertexState.fromNative(bindings.WGPUVertexState v) {
    final _constants = <ConstantEntry>[];
    if (v.constants != nullptr && v.constantCount > 0) {
      for (var i = 0; i < v.constantCount; i++) {
        _constants.add(ConstantEntry.fromNative(v.constants[i]));
      }
    }
    final _buffers = <VertexBufferLayout>[];
    if (v.buffers != nullptr && v.bufferCount > 0) {
      for (var i = 0; i < v.bufferCount; i++) {
        _buffers.add(VertexBufferLayout.fromNative(v.buffers[i]));
      }
    }
    return VertexState(
      next: ChainedStruct.fromNative(v.nextInChain),
      module: ShaderModule._(v.module),
      entryPoint: StringView.fromNative(v.entryPoint).toDartString(),
      constants: _constants,
      buffers: _buffers,
    );
  }
}

class FragmentState {
  const FragmentState({
    this.next,
    required this.module,
    this.entryPoint,
    this.constants = const [],
    this.targets = const [],
  });

  final ChainedStruct? next;
  final ShaderModule module;
  final String? entryPoint;
  final List<ConstantEntry> constants;
  final List<ColorTargetState> targets;

  Pointer<bindings.WGPUFragmentState> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPUFragmentState>();
    ptr.ref.nextInChain = next?.toNative(allocator).cast() ?? nullptr;
    ptr.ref.module = module._ptr;
    if (entryPoint != null) ptr.ref.entryPoint = StringView(data: entryPoint!, length: entryPoint!.length).toNative(allocator).ref;
    if (constants.isNotEmpty) {
      final arrayPtr = allocator<bindings.WGPUConstantEntry>(constants.length);
      for (var i = 0; i < constants.length; i++) {
        arrayPtr[i] = constants[i].toNative(allocator).ref;
      }
      ptr.ref.constants = arrayPtr;
      ptr.ref.constantCount = constants.length;
    } else {
      ptr.ref.constants = nullptr;
      ptr.ref.constantCount = 0;
    }

    if (targets.isNotEmpty) {
      final arrayPtr = allocator<bindings.WGPUColorTargetState>(targets.length);
      for (var i = 0; i < targets.length; i++) {
        arrayPtr[i] = targets[i].toNative(allocator).ref;
      }
      ptr.ref.targets = arrayPtr;
      ptr.ref.targetCount = targets.length;
    } else {
      ptr.ref.targets = nullptr;
      ptr.ref.targetCount = 0;
    }

    return ptr;
  }

  factory FragmentState.fromNative(bindings.WGPUFragmentState v) {
    final _constants = <ConstantEntry>[];
    if (v.constants != nullptr && v.constantCount > 0) {
      for (var i = 0; i < v.constantCount; i++) {
        _constants.add(ConstantEntry.fromNative(v.constants[i]));
      }
    }
    final _targets = <ColorTargetState>[];
    if (v.targets != nullptr && v.targetCount > 0) {
      for (var i = 0; i < v.targetCount; i++) {
        _targets.add(ColorTargetState.fromNative(v.targets[i]));
      }
    }
    return FragmentState(
      next: ChainedStruct.fromNative(v.nextInChain),
      module: ShaderModule._(v.module),
      entryPoint: StringView.fromNative(v.entryPoint).toDartString(),
      constants: _constants,
      targets: _targets,
    );
  }
}

class RenderPassDescriptor {
  const RenderPassDescriptor({
    this.next,
    this.label,
    this.colorAttachments = const [],
    this.depthStencilAttachment,
    this.occlusionQuerySet,
    this.timestampWrites,
  });

  final ChainedStruct? next;
  final String? label;
  final List<RenderPassColorAttachment> colorAttachments;
  final RenderPassDepthStencilAttachment? depthStencilAttachment;
  final QuerySet? occlusionQuerySet;
  final PassTimestampWrites? timestampWrites;

  Pointer<bindings.WGPURenderPassDescriptor> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPURenderPassDescriptor>();
    ptr.ref.nextInChain = next?.toNative(allocator).cast() ?? nullptr;
    if (label != null) ptr.ref.label = StringView(data: label!, length: label!.length).toNative(allocator).ref;
    if (colorAttachments.isNotEmpty) {
      final arrayPtr = allocator<bindings.WGPURenderPassColorAttachment>(colorAttachments.length);
      for (var i = 0; i < colorAttachments.length; i++) {
        arrayPtr[i] = colorAttachments[i].toNative(allocator).ref;
      }
      ptr.ref.colorAttachments = arrayPtr;
      ptr.ref.colorAttachmentCount = colorAttachments.length;
    } else {
      ptr.ref.colorAttachments = nullptr;
      ptr.ref.colorAttachmentCount = 0;
    }

    ptr.ref.depthStencilAttachment = (depthStencilAttachment?.toNative(allocator) ?? nullptr);
    ptr.ref.occlusionQuerySet = (occlusionQuerySet?._ptr ?? nullptr);
    ptr.ref.timestampWrites = (timestampWrites?.toNative(allocator) ?? nullptr);

    return ptr;
  }

  factory RenderPassDescriptor.fromNative(bindings.WGPURenderPassDescriptor v) {
    final _colorAttachments = <RenderPassColorAttachment>[];
    if (v.colorAttachments != nullptr && v.colorAttachmentCount > 0) {
      for (var i = 0; i < v.colorAttachmentCount; i++) {
        _colorAttachments.add(RenderPassColorAttachment.fromNative(v.colorAttachments[i]));
      }
    }
    return RenderPassDescriptor(
      next: ChainedStruct.fromNative(v.nextInChain),
      label: StringView.fromNative(v.label).toDartString(),
      colorAttachments: _colorAttachments,
      depthStencilAttachment: (v.depthStencilAttachment != nullptr ? RenderPassDepthStencilAttachment.fromNative(v.depthStencilAttachment.ref) : null),
      occlusionQuerySet: (v.occlusionQuerySet != nullptr ? QuerySet._(v.occlusionQuerySet) : null),
      timestampWrites: (v.timestampWrites != nullptr ? PassTimestampWrites.fromNative(v.timestampWrites.ref) : null),
    );
  }
}

class RenderPipelineDescriptor {
  const RenderPipelineDescriptor({
    this.next,
    this.label,
    this.layout,
    this.vertex,
    this.primitive,
    this.depthStencil,
    this.multisample,
    this.fragment,
  });

  final ChainedStruct? next;
  final String? label;
  final PipelineLayout? layout;
  final VertexState? vertex;
  final PrimitiveState? primitive;
  final DepthStencilState? depthStencil;
  final MultisampleState? multisample;
  final FragmentState? fragment;

  Pointer<bindings.WGPURenderPipelineDescriptor> toNative(Allocator allocator) {
    final ptr = allocator<bindings.WGPURenderPipelineDescriptor>();
    ptr.ref.nextInChain = next?.toNative(allocator).cast() ?? nullptr;
    if (label != null) ptr.ref.label = StringView(data: label!, length: label!.length).toNative(allocator).ref;
    ptr.ref.layout = (layout?._ptr ?? nullptr);
    if (vertex != null) ptr.ref.vertex = vertex!.toNative(allocator).ref;
    if (primitive != null) ptr.ref.primitive = primitive!.toNative(allocator).ref;
    ptr.ref.depthStencil = (depthStencil?.toNative(allocator) ?? nullptr);
    if (multisample != null) ptr.ref.multisample = multisample!.toNative(allocator).ref;
    ptr.ref.fragment = (fragment?.toNative(allocator) ?? nullptr);

    return ptr;
  }

  factory RenderPipelineDescriptor.fromNative(bindings.WGPURenderPipelineDescriptor v) {
    return RenderPipelineDescriptor(
      next: ChainedStruct.fromNative(v.nextInChain),
      label: StringView.fromNative(v.label).toDartString(),
      layout: (v.layout != nullptr ? PipelineLayout._(v.layout) : null),
      vertex: VertexState.fromNative(v.vertex),
      primitive: PrimitiveState.fromNative(v.primitive),
      depthStencil: (v.depthStencil != nullptr ? DepthStencilState.fromNative(v.depthStencil.ref) : null),
      multisample: MultisampleState.fromNative(v.multisample),
      fragment: (v.fragment != nullptr ? FragmentState.fromNative(v.fragment.ref) : null),
    );
  }
}

ChainedStruct? _chainedStructFromNative(Pointer<bindings.WGPUChainedStruct> ptr) {
  if (ptr == nullptr) return null;
  final stype = SType.fromNative(ptr.ref.sType);
  return switch (stype) {
    .adapterPropertiesD3D => AdapterPropertiesD3D.fromNative(ptr.cast<bindings.WGPUAdapterPropertiesD3D>().ref),
    .adapterPropertiesDrm => AdapterPropertiesDrm.fromNative(ptr.cast<bindings.WGPUAdapterPropertiesDrm>().ref),
    .adapterPropertiesVk => AdapterPropertiesVk.fromNative(ptr.cast<bindings.WGPUAdapterPropertiesVk>().ref),
    .adapterPropertiesWGPU => AdapterPropertiesWGPU.fromNative(ptr.cast<bindings.WGPUAdapterPropertiesWGPU>().ref),
    .bufferHostMappedPointer => BufferHostMappedPointer.fromNative(ptr.cast<bindings.WGPUBufferHostMappedPointer>().ref),
    .colorTargetStateExpandResolveTextureDawn => ColorTargetStateExpandResolveTextureDawn.fromNative(ptr.cast<bindings.WGPUColorTargetStateExpandResolveTextureDawn>().ref),
    .compatibilityModeLimits => CompatibilityModeLimits.fromNative(ptr.cast<bindings.WGPUCompatibilityModeLimits>().ref),
    .dawnAdapterPropertiesPowerPreference => DawnAdapterPropertiesPowerPreference.fromNative(ptr.cast<bindings.WGPUDawnAdapterPropertiesPowerPreference>().ref),
    .dawnBufferDescriptorErrorInfoFromWireClient => DawnBufferDescriptorErrorInfoFromWireClient.fromNative(ptr.cast<bindings.WGPUDawnBufferDescriptorErrorInfoFromWireClient>().ref),
    .dawnCompilationMessageUtf16 => DawnCompilationMessageUtf16.fromNative(ptr.cast<bindings.WGPUDawnCompilationMessageUtf16>().ref),
    .dawnConsumeAdapterDescriptor => DawnConsumeAdapterDescriptor.fromNative(ptr.cast<bindings.WGPUDawnConsumeAdapterDescriptor>().ref),
    .dawnDeviceAllocatorControl => DawnDeviceAllocatorControl.fromNative(ptr.cast<bindings.WGPUDawnDeviceAllocatorControl>().ref),
    .dawnEncoderInternalUsageDescriptor => DawnEncoderInternalUsageDescriptor.fromNative(ptr.cast<bindings.WGPUDawnEncoderInternalUsageDescriptor>().ref),
    .dawnFakeBufferOOMForTesting => DawnFakeBufferOOMForTesting.fromNative(ptr.cast<bindings.WGPUDawnFakeBufferOOMForTesting>().ref),
    .dawnFakeDeviceInitializeErrorForTesting => DawnFakeDeviceInitializeErrorForTesting.fromNative(ptr.cast<bindings.WGPUDawnFakeDeviceInitializeErrorForTesting>().ref),
    .dawnHostMappedPointerLimits => DawnHostMappedPointerLimits.fromNative(ptr.cast<bindings.WGPUDawnHostMappedPointerLimits>().ref),
    .dawnInjectedInvalidSType => DawnInjectedInvalidSType.fromNative(ptr.cast<bindings.WGPUDawnInjectedInvalidSType>().ref),
    .dawnRenderPassSampleCount => DawnRenderPassSampleCount.fromNative(ptr.cast<bindings.WGPUDawnRenderPassSampleCount>().ref),
    .dawnShaderModuleSPIRVOptionsDescriptor => DawnShaderModuleSPIRVOptionsDescriptor.fromNative(ptr.cast<bindings.WGPUDawnShaderModuleSPIRVOptionsDescriptor>().ref),
    .dawnTexelCopyBufferRowAlignmentLimits => DawnTexelCopyBufferRowAlignmentLimits.fromNative(ptr.cast<bindings.WGPUDawnTexelCopyBufferRowAlignmentLimits>().ref),
    .dawnTextureInternalUsageDescriptor => DawnTextureInternalUsageDescriptor.fromNative(ptr.cast<bindings.WGPUDawnTextureInternalUsageDescriptor>().ref),
    .dawnTogglesDescriptor => DawnTogglesDescriptor.fromNative(ptr.cast<bindings.WGPUDawnTogglesDescriptor>().ref),
    .dawnWGSLBlocklist => DawnWGSLBlocklist.fromNative(ptr.cast<bindings.WGPUDawnWGSLBlocklist>().ref),
    .dawnWireWGSLControl => DawnWireWGSLControl.fromNative(ptr.cast<bindings.WGPUDawnWireWGSLControl>().ref),
    .emscriptenSurfaceSourceCanvasHTMLSelector => EmscriptenSurfaceSourceCanvasHTMLSelector.fromNative(ptr.cast<bindings.WGPUEmscriptenSurfaceSourceCanvasHTMLSelector>().ref),
    .externalTextureBindingEntry => ExternalTextureBindingEntry.fromNative(ptr.cast<bindings.WGPUExternalTextureBindingEntry>().ref),
    .externalTextureBindingLayout => ExternalTextureBindingLayout.fromNative(ptr.cast<bindings.WGPUExternalTextureBindingLayout>().ref),
    .pipelineLayoutResourceTable => PipelineLayoutResourceTable.fromNative(ptr.cast<bindings.WGPUPipelineLayoutResourceTable>().ref),
    .renderPassDescriptorResolveRect => RenderPassDescriptorResolveRect.fromNative(ptr.cast<bindings.WGPURenderPassDescriptorResolveRect>().ref),
    .renderPassMaxDrawCount => RenderPassMaxDrawCount.fromNative(ptr.cast<bindings.WGPURenderPassMaxDrawCount>().ref),
    .requestAdapterWebGPUBackendOptions => RequestAdapterWebGPUBackendOptions.fromNative(ptr.cast<bindings.WGPURequestAdapterWebGPUBackendOptions>().ref),
    .requestAdapterWebXROptions => RequestAdapterWebXROptions.fromNative(ptr.cast<bindings.WGPURequestAdapterWebXROptions>().ref),
    .shaderModuleCompilationOptions => ShaderModuleCompilationOptions.fromNative(ptr.cast<bindings.WGPUShaderModuleCompilationOptions>().ref),
    .shaderSourceSPIRV => ShaderSourceSPIRV.fromNative(ptr.cast<bindings.WGPUShaderSourceSPIRV>().ref),
    .shaderSourceWGSL => ShaderSourceWGSL.fromNative(ptr.cast<bindings.WGPUShaderSourceWGSL>().ref),
    .sharedBufferMemoryD3D12SharedMemoryFileMappingHandleDescriptor => SharedBufferMemoryD3D12SharedMemoryFileMappingHandleDescriptor.fromNative(ptr.cast<bindings.WGPUSharedBufferMemoryD3D12SharedMemoryFileMappingHandleDescriptor>().ref),
    .sharedFenceDXGISharedHandleDescriptor => SharedFenceDXGISharedHandleDescriptor.fromNative(ptr.cast<bindings.WGPUSharedFenceDXGISharedHandleDescriptor>().ref),
    .sharedFenceDXGISharedHandleExportInfo => SharedFenceDXGISharedHandleExportInfo.fromNative(ptr.cast<bindings.WGPUSharedFenceDXGISharedHandleExportInfo>().ref),
    .sharedFenceEGLSyncDescriptor => SharedFenceEGLSyncDescriptor.fromNative(ptr.cast<bindings.WGPUSharedFenceEGLSyncDescriptor>().ref),
    .sharedFenceEGLSyncExportInfo => SharedFenceEGLSyncExportInfo.fromNative(ptr.cast<bindings.WGPUSharedFenceEGLSyncExportInfo>().ref),
    .sharedFenceMTLSharedEventDescriptor => SharedFenceMTLSharedEventDescriptor.fromNative(ptr.cast<bindings.WGPUSharedFenceMTLSharedEventDescriptor>().ref),
    .sharedFenceMTLSharedEventExportInfo => SharedFenceMTLSharedEventExportInfo.fromNative(ptr.cast<bindings.WGPUSharedFenceMTLSharedEventExportInfo>().ref),
    .sharedFenceSyncFDDescriptor => SharedFenceSyncFDDescriptor.fromNative(ptr.cast<bindings.WGPUSharedFenceSyncFDDescriptor>().ref),
    .sharedFenceSyncFDExportInfo => SharedFenceSyncFDExportInfo.fromNative(ptr.cast<bindings.WGPUSharedFenceSyncFDExportInfo>().ref),
    .sharedFenceVkSemaphoreOpaqueFDDescriptor => SharedFenceVkSemaphoreOpaqueFDDescriptor.fromNative(ptr.cast<bindings.WGPUSharedFenceVkSemaphoreOpaqueFDDescriptor>().ref),
    .sharedFenceVkSemaphoreOpaqueFDExportInfo => SharedFenceVkSemaphoreOpaqueFDExportInfo.fromNative(ptr.cast<bindings.WGPUSharedFenceVkSemaphoreOpaqueFDExportInfo>().ref),
    .sharedFenceVkSemaphoreZirconHandleDescriptor => SharedFenceVkSemaphoreZirconHandleDescriptor.fromNative(ptr.cast<bindings.WGPUSharedFenceVkSemaphoreZirconHandleDescriptor>().ref),
    .sharedFenceVkSemaphoreZirconHandleExportInfo => SharedFenceVkSemaphoreZirconHandleExportInfo.fromNative(ptr.cast<bindings.WGPUSharedFenceVkSemaphoreZirconHandleExportInfo>().ref),
    .sharedTextureMemoryAHardwareBufferDescriptor => SharedTextureMemoryAHardwareBufferDescriptor.fromNative(ptr.cast<bindings.WGPUSharedTextureMemoryAHardwareBufferDescriptor>().ref),
    .sharedTextureMemoryD3D11BeginState => SharedTextureMemoryD3D11BeginState.fromNative(ptr.cast<bindings.WGPUSharedTextureMemoryD3D11BeginState>().ref),
    .sharedTextureMemoryD3DSwapchainBeginState => SharedTextureMemoryD3DSwapchainBeginState.fromNative(ptr.cast<bindings.WGPUSharedTextureMemoryD3DSwapchainBeginState>().ref),
    .sharedTextureMemoryDXGISharedHandleDescriptor => SharedTextureMemoryDXGISharedHandleDescriptor.fromNative(ptr.cast<bindings.WGPUSharedTextureMemoryDXGISharedHandleDescriptor>().ref),
    .sharedTextureMemoryEGLImageDescriptor => SharedTextureMemoryEGLImageDescriptor.fromNative(ptr.cast<bindings.WGPUSharedTextureMemoryEGLImageDescriptor>().ref),
    .sharedTextureMemoryIOSurfaceDescriptor => SharedTextureMemoryIOSurfaceDescriptor.fromNative(ptr.cast<bindings.WGPUSharedTextureMemoryIOSurfaceDescriptor>().ref),
    .sharedTextureMemoryOpaqueFDDescriptor => SharedTextureMemoryOpaqueFDDescriptor.fromNative(ptr.cast<bindings.WGPUSharedTextureMemoryOpaqueFDDescriptor>().ref),
    .sharedTextureMemoryVkDedicatedAllocationDescriptor => SharedTextureMemoryVkDedicatedAllocationDescriptor.fromNative(ptr.cast<bindings.WGPUSharedTextureMemoryVkDedicatedAllocationDescriptor>().ref),
    .sharedTextureMemoryVkImageLayoutBeginState => SharedTextureMemoryVkImageLayoutBeginState.fromNative(ptr.cast<bindings.WGPUSharedTextureMemoryVkImageLayoutBeginState>().ref),
    .sharedTextureMemoryVkImageLayoutEndState => SharedTextureMemoryVkImageLayoutEndState.fromNative(ptr.cast<bindings.WGPUSharedTextureMemoryVkImageLayoutEndState>().ref),
    .sharedTextureMemoryZirconHandleDescriptor => SharedTextureMemoryZirconHandleDescriptor.fromNative(ptr.cast<bindings.WGPUSharedTextureMemoryZirconHandleDescriptor>().ref),
    .staticSamplerBindingLayout => StaticSamplerBindingLayout.fromNative(ptr.cast<bindings.WGPUStaticSamplerBindingLayout>().ref),
    .surfaceColorManagement => SurfaceColorManagement.fromNative(ptr.cast<bindings.WGPUSurfaceColorManagement>().ref),
    .surfaceDescriptorFromWindowsCoreWindow => SurfaceDescriptorFromWindowsCoreWindow.fromNative(ptr.cast<bindings.WGPUSurfaceDescriptorFromWindowsCoreWindow>().ref),
    .surfaceDescriptorFromWindowsUWPSwapChainPanel => SurfaceDescriptorFromWindowsUWPSwapChainPanel.fromNative(ptr.cast<bindings.WGPUSurfaceDescriptorFromWindowsUWPSwapChainPanel>().ref),
    .surfaceDescriptorFromWindowsWinUISwapChainPanel => SurfaceDescriptorFromWindowsWinUISwapChainPanel.fromNative(ptr.cast<bindings.WGPUSurfaceDescriptorFromWindowsWinUISwapChainPanel>().ref),
    .surfaceSourceAndroidNativeWindow => SurfaceSourceAndroidNativeWindow.fromNative(ptr.cast<bindings.WGPUSurfaceSourceAndroidNativeWindow>().ref),
    .surfaceSourceMetalLayer => SurfaceSourceMetalLayer.fromNative(ptr.cast<bindings.WGPUSurfaceSourceMetalLayer>().ref),
    .surfaceSourceWaylandSurface => SurfaceSourceWaylandSurface.fromNative(ptr.cast<bindings.WGPUSurfaceSourceWaylandSurface>().ref),
    .surfaceSourceWindowsHWND => SurfaceSourceWindowsHWND.fromNative(ptr.cast<bindings.WGPUSurfaceSourceWindowsHWND>().ref),
    .surfaceSourceXCBWindow => SurfaceSourceXCBWindow.fromNative(ptr.cast<bindings.WGPUSurfaceSourceXCBWindow>().ref),
    .surfaceSourceXlibWindow => SurfaceSourceXlibWindow.fromNative(ptr.cast<bindings.WGPUSurfaceSourceXlibWindow>().ref),
    .texelBufferBindingEntry => TexelBufferBindingEntry.fromNative(ptr.cast<bindings.WGPUTexelBufferBindingEntry>().ref),
    .texelBufferBindingLayout => TexelBufferBindingLayout.fromNative(ptr.cast<bindings.WGPUTexelBufferBindingLayout>().ref),
    .textureBindingViewDimension => TextureBindingViewDimension.fromNative(ptr.cast<bindings.WGPUTextureBindingViewDimension>().ref),
    .yCbCrVkDescriptor => YCbCrVkDescriptor.fromNative(ptr.cast<bindings.WGPUYCbCrVkDescriptor>().ref),
    .adapterPropertiesMemoryHeaps => AdapterPropertiesMemoryHeaps.fromNative(ptr.cast<bindings.WGPUAdapterPropertiesMemoryHeaps>().ref),
    .adapterPropertiesSubgroupMatrixConfigs => AdapterPropertiesSubgroupMatrixConfigs.fromNative(ptr.cast<bindings.WGPUAdapterPropertiesSubgroupMatrixConfigs>().ref),
    .dawnDrmFormatCapabilities => DawnDrmFormatCapabilities.fromNative(ptr.cast<bindings.WGPUDawnDrmFormatCapabilities>().ref),
    .pipelineLayoutPixelLocalStorage => PipelineLayoutPixelLocalStorage.fromNative(ptr.cast<bindings.WGPUPipelineLayoutPixelLocalStorage>().ref),
    .renderPassRenderAreaRect => RenderPassRenderAreaRect.fromNative(ptr.cast<bindings.WGPURenderPassRenderAreaRect>().ref),
    .sharedTextureMemoryAHardwareBufferProperties => SharedTextureMemoryAHardwareBufferProperties.fromNative(ptr.cast<bindings.WGPUSharedTextureMemoryAHardwareBufferProperties>().ref),
    .sharedTextureMemoryDmaBufDescriptor => SharedTextureMemoryDmaBufDescriptor.fromNative(ptr.cast<bindings.WGPUSharedTextureMemoryDmaBufDescriptor>().ref),
    .sharedTextureMemoryMetalEndAccessState => SharedTextureMemoryMetalEndAccessState.fromNative(ptr.cast<bindings.WGPUSharedTextureMemoryMetalEndAccessState>().ref),
    .textureComponentSwizzleDescriptor => TextureComponentSwizzleDescriptor.fromNative(ptr.cast<bindings.WGPUTextureComponentSwizzleDescriptor>().ref),
    .renderPassPixelLocalStorage => RenderPassPixelLocalStorage.fromNative(ptr.cast<bindings.WGPURenderPassPixelLocalStorage>().ref),
    _ => throw ArgumentError('Unknown chained struct type: $stype'),
  };
}
