name: vector
publish_to: none
resolution: workspace

environment:
  sdk: ^3.12.0

dependencies:
  flutter:
    sdk: flutter

  canvas:
  color:
  geometry:
  ink_stroke_modeler:
  shared:
  tool:
  vgc:
  vgc_renderer:
  ui:
  stack_ffi:

  vector_math: ^2.2.0